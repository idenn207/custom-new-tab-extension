# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**Primary**: 여러 프로젝트를 동시에 진행하는 IT 개발자 1인(이 확장을 만든 본인).

- 작업은 프로젝트 단위로 생기고 일정과 메모도 프로젝트 단위로 기록된다. 유지보수 업무도 특정 프로젝트에 속하며, 무소속 작업은 예외적으로만 존재한다
- 작업이 dev/stg/prod를 거치되 **경로가 작업마다 다르다.** 고정 파이프라인이 아니다
- **일정이 겹치는 것이 정상이다.** 여러 작업을 병행하며, 하나의 작업이 월·수에 걸치고 화요일에는 다른 작업을 하는 식으로 불연속하게 배치된다
- 프로젝트 수준 마감은 대개 외부(팀·릴리즈 창)가 정하지만, 개발 세션 단위 일정은 본인이 조정한다
- 필요가 발생하는 트리거는 **새 탭을 열고 다른 페이지로 이동하기까지의 몇 초**다. 그 몇 초 안에 오늘 손댈 것이 눈에 들어와야 한다

**Not for**: 시계 모드 사용자(캘린더 모드는 별개의 제품 목표를 가지며 시계 모드의 화면은 이 문서로 달라지지 않는다) · 팀과 일정을 공유하려는 사용자(로컬 저장만 하며 동기화 계층이 없다) · 시:분 단위 회의 일정을 관리하려는 사용자(all-day 날짜만 다룬다).

## Product Purpose

프로젝트 단위로 묶인 다단계 관문과 제목이 노출되는 "오늘 줄"이, **새 탭을 지나치는 몇 초 안에 오늘 손댈 작업이 무엇인지 상기시키고 그 작업의 기록을 한곳에 붙잡아두는 것**을 가능하게 한다.

성공은 하루치 실무를 넣어본 뒤 (a) 오늘 할 일의 제목이 클릭 0회로 읽히고, (b) 마감 확인 목적으로 외부 캘린더·노트 도구를 열지 않으며, (c) 메모장을 여는 이유가 남는다면 그것이 목록으로 나오는 것이다. (c)는 실패 조건이 아니라 다음 마일스톤의 입력이며 0을 목표로 삼지 않는다.

**이 제품에는 비교 대상이 없어 절대 기준을 세울 근거가 없다.** 그래서 지표를 만드는 대신 대체 행동의 소멸을 관찰한다. 물음은 "0이 되었는가"가 아니라 "무엇이 남았는가"이고, 남은 것이 곧 백로그가 된다.

## Positioning

**주인공은 배경 사진이 아니라 리마인드 표면이다.** 이 한 줄이 시계 모드와 갈리는 지점이며, 화면 판단의 기준을 배경 이미지의 가시 면적에서 **"상기되는가"**로 옮긴다.

이웃 제품이 그대로 베낄 수 없는 것은 **작업당 마감이 하나가 아니라는 전제**다. Linear는 이슈당 due date가 하나이고, Jira는 version 객체가 단순해 어느 환경에 배포됐는지 추적할 수단이 사실상 없으며, 계획 대비 실제는 팀용 Gantt 도구에만 있고 개인용 할 일 앱에는 없다. 여기서는 관문의 조합이 작업마다 다른 것이 정상이고, 그 다름을 옮기는 비용 없이 담는 것이 기능의 전부다 — **옮기는 비용이 기록의 이득을 넘는 순간 기록이 멈추기 때문이다. 실제로 멈췄다.**

## Operating Context

- 사용 장면은 새 탭 한 장이다. 스크롤하지 않고, 사용자는 일정한 DPI에서 본다
- **메인 위젯은 시계와 달력 중 택일이며 두 모드는 공존하지 않는다.** 이 문서가 다루는 것은 캘린더 모드 하나다
- 화면은 두 상태를 갖는다. **지나침**(새 탭을 통과하는 몇 초)과 **머무름**(작업을 읽거나 적는 상태)이며, 둘 사이의 계약은 명시적 진입 · **Esc** 이탈 · 흔적 없음이다. 머무름에서 빠져나오면 지나침의 화면이 그대로 돌아온다
- 저장은 로컬(`chrome.storage`)뿐이고 동기화 계층이 없다. 재생 불가 데이터이므로 백업 경로가 데이터 모델과 함께 온다
- 도구는 하나가 아니다 — 현재는 마감 확인이 외부 캘린더에서, 기록이 별도 메모장에서 일어난다. 이 문서의 제품은 그 둘을 여는 **이유**를 줄이는 것으로 판정된다

## Capabilities and Constraints

- 다루는 것은 **all-day 날짜**이며 시:분·타임존·반복 일정은 범위 밖이다. 용도가 마감일이지 회의 시간이 아니다
- **겹침을 막지 않는다.** 겹침은 이미 정상 상태이므로 경고를 띄우지 않는다 — 매일 뜨는 경고는 경고이기를 멈춘다. 부하는 경고 대신 표시만 한다
- **매니저의 경계는 제안까지 하고 결정은 사용자다.** 자동 일정 재배치를 하지 않는다. 겹침이 정상인 워크플로에서 도구가 일정을 옮기면 의도적으로 겹쳐 둔 배치를 조용히 망가뜨린다
- 계획 대비 실제는 저장하되 회고·통계 화면을 갖지 않는다. 결정을 내리는 순간에만 그 자리에서 한 줄로 나온다
- 진입점은 하나만 둔다. 지나치는 상태에 새 버튼을 늘리지 않는다
- **아직 정하지 않은 것**(발명하지 말 것) — 관문 프리셋 목록의 최종 범위, 메모 종류의 최종 개수, 연속 범위를 관문으로 승격하는 마이그레이션 방식, 부하 표시가 쓸 시각 축.

## Brand Commitments

**목표는 갈리지만 재료는 상속한다.** 같은 확장에서 나오는 두 모드이므로 패턴이 완전히 바뀌면 이질감이 생긴다. 이 모드는 시각 재료를 새로 발명하지 않고 시계 모드의 문서를 **이름으로** 가리키며, 가리킨 값을 다시 정하지 않는다.

- 이 모드는 `DESIGN.md`의 **4절 Elevation**(유리 표면의 정당화 조건), **4절 Named Rules**(The brightness Rule · The 표면을 누르지 알파를 올리지 않는다 Rule), **2절 Named Rules**(The 두 단계 Rule — 유리 알파는 0.3과 0.5 두 단계뿐이다), **3절 Typography**, **6절 Do's and Don'ts**, 그리고 `PRODUCT.md`의 Accessibility & Inclusion 절에 있는 대비 실측 표를 **이름으로 상속하며 값을 다시 정하지 않는다.**
- 화면의 hue 계열은 accent 하나이며 상태 색은 그 상태가 실제로 발생했을 때만 쓴다 — `DESIGN.md`의 2절 Colors 절을 따른다.
- 목소리는 담백한 한국어를 유지한다. 예외를 숨기지 않고 적는다.
- **패턴을 새로 발명하지 않는다.** 갈리는 것은 목표 층위뿐이고 재료는 상속한다.

## Evidence on Hand

- **관찰된 실패** — 이전 마일스톤을 실무에 부착하지 못했다. "실무에서 작업하는 패턴과 매칭되는 게 거의 없어서 측정 가치가 없었다"(사용자, 2026-08-20). 계획했던 2주 dogfooding은 수행되지 않았고, 수행할 수 없었다는 사실 자체가 데이터다
- **실패한 우회책 둘** — 별도 메모장 프로그램(따로 떠 있어 손이 안 가고 항목 10개 이상에서 탭 관리가 불가능하다) · 현재 캘린더(켜져 있으나 마감 확인은 여전히 외부 도구에서 한다)
- **코드 증거**(2026-08-21 기준) — 작업당 마감 날짜 1개 · 완료 시각을 남기는 필드 없음 · 그룹 필드 없음 · 요약 배너는 건수만 렌더 · 겹침을 막는 로직이 없음
- **운영 경험** — 고정 문구 배너는 대시보드에서 곧 무시된다(본인의 관리자 대시보드 제작·운영 경험). 반면 입장 목적에 정확히 답하는 단순한 값은 단순해도 집중을 끈다
- 근거 조사 3건 — `.claude/_meta/2026-08-20-calendar-notes-integration.md` · `.claude/_meta/2026-08-20-retrieval-and-manager-mode.md` · `.claude/_meta/2026-08-20-calendar-mode-work-goal.md`
- **약점을 숨기지 않는다 — 근거는 전부 단일 사용자의 자기 보고이고 정량 측정은 0건이다.** 이 문서는 그 위에 선다. 완화는 지표를 늘리는 것이 아니라 각 마일스톤을 하루 재현 테스트로 판정하는 것이다. 존재하지 않는 사용자·벤치마크·가격을 만들어 이 자리를 채우지 않는다

## Product Principles

1. **주인공은 리마인드 표면이다.** 화면 판단은 배경 이미지의 가시 면적이 아니라 "상기되는가"로 내린다. 시계 모드의 판정 기준을 그대로 들고 오면 이 모드는 자기 목표를 잃는다
2. **제안까지 하고 결정은 사용자가 한다.** 도구가 사용자의 배치를 대신 바꾸지 않는다. 겹침이 정상인 워크플로에서 자동 정리는 의도를 지우는 것과 같다
3. **옮기는 비용이 기록을 죽인다.** 한 작업의 일정이 밀릴 때 손으로 여러 개를 옮겨야 하면 기록이 멈춘다. 구조는 그 비용을 0에 가깝게 만드는 방향으로만 복잡해진다
4. **anti-reference는 지나치는 상태에만 적용한다.** "일처럼 느껴지면 실패"는 통과하는 몇 초에 대한 규칙이고, 머무름 상태에서 밀도가 올라가는 것은 실패가 아니다. 적용 범위를 갈라 두지 않으면 업무용 화면이 자기 일을 하지 못한다
5. **없는 것을 만들어 확신을 흉내내지 않는다.** 정량 지표를 새로 만들지 않고, 남은 것을 목록으로 남긴다

## Accessibility & Inclusion

- 대비 기준은 새로 정하지 않는다. `PRODUCT.md`의 Accessibility & Inclusion 절에 있는 대비 실측 표를 상속하며, 판정 방법은 토큰 계산이 아니라 **밝은 배경 위에서 렌더된 픽셀 실측**이다
- 색에만 의존하지 않는다 — 마감 상태와 중요도는 색과 형태로 이중 부호화한다
- 키보드만으로 조작할 수 있어야 한다. 그리드는 roving tabindex, 모달은 포커스 트랩과 Esc 이탈을 갖는다
- `prefers-reduced-motion: reduce`에서 전환을 제거한다
- 한국어 줄바꿈 규약(`word-break: keep-all` + `overflow-wrap`)은 `DESIGN.md`의 Typography 절에서 상속한다
