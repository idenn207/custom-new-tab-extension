# Product

## Register [셸]

product

## Users [셸]

만든 사람 본인을 포함한 개인 사용자. 하루에 수십 번, 브라우저에서 새 탭을 열 때마다 마주치는 화면이다.

사용 맥락이 이 제품의 모든 것을 규정한다. **사용자는 이 화면을 보러 오지 않는다.** 다른 곳으로 가는 길에 지나칠 뿐이다. 대부분은 1초 안에 검색어를 치거나 바로가기를 누르고 떠난다. 화면에 머무는 유일한 경우는 배경 사진을 잠깐 볼 때다.

해결하려는 일:

- 좋아하는 사진을 브라우저를 열 때마다 큰 화면으로 본다
- 그 사진을 가리지 않으면서, 새 탭에서 바로 검색하거나 자주 가는 곳으로 이동한다
- 필요 없는 요소는 끈다

## Product Purpose [시계]

**배경 사진을 보여주는 새 탭.** 크롬 기본 새 탭이 차지하던 자리를 사용자가 고른 사진으로 바꾸는 것이 이 확장의 존재 이유다.

시계, 검색창, 바로가기는 그 위에 얹는 **부가 옵션**이다. 이것은 수사가 아니라 코드의 사실이다. 설정에서 위젯 표시와 검색창 표시가 각각 독립 토글이고, 셋 다 끄면 사진과 배경 밝기 슬라이더만 남는다. 기능을 하나 더 만들 때 "이걸 켠 사람만 본다"가 성립하지 않으면 그 기능은 이 제품에 맞지 않는다.

성공은 기능 수가 아니라 **사진이 몇 퍼센트 보이느냐**로 측정된다. 화면에서 UI가 차지하는 면적이 늘어나는 방향의 변경은 그 자체로 후퇴다.

## Brand Personality [시계]

**조용한 / 개인적인 / 물러서는**

주인공은 사진이고, UI는 그 위에 떠 있는 유리판이다. 유리는 사진을 가리기 위한 판이 아니라, 사진을 통과시키면서 글자를 읽히게 하려고 고른 재료다. 이 구분이 이 제품 디자인의 전부다.

목소리는 담백한 한국어. 안내 문구는 사용자가 다음에 할 일을 그대로 말한다. 감탄하지 않고, 격려하지 않고, 설명을 늘리지 않는다. 개인 도구는 자기 소개를 할 필요가 없다.

한 가지 예외가 있다. **저장에 실패했을 때는 물러서지 않는다.** 사용자가 등록한 바로가기와 업로드한 사진은 이 확장에서 재생 불가능한 데이터다. 조용함보다 정직함이 우선한다.

## Anti-references [시계]

- **업무용 SaaS 대시보드** (Notion, Asana 류). 새 탭이 "일"처럼 느껴지면 실패다. 통계 카드, 진행률 바, 대시보드 그리드를 쓰지 않는다.
- **알록달록한 위젯 모음** (포털 시작페이지 류). 날씨, 뉴스, 주식 타일이 색색으로 깔린 화면. 화면의 hue 계열은 accent 하나로 유지하고, 상태 색(경고, 위험)은 그 상태가 실제로 발생했을 때만 나타난다.
- **Windows 기본 앱 스타일**. 각지고 회색진 네이티브 앱 UI. 유리 질감과 둥근 모서리를 유지한다.
- **불투명 패널로 덮은 배경화면 앱.** 사진을 배경으로 깔아 놓고 그 위에 불투명 카드를 얹으면 사진은 액자 테두리로 전락한다. 이 제품이 피하려는 것 중 가장 가까이 있는 실패다.

## Design Principles [시계]

### 1. 배경이 주인공이다

위젯은 사진 위에 떠 있고, 사진을 대체하지 않는다. 새 표면을 추가할 때 기본값은 "더 투명하게, 더 작게"다. 면적을 늘리는 변경에는 무엇을 줄여서 상쇄했는지가 함께 있어야 한다.

### 2. 글래스모피즘은 장식이 아니라 이 제품의 유일한 해법이다

배경이 사용자 사진이라는 제약에서 출발하면 표면 선택지는 셋뿐이다.

- 불투명 패널: 글자는 읽히지만 사진이 사라진다. 원칙 1과 정면 충돌한다.
- 표면 없이 글자만: 사진이 다 보이지만 밝은 사진에서 글자가 사라진다. 실측으로 시계는 흰 배경에서 **1.0:1**까지 떨어진다.
- 반투명 + `backdrop-filter`: 사진이 표면을 통과해 보이면서, 그 자리의 사진만 눌러 글자를 띄운다.

세 번째만 원칙 1과 원칙 3을 동시에 만족한다. 그래서 유리는 이 제품에서 **기본 표면**이고, 다른 디자인 맥락에서 유리를 장식으로 남용하는 것과는 성격이 다르다.

다만 이 정당화는 조건부다. **유리가 다음 중 하나라도 하면 그 자리에서는 유리를 쓰지 않는다.**

- 뒤가 이미 어두운 스크림으로 덮여 있어 사진이 통과하지 않을 때 (전면 모달 내부). 이때 유리는 비용만 내고 얻는 것이 없다.
- 블러 뒤에 아무 사진도 없을 때 (사이드바 내부에 중첩된 유리).
- 유리를 걷어내도 대비가 그대로일 때. 그러면 그 유리는 순수한 장식이다.

### 3. 읽히지 않는 정보는 정보가 아니다

배경은 사용자 사진이므로 밝기를 통제할 수 없다. 대비는 "어두운 배경일 때"가 아니라 **가장 밝은 배경일 때** 기준으로 맞춘다. 이 원칙이 원칙 1과 충돌하면, 그 표면에 한해 원칙 3이 이긴다.

판정은 토큰 값 계산이 아니라 **밝은 배경을 깐 상태에서 렌더된 픽셀 실측**으로 한다. 유리는 합성 결과가 CSS 값과 다르므로 계산으로는 판정할 수 없다.

### 4. 배경 밝기 슬라이더는 대비의 근거가 될 수 없다

배경 어둡게(오버레이) 값은 0에서 100까지 사용자가 정하고, 100이면 오버레이 알파가 정확히 0이 된다. 즉 **사용자는 언제든 대비 보정을 완전히 꺼 버릴 수 있다.**

따라서 각 표면은 오버레이가 없다고 가정하고 자기 대비를 스스로 확보해야 한다. 오버레이는 취향 조절이지 접근성 장치가 아니다.

### 5. 저장된 척하지 않는다

실패한 쓰기는 UI에 반영하지 않는다. 사용자가 등록한 것이 사라졌다면 사라졌다고 말한다.

### 6. 기존 사용자의 화면을 바꾸지 않는다

새 기능은 언제나 꺼진 상태로 도착한다. 설정 마이그레이션은 레거시 키를 보존해 구버전으로 되돌아가도 동작하게 둔다.

## Accessibility & Inclusion [셸]

**목표는 WCAG 2.1 AA다.** 본문 4.5:1, 큰 텍스트(18px 이상 또는 굵은 14px 이상) 3:1, 비텍스트 UI 컴포넌트 3:1. 이 목표는 유지하되, 현재 셸이 목표에 미달한다는 사실을 아래에 그대로 적어 둔다.

### 현재 대비 실측 (2026-08-19)

흰색 배경 사진(가장 밝은 조건), 1440x900, 달력 제외. 렌더된 PNG 픽셀에서 유리 표면 색을 읽고 그 위에 텍스트 알파를 합성해 산출했다.

| 요소 | 크기/알파 | 밝기 50 (기본값) | 밝기 100 (최악) | 기준 | 판정 |
|---|---|---|---|---|---|
| 고정 바로가기 이름 | 14px / 0.95 | 3.02:1 | **1.94:1** | 4.5:1 | 미달 |
| 날짜 표시 | 14px / 0.95 | 3.65:1 | **1.94:1** | 4.5:1 | 미달 |
| 사이드바 즐겨찾기 이름 | 14px / 0.95 | 7.68:1 | 5.77:1 | 4.5:1 | 통과 |
| 검색 플레이스홀더 | 16px / 0.65 | 3.72:1 | **2.49:1** | 4.5:1 | 미달 |
| 검색 입력 텍스트 | 16px / 0.95 | 5.86:1 | **3.50:1** | 4.5:1 | 밝기 60 이상에서 미달 |
| 설정 모달 설명 | 12px / 0.65 | 5.75:1 | 5.75:1 | 4.5:1 | 통과 (스크림 위) |
| 시계 숫자 | 144px / 0.95 | 1.86:1 | **1.00:1** | 3:1 | 미달 (그림자로만 읽힘) |
| 코너 아이콘 버튼 | 0.95 | 5.63:1 | 3.43:1 | 3:1 | 통과 |
| 검색 버튼 아이콘 | 0.95 | 4.59:1 | 3.60:1 | 3:1 | 통과 |

**텍스트 색으로 할 수 있는 일은 이미 했다.** 고정 바로가기 이름과 날짜 표시의 알파를 0.65에서 0.95로 올려 밝기 50에서 2.25 → 3.02, 2.78 → 3.65로 끌어올렸지만, 밝기 100에서는 1.94:1로 여전히 미달이다.

남은 원인은 전부 **표면**이다. 흰 사진 위에서 알파 0.3 유리는 sRGB 183으로 렌더되고, 그 위에서는 불투명 흰색(알파 1.0) 텍스트조차 2.00:1에 그친다. 반대로 유리가 겹치거나(사이드바 항목, sRGB 97) 스크림이 깔린 자리(설정 모달, sRGB 60)는 같은 텍스트 색으로 기준을 넘는다. 즉 대비는 텍스트가 아니라 그 자리의 표면이 정한다. 해결값은 DESIGN.md의 4절 Elevation 절에 있는 The brightness Rule이 정한다 — 본문을 담는 단일 유리에 표면을 누르는 필터를 함께 거는 것이고, 그 값은 그 규칙이 갖는다. 여기에 옮겨 적지 않는다.

### 그 외

- **색에만 의존하지 않는다.** 상태는 색상과 함께 형태나 아이콘으로도 표시한다.
- **키보드.** 모든 조작은 마우스 없이 가능해야 한다. 현재 셸 컨트롤(코너 토글 3종, 고정 바로가기, 사이드바 항목, 검색 버튼, 모달 버튼, 토글 스위치, 위치 그리드)에는 `:focus-visible` 링이 없다. 코드베이스의 `:focus-visible` 규칙 16개는 전부 달력과 그 인접 컨트롤에 있다.
- **모션.** `prefers-reduced-motion: reduce`에서 전환을 제거한다. 현재 이 블록도 달력 위주이며, 셸의 전환 48건(사이드바 슬라이드, 위치 이동, 호버 확대 포함)은 감축 대상에서 빠져 있다.
- **터치/클릭 타깃.** 시각 크기가 작은 컨트롤은 가상 요소로 히트 영역을 넓힌다.
- **한국어 줄바꿈.** 문장에는 `word-break: keep-all`을 쓰고, URL처럼 끊을 곳이 없는 문자열을 위해 `overflow-wrap: anywhere`를 함께 둔다.

## 이 문서의 범위 [시계]

**달력 위젯의 제품 판단은 이 문서가 하지 않는다 — 별도 문서로 위임한다.** 캘린더 모드는 업무용이고 이 문서가 기술하는 것은 가정용 시계 모드라, 두 모드는 주인공도 성공 지표도 다르다. 그 판단은 `PRODUCT.calendar.md` 가 갖는다.

문서에 남는 달력 관련 사실은 구조적인 것 하나뿐이다. **메인 위젯은 시계와 달력 중 택일이며 공존하지 않는다.** 새 메인 위젯을 만들 때 이 제약이 유지된다.

달력 모드의 시각 판정 기준은 `DESIGN.calendar.md` 에 있고, 구체 토큰은 코드가 생긴 뒤 `/impeccable document` 의 scan 모드가 채운다.
