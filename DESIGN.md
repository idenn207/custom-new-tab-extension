# Design

현재 코드(`newtab.css`)에서 캡처한 시각 시스템이다. 새 표면을 만들 때 여기 있는 값을 먼저 쓰고, 없을 때만 새로 정한다.

## Theme

**다크 전용.** 라이트 테마는 없고 앞으로도 만들지 않는다.

이유는 취향이 아니라 구조다. 배경이 사용자 사진이므로 표면은 항상 사진 위에 뜬 반투명 판이고, 그 위의 글자는 흰색이어야 어떤 사진에서도 성립한다. 검은 글자는 어두운 사진에서 즉시 무너진다.

## Color

색은 rgba로 정의한다(OKLCH 아님). 반투명이 이 디자인의 핵심 재료라 알파를 직접 다루는 편이 정확하다.

### 표면

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `--bg-primary` | `rgba(12, 12, 14, 0.92)` | 모달처럼 배경을 가려야 하는 표면 |
| `--bg-primary-glass` | `rgba(12, 12, 14, 0.5)` | 반투명 오버레이 |
| `--bg-secondary` | `rgba(18, 18, 20, 0.95)` | 사이드바 본체 |
| `--bg-secondary-glass` | `rgba(18, 18, 20, 0.3)` | 떠 있는 유리 카드 (날짜 표시, 고정 즐겨찾기) |
| `--border-color` | `rgba(255, 255, 255, 0.08)` | 모든 경계선 |
| `--hover-bg` | `rgba(255, 255, 255, 0.06)` | 호버 배경 |

> **예외**: 달력 위젯은 배경 알파를 올리는 대신 **`backdrop-filter`로 배경을 눌러** 대비를 확보한다. 실제 값은 `--bg-secondary-glass`(0.3) + `blur(10px) saturate(115%) brightness(0.62)`이다. 13px 숫자 42칸이 밝은 배경 사진 위에서 AA에 미달했기 때문인데, 알파를 0.7까지 올리면 사진이 그만큼 죽어 원칙 1과 정면으로 부딪힌다. `brightness()`는 사진의 밝은 부분만 눌러서 같은 대비를 더 적은 비용으로 얻는다. 조밀한 텍스트를 담는 새 위젯은 이 조합을 따른다.
>
> (이 문단은 한동안 "알파 0.7"이라고 적혀 있었다. 코드에 그런 값은 없었다.)

### 텍스트

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `--text-primary` | `rgba(255, 255, 255, 0.95)` | 본문, 숫자, 제목 |
| `--text-secondary` | `rgba(255, 255, 255, 0.65)` | 라벨, 보조 설명, 플레이스홀더 |

비활성/약화 텍스트의 알파 하한은 **0.55**다. 그 아래는 밝은 배경 사진 위에서 AA에 미달한다.

### 강조

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `--accent-color` | `rgba(88, 128, 230, 0.5)` | 검색 버튼, 포커스 |
| `--calendar-accent` | `rgba(126, 160, 245, 0.95)` | 오늘 날짜, 선택 상태, 포커스 링 |
| `--calendar-overdue` | `rgba(255, 118, 110, 0.95)` | 기한 초과 |
| `--calendar-soon` | `rgba(242, 190, 104, 0.95)` | 내일 마감 |

상태 색은 **항상 dot 인디케이터와 함께** 쓴다. 색만으로 상태를 전달하지 않는다.

## Typography

시스템 폰트 스택. 웹폰트를 로드하지 않는다 — 새 탭은 매번 새로 그려지므로 폰트 로딩 지연이 곧 깜빡임이다.

```
-apple-system, BlinkMacSystemFont, 'Segoe UI', 'Noto Sans KR', sans-serif
```

| 크기 | 쓰임 |
|---|---|
| 144px | 시계 (`center-*` 위치) |
| 80px | 시계 (모서리 위치) |
| 16px | 달력 월 제목, 섹션 제목 |
| 14px | 설정 항목 라벨 |
| **13px** | 본문 기본 — 날짜 숫자, 할 일 제목, 입력창 |
| 12px | 보조 설명, 버튼 라벨 |
| 11px | 요일 약자 |

굵기는 400 / 500 / 600 / 700 네 단계. 위계는 크기보다 **굵기와 색 알파**로 만든다. 좁은 위젯 안에서 크기를 키우면 줄바꿈이 깨진다.

한국어 문장에는 `word-break: keep-all`을 쓴다. 어절 중간에서 꺾이면 읽기 리듬이 끊긴다. 단 URL처럼 끊을 곳이 없는 문자열을 위해 `overflow-wrap: anywhere`를 함께 둔다.

## Spacing

4pt 기반 스케일.

| 토큰 | 값 |
|---|---|
| `--space-2xs` | 4px |
| `--space-xs` | 8px |
| `--space-sm` | 12px |
| `--space-md` | 16px |
| `--space-lg` | 24px |

## Shape

`border-radius`는 표면 크기에 비례한다.

| 값 | 쓰임 |
|---|---|
| 16px | 큰 위젯 (달력) |
| 10~12px | 카드, 유리 패널 |
| 6~8px | 버튼, 입력창, 배너 |
| 3~5px | 체크박스, dot |
| 50% | 원형 아이콘 버튼 |

## Glass

이 디자인의 시그니처다. 세 가지가 함께 와야 성립한다.

```css
background: var(--bg-secondary-glass);   /* 또는 밀도 높은 표면은 0.7 */
backdrop-filter: blur(10px);
border: 1px solid var(--border-color);
```

`blur(10px)`이 기본값이다. 달력은 `blur(10px) saturate(115%) brightness(0.62)`로 배경을 눌러 글자를 띄운다.

유리는 **떠 있는 표면에만** 쓴다. 배경 자체나 전면 모달에는 쓰지 않는다.

## Layout

### 위치 시스템

검색창과 **시계**는 각각 9개 위치(`top|center|bottom` × `left|center|right`) 중 하나를 갖는다. `position-{v}-{h}` 클래스와 `transform` 조합으로 배치하며, JS는 클래스만 토글한다.

- 위치 클래스는 `position-*` / `overlap-offset` / `collision-compact` 셋만 positioning이 소유한다. 그 외 클래스(`clock`, `calendar-widget`, `enable-transition`)는 위치 변경 시 보존한다.
- 같은 위치일 때 `overlap-offset`으로 서로 비킨다.
- 위치가 달라도 겹칠 수 있으므로 `getBoundingClientRect()`로 실측해 교차를 확인한다.

> **달력은 이 시스템 밖에 있다.** 달력 위젯은 상단 전체 너비 밴드이고 고를 위치가 없다. `.calendar-widget`의 `position-*` / `overlap-offset` / `collision-compact` CSS 규칙은 전부 삭제했다 — 남겨 두면 JS가 여전히 붙이는 `position-*` 클래스가 밴드의 `inset`을 덮어 위치를 바꿀 때마다 밴드가 흔들린다.
>
> 저장값 `mainWidgetPosition`은 달력 모드에서 **읽지도 쓰지도 않는다.** 설정에서 위치 그리드만 숨기므로, 시계로 되돌리면 이전에 고른 위치가 그대로 복원된다.
>
> 달력이 떠 있으면 `body.has-calendar-band`가 켜지고 JS가 밴드 실측 높이를 `--calendar-band-height`로 내보낸다. 상단 검색창 3종(`position-top-*`)만 그 높이만큼 아래로 비킨다. 검색창의 9위치 설정 자체는 그대로 남는다 — 밴드가 생겼다고 사용자가 이미 고른 배치를 빼앗지 않는다.

### 브레이크포인트

| 폭 | 변화 |
|---|---|
| ≤1024px | 위젯 상하 여백 축소, 시계 폰트 축소, 밴드 좌우 패딩 축소 |
| ≤860px | 밴드 본문이 1열로 접힌다 (할 일 패널이 달력 아래로) |
| ≤768px | 밴드 높이 상한 `min(56vh, 420px)` + 칸 높이 40px, 사이드바 전체 폭 |

## Motion

| 대상 | 값 |
|---|---|
| 위치 전환 | `0.3s ease` (top/bottom/left/right/transform/max-width) |
| 호버, 색 변화 | `0.15s ease` |

두 가지 규칙:

1. **초기 렌더에는 트랜지션을 걸지 않는다.** `.enable-transition` 클래스를 사용자 조작 시점에만 부여한다. 첫 페인트에 전환이 걸리면 위젯이 화면을 가로질러 날아온다.
2. **달력 밴드는 위치 전환 자체가 없다.** 상단에 고정되어 움직이지 않으므로 `.calendar-widget.enable-transition` 규칙도 삭제했다. (폭을 바꾸던 `collision-compact`가 사라져 "매 프레임 `ResizeObserver`가 발화한다"는 옛 위험도 함께 없어졌다.)

`prefers-reduced-motion: reduce`에서 전환을 제거한다.

## Components

| 컴포넌트 | 위치 |
|---|---|
| 시계 (`.clock`) | 시:분, 144px / 모서리에서 80px |
| 달력 (`.calendar-widget`) | 상단 전체 너비 밴드. 6주 고정 그리드 + 오른쪽 할 일 패널 + 요약 한 줄 |
| 일정 상세 (`#calendarEventModal`) | 범위 · 중요도 · 작업 메모. body 직속 |
| 검색창 (`.search-container`) | 9위치. 시계와 같은 위치일 때만 위젯 폭에 맞춘다 (달력 밴드에서는 맞추지 않고, 상단이면 밴드 아래로 비킨다) |
| 사이드바 (즐겨찾기 / 배경 이미지) | 좌우 슬라이드, 320px |
| 설정 모달 (`.modal`) | 좌측 탭 네비 + 우측 패널 |
| 유리 카드 | 날짜 표시, 고정 즐겨찾기 |

### 달력 내부 규칙

**밴드**

- 상단 고정, 좌우 0, 아래 모서리만 둥글다(16px). 위쪽은 화면 끝에 붙는다.
- 높이 상한은 `min(48vh, 480px)`이고 넘치는 만큼은 `.calendar-body`가 스크롤한다. 폭을 줄여 밀도를 낮추던 `collision-compact` 경로를 이것이 대신한다 — 폭이 100%라 줄일 폭이 없다.
- 표면은 화면 끝까지 가지만 **내용은 `--calendar-measure`(1440px) 안에서 가운데 정렬**된다. 상한이 없으면 초광폭 모니터에서 날짜 칸 하나가 400px가 되어 숫자 하나가 허허벌판에 놓인다.
- 헤더는 `‹ 월 › [오늘]` 한 덩어리를 왼쪽에 모으고, 요약 한 줄과 `일정 추가`를 오른쪽 끝으로 민다. 제목을 `flex: 1`로 늘려 가운데 두면 화살표가 1400px 떨어진 양끝으로 흩어진다.

**그리드**

- **6주 42칸 고정**. 달마다 높이가 변하면 아래 요소가 밀린다.
- 인덱스는 전역이 아니라 **표시 중인 42일 창**에만 만든다. 범위 일정을 전역으로 날짜 전개하면 최악의 경우 `이벤트 수 × 범위 일수`(5000 × 366)만큼 엔트리가 생긴다.
- 날짜 숫자는 칸 **왼쪽 위**. 밴드에서 칸이 가로로 넓어 가운데 정렬하면 숫자가 빈 칸 한가운데 떠 있는 꼴이 된다.

**칩** (`.calendar-chip`)

- 셀당 최대 **2개 + `+N`**. dot 3개였던 자리보다 하나 적다 — 칩은 가로로 길어 3개를 넣으면 날짜 숫자가 밀린다.
- 칩 하나가 두 가지를 동시에 말한다.
  - **범위에서의 위치 → 모서리 모양.** 하루짜리(`is-single`)는 양끝이 둥글고, `is-start`는 왼쪽만, `is-end`는 오른쪽만, `is-middle`은 양끝이 각져 좌우 칸으로 이어져 보인다.
  - **중요도 → 두께 3단계**(low 3px / normal 4px / high 7px)와 같은 hue의 알파 단계(0.55 / 0.78 / 1).
- **중요도에 새 색을 만들지 않는다.** 화면에 이미 accent(파랑) · overdue(빨강) · soon(주황) 3계열이 있어서 중요도 3색을 더하면 6계열이 되고 "알록달록한 위젯 모음" anti-reference에 정면으로 걸린다. 1차 부호가 색이 아니라 두께이므로 화면을 회색조로 낮춰도 중요도가 읽힌다.
- 마감 상태가 hue를 정한다(`is-due-overdue` / `is-due-soon`). 완료(`is-done`)는 색을 잃고 회색으로 물러나되 두께는 유지해 범위는 계속 읽힌다.

**할 일 패널**

- 밴드 안에서 달력 **오른쪽 열**에 선다(`minmax(260px, 320px)`, 1px 세로 구분선). 아래로 쌓으면 밴드 높이가 두 배가 되고 그만큼 사진을 더 가린다. 340px 카드 시절 "하단 펼침"이었던 이유(위젯 폭을 지켜 위치 9종을 성립시킨다)는 위치 시스템과 함께 사라졌다.
- ≤860px에서는 1열로 접히고 구분선이 위쪽으로 옮겨 간다.
- 목록은 고정 높이가 아니라 남는 높이를 다 쓰고 넘치면 스크롤한다. 밴드 높이 상한이 이미 전체를 묶고 있다.
- 항목은 제목 + (여러 날이면) 기간 + (보통이 아니면) 중요도 + (있으면) 메모 2줄까지.

**요약 한 줄** (`.calendar-summary`)

- `오늘 마감 2건 · 지연 1건` 형태의 **문장 한 줄**. 카드도 타일도 진행률 바도 아니다.
- **0건이면 노드 자체가 없다.** "마감 없음"이라고 말하려고 한 줄을 차지하지 않는다.
- 절충: `aria-live`는 이미 문서에 있는 노드의 변화만 알리므로, 0 → 1건 전환은 보조기기가 못 읽을 수 있다. "없을 때 아무것도 없다"를 우선했다.

**기타**

- 저장 실패 배너는 텍스트가 한 줄을 다 쓰고 버튼이 아랫줄로 내려간다.
- 일정 상세 모달(`#calendarEventModal`)은 **달력 루트 밖(body 직속)** 에 있다. 밴드 안에 두면 밴드의 `max-height`와 `overflow`에 갇혀 잘린다.

## Content

- 사용자가 입력한 문자열은 **항상 `textContent`**로 렌더한다. `innerHTML`은 하드코딩된 SVG 아이콘에만 쓴다.
- 버튼 라벨은 동사 + 목적어("변경 취소", "다시 시도").
- 오류 문구는 무슨 일이 일어났는지와 그 결과를 함께 말한다("할 일을 저장하지 못했습니다. 변경 사항은 적용되지 않았습니다").
