<!-- SEED: established with the user before implementation; re-run /impeccable document once there's code to capture the actual tokens and components. -->

---
name: 업무 캘린더 모드
description: 새 탭을 지나치는 몇 초 안에 오늘 손댈 작업을 상기시키는 리마인드 표면. 시계 모드의 유리 언어를 상속하고 목표 층위만 갈아 끼운다.
---

## Overview

**주인공은 리마인드 표면이다.** 시계 모드에서 주인공은 배경 이미지였고 UI는 그것을 가리지 않는 유리판이었다. 이 모드에서는 그 관계가 뒤집힌다 — 표면이 무엇을 말하는지가 먼저이고, 배경은 그 말이 읽히는 조건이다.

**시각 세계를 새로 만들지 않는다.** 같은 확장에서 나오는 두 모드이므로 재료가 갈리면 이질감이 생긴다. 유리 표면의 정당화 조건은 `DESIGN.md`의 4절 Elevation 절에서, 대비 판정 방법은 `PRODUCT.md`의 Accessibility & Inclusion 절에서, 타이포와 한국어 줄바꿈은 `DESIGN.md`의 Typography 절에서 각각 이름으로 상속한다. 갈리는 것은 네 층위뿐이다 — 주인공, 성공 지표, anti-reference의 적용 범위, 두 상태의 계약.

**The 상기 Rule.** 이 모드의 화면 판단은 "상기되는가"로 내린다. 배경 이미지의 가시 면적은 이 모드에서 판정 기준이 아니며, 그 기준을 그대로 들고 오면 화면은 자기 일을 하지 못한 채 얌전해진다. 어떤 변경이 상기를 돕는지 답하지 못하면 그 변경은 근거가 없다.

**The 지나침이 기본 Rule.** 기본 상태는 통과다. 화면은 클릭 없이 읽히는 것만 지나침 상태에 두고, 나머지는 머무름으로 미룬다. 지나침에 정보를 더하는 변경은 무엇을 뺐는지 함께 있어야 한다.

**모션 문법은 상속한다.** 등장 연출·스크롤 시퀀스·스태거를 넣지 않으며 모션은 상태 전달에만 쓴다 — `DESIGN.md`의 1절 Overview 절과 그 안의 상태 전달 규칙을 따른다. `prefers-reduced-motion: reduce`에서는 전환을 제거한다.

**메인 위젯은 시계와 달력 중 택일이며 공존하지 않는다.** 이 문서가 다루는 것은 그 택일에서 달력을 고른 상태 하나다.

**이미지 스탠스**: 이 모드는 자체 이미지를 갖지 않는다. 배경은 사용자 설정이 정하고 표면은 그 위에서 자기 대비를 스스로 확보한다.

## Colors

**전략은 Restrained이며 상속이다.** 화면의 hue 계열은 accent 하나이고 상태 색은 그 상태가 실제로 발생했을 때만 나타난다 — `DESIGN.md`의 2절 Colors 절이 정한 그대로이며 이 문서는 값을 다시 정하지 않는다.

캘린더 모드가 더하는 것은 **역할 하나**다. 마감 상태(예정·임박·지남·완료)가 색을 쓰는 첫 소비자가 되며, 그 색들은 새 hue가 아니라 이미 있는 상태 색 어휘에서 나온다. 구체 값은 `[to be resolved during implementation]`.

**The 이중 부호화 Rule.** 마감 상태와 중요도는 색에만 실리지 않는다. 색과 형태를 함께 쓰며, 색을 지웠을 때 정보가 남는지가 통과 조건이다. 상태 색은 이 모드에서 매일 나타나므로, 색 하나에 의미를 몰아 두면 그 색이 곧 배경처럼 읽힌다.

## Typography

**서체와 위계는 상속한다.** 웹폰트를 더 늘리지 않고 굵기는 가변 폰트의 축에서 꺼내며, 한국어 줄바꿈 규약과 고정폭 숫자 규약을 그대로 쓴다 — `DESIGN.md`의 Typography 절과 그 안의 규칙들을 이름으로 가리킨다. 서체를 새로 고르지 않는다.

캘린더 모드가 여는 것은 **본문 위계 한 단계**다. 오늘 줄이 건수가 아니라 제목을 담으면 그 제목은 기존 배너 문구보다 길고 작아질 수 있고, 밴드 위 텍스트는 이미 실측에서 취약한 자리다. 크기·자간·행간은 `[to be resolved during implementation]`.

**The 재측정 Rule.** 오늘 줄이 제목을 담으면 대비를 다시 잰다. 재측정 절차는 세 축을 갖추며 셋 다 `PRODUCT.md`의 Accessibility & Inclusion 절에 있는 대비 실측 표에서 이름으로 온다 — **무엇을 무슨 배경 위에 렌더해 재는가**(실제 표면 위의 실제 글자를, 가장 불리한 밝은 배경에서), **어떤 값을 읽는가**(토큰 계산이 아니라 렌더된 픽셀), **어떤 기준과 대조하는가**(그 표가 정한 본문·보조 기준). 값을 이 문서에 옮기지 않는다 — 옮기면 두 문서가 갈라지고, 갈라지면 어느 쪽이 참인지 아무도 모른다.

## Layout

공간 문법은 상속한다. 새 탭은 스크롤하지 않고 사용자는 일정한 DPI에서 보므로, 반응형은 구조로 처리하고 유동 타이포를 쓰지 않는다 — `DESIGN.md`의 1절 단일 화면 규칙을 따른다.

이 모드가 더하는 배치 규칙은 둘이다.

**The 한 줄이 먼저 Rule.** 지나침 상태에서 읽히는 것은 한 줄이다. 그 줄이 자리를 얻고 나머지가 그 아래에 오며, 반대 순서로 배치하면 몇 초 안에 읽히는 것이 사라진다.

**The 밀도는 상태를 따른다 Rule.** 지나침은 성기고 머무름은 조밀하다. 두 밀도는 같은 데이터의 다른 투영이며 사용자가 설정에서 고른다. 자동 전환하지 않는다 — 화면이 스스로 바뀌면 사용자는 매번 어느 상태인지부터 확인해야 한다.

구체 간격·그리드·중단점은 이 마일스톤에서 정하지 않는다. `[to be resolved during implementation]`.

## Elevation & Depth

**유리는 장식이 아니라 이 제품의 해법이며, 그 정당화 조건은 상속한다.** 유리 표면을 언제 쓰는지, 알파를 몇 단계로 묶는지, 대비가 부족할 때 무엇을 조정하는지는 전부 `DESIGN.md`의 4절 Elevation 절과 그 안의 규칙들이 정한다. 이 문서는 그 값을 다시 정하지 않는다.

**The 근거를 다시 쓴다 Rule.** 재료는 상속하되 근거가 시계 모드의 성공 지표에 매여 있으면 근거만 다시 쓴다. 알파를 올리지 않고 표면을 누르는 이유는 이 모드에서 **면적 예산이 아니라** 상기이기 때문이다 — 알파를 올리면 표면이 균일하게 무거워져 오늘 줄과 나머지의 위계가 함께 뭉개지고, 위계가 뭉개지면 몇 초 안에 무엇을 봐야 하는지가 사라진다. 결론(알파를 올리지 않는다)은 같고 근거가 갈린다. 근거를 옮겨 오지 않으면 다음 마일스톤이 "이 모드에는 그 예산이 없으니 알파를 올려도 된다"로 갈 수 있고, 그 경로는 형식 검사를 전부 통과한다.

**The 겹침은 깊이가 아니다 Rule.** 겹친 일정은 정상 상태이므로 깊이로 표현하지 않는다. 표면을 쌓으면 겹침이 많은 날일수록 무거워지고, 무거움은 경고로 읽힌다. 이 제품은 겹침을 경고하지 않는다.

## Shapes

형태 언어는 상속한다 — 모서리 반경, 테두리의 굵기와 색, 포커스 링은 시계 모드와 같은 것을 쓴다. 캘린더 모드는 형태를 새로 발명하지 않는다.

형태가 이 모드에서 얻는 새 일은 **부호화**다.

**The 다른 축 Rule.** 부하와 중요도는 같은 시각 축을 쓰지 않는다. 중요도가 이미 유리 알파 단계를 쓰고 있으므로, 겹친 부하는 형태로 가른다 — 테두리, 패턴, 또는 개수 표기이며 셋 중 무엇인지는 다음 마일스톤이 정한다. 알파는 쓸 수 없고(단계가 이미 묶여 있고 중요도가 점유했다), hue를 늘리는 것도 쓸 수 없다(accent가 하나다). 남는 축이 형태다.

구체 반경·굵기·패턴은 이 마일스톤에서 정하지 않는다. `[to be resolved during implementation]`.

## Do's and Don'ts

### Do

- **두 상태의 계약을 지킨다.** 머무름은 명시적 진입으로만 들어가고 **Esc**로 나오며, 나오면 흔적을 남기지 않는다. 지나침의 화면이 그대로 돌아온다
- **제안까지 하고 결정은 사용자에게 남긴다.** 일정을 대신 옮기지 않는다
- **색을 지웠을 때 정보가 남는지 확인한다.** 마감 상태와 중요도는 색과 형태로 이중 부호화한다
- **키보드만으로 도달한다.** 그리드는 roving tabindex, 모달은 포커스 트랩과 Esc 이탈을 갖는다
- **상속한 것은 이름으로 가리킨다.** 값을 옮겨 적으면 두 문서가 갈라진다
- 금지 목록은 시계 모드의 것을 상속한다 — `DESIGN.md`의 Do's and Don'ts 절을 따른다

### Don't

- **경고를 만들지 않는다.** 겹침은 정상이므로 겹침 경고는 매일 뜨고, 매일 뜨는 경고는 경고이기를 멈춘다. 부하는 표시만 한다
- **anti-reference를 머무름에까지 적용하지 않는다.** "일처럼 느껴지면 실패"는 지나치는 몇 초에 대한 규칙이다. 머무름에서 밀도가 올라가는 것은 실패가 아니며, 이 경계를 지우면 업무용 화면이 자기 일을 못 한다
- **지나침에 진입점을 늘리지 않는다.** 새 버튼은 머무름 안에서만 는다
- **컴포넌트 값을 이 문서에서 정하지 않는다.** 이 마일스톤이 쓰는 것은 판정 기준까지이고, 실제 토큰은 코드가 생긴 뒤 scan 모드가 채운다
