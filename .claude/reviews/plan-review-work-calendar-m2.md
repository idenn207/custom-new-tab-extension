# Plan Review Panel — work-calendar-m2

**Plan**: `.claude/plans/work-calendar-m2.plan.md` · **Plan version**: `sha256:d8b7b45ac1eb2092e949b46899a7c88f31930a650bf52dbc96708f23823a9880`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 6 blocking finding(s): invariant/CRITICAL, invariant/CRITICAL, invariant/CRITICAL, invariant/HIGH — MCCP_REVIEW_SINGLE_PASS=deferred_to_prd_completion 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| invariant | CRITICAL | Plan approval cannot verify that promised functions exist, yet Validation treats function existence as an approval-time gate | Plan lines 26-34 state the 12 required newtab.js functions and 5 test functions 'do not exist' and acknowledges 'Validation items 2·2b·3·4 must fail at approval time.' Yet Acceptance line 1434 lists function existence as `[기계]` (machine-verified), implying mechanical enforcement. The Validation section splits into approval-time and implementation-time blocks, but the function existence checks are documented in the approval-time block (Validation 2, lines 1137-1150) despite being impossible to pass until Task 0-8 are complete. |
| invariant | CRITICAL | 35 of 42 Acceptance items are human-verified with no enforcement mechanism, yet the plan structure implies they are gates | Line 1467-1468 states: '`[사람]` 항목은 마흔셋 중 서른다섯이다. 그 서른다섯은 체크한다고 해서 참이 되지 않는다 — 러너도 CI도 커밋 훅도 없으므로' (35 out of 42 items are human-verified. Those 35 don't become true by checking - there's no runner, CI, or commit hook). Line 1345 explicitly states: '게이트가 커밋을 막지 못한다' (The gate cannot prevent commits). This creates a gate that appears closed in the Acceptance section but has no actual enforcement mechanism. |
| invariant | CRITICAL | Baseline anchoring fails: baseline.json created after plan approval cannot be constrained by plan sha256, and the baseline integrity check relies on an editable JSON file | The plan sha256 is d8b7b45ac1eb2092e949b46899a7c88f31930a650bf52dbc96708f23823a9880 provided in the prompt. Task 0 (lines 562-611) creates baseline.sha256 and baseline.json, but Task 0 occurs AFTER plan approval. Line 1320-1326 shows the validation check: `grep -qE '"assertFailures"[[:space:]]*:[[:space:]]*0' work-calendar-m2.baseline.json` — this verifies a JSON file downloaded from a browser button, which is neither deterministic nor immutable. Line 1339-1340 acknowledges: '앵커가 플랜 해시에 묶이지 않는다' (anchor is not bound to plan hash). The plan cannot verify the baseline was created cleanly at approval time. |
| invariant | HIGH | Rollback safety for migration chain is not mechanically verified: the v2→v3→v4 chain test relies on a comment marker, not code structure | Line 1269-1274 describes checking for the v2→v3→v4 chain via a text marker `V2V3V4-CHAIN`: 'C=$(grep -c "V2V3V4-CHAIN" test/positioning.smoke.js); [ "$C" -ge 1 ]'. The plan acknowledges this is only a 'hlower bound check' (하한 검사) that detects 'the case wasn't written at all' (아예 안 썼다 하나뿐이다). If someone adds the marker to an incorrect or empty test, the gate passes. Line 1276: '표식이 값을 증명하지 않는다' (markers don't prove values). This is the only verification that DD10's critical migration guard separation is actually tested. |
| invariant | MEDIUM | Function body extraction validation assumes fixed sed/grep formatting but doesn't verify the sed patterns work correctly | The validation script (lines 1194-1200) uses `sed` to extract function bodies with assumes column 0 declarations, column 0 closing braces, and 2-space indents for methods. The prelight check (line 1105) verifies tools exist but doesn't test whether sed formatting assumptions hold. The comment at lines 1085-1090 admits the script previously had three parsing failures from whitespace handling, yet uses the same sed technique without validating pattern correctness on the target files. If file formatting changes, sed silently produces empty output, causing grep to fail, but this only happens if the extraction fails (fail-closed). However, the assumption itself is not validated. |
| invariant | MEDIUM | SETTINGS_VERSION constant separation (DD10) relies on grep pattern that could match in wrong contexts | Line 1239-1240 checks: `grep -qE '^const SETTINGS_VERSION_V3[[:space:]]*=' newtab.js` (existence) and `body migrateCalendarToV3 newtab.js \| grep -q 'SETTINGS_VERSION_V3'` (usage). If a comment, string literal, or other context contains 'SETTINGS_VERSION_V3', the grep passes even if the guard isn't actually using it. The Risks section (line 1352) marks this as 'High/High' — v3 migration will never run if the guard isn't updated. The mechanical validation would miss a case like `// TODO: use SETTINGS_VERSION_V3` or `let oldGuardValue = 'SETTINGS_VERSION'`. |
| invariant | HIGH | Plan's own statement that 'harness passing ≠ actual path operation working' undermines the assertion model | Line 1465: '**하네스 통과가 경로 작동과 같지 않다**' (Harness passing is not the same as path operation working). Lines 1338-1340 explain that the snapshot() baseline test 'doesn't see meaning' (기하 diff only, no semantic checks). The equivalence case at line 1430 explicitly tests only `getEventDueState()` meaning, not banner rendering. The Design Critique section (line 1403-1406) states: 'CONVERGED가 말하는 것은 플랜 본문이 앵커를 어기지 않는다까지이지 구현이 그것을 지킨다는 뜻이 아니다' (CONVERGED means plan text doesn't violate anchors, not that implementation follows them). This creates a situation where assertions can pass while actual browser behavior diverges. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | Examined the plan's core structural claims: 1. **Abstraction boundaries**: The plan claims occupancy will remain unchanged because `rebuildIndex()` and `getEventsForDate()` are untouched. Verified this is sound - the occupancy assertion tests whether derived event dates (min/max of gates) equal the original dates, catching any derivation errors. Not a tautology. 2. **Two-plan split consistency**: Verified that Open Question 1 in the PRD is actually marked [x] as resolved per plan's claim (2026-08-23). The PRD M2 row shows "in-progress" as plan correctly states (not "complete" yet). The split between M2 (data) and M2b (rendering) is clearly bounded at occupancy/rendering logic that remains untouched. 3. **Snapshot() limitations**: Confirmed snapshot() definition at line 191 of test/positioning.smoke.js captures only geometry (rect, display, classes, inlineWidth) and intersection - NOT semantic content like due dates, text, or colors. This validates plan's claim that snapshot() diff cannot catch DD3 semantic changes, requiring separate domain-projection assertions instead. 4. **Three-axis pattern precedent**: Found existing `runCalendarV3MigrationCases()` at line 497 of test/positioning.smoke.js validates three axes (idempotence, preservation, legacy field survival). Plan correctly identifies this as extendable pattern; DD11's no-second-validator claim holds. 5. **Gate derivation call sites**: Plan identifies three required call sites for `deriveEventRange()`: createCalendarEvent, CRUD operations, and promoteEventsToV4. Validation script (lines 1196-1197) checks for these with location-specific `site()` function checking function body, not just existence. This addresses R10 architect findings. 6. **Hidden assumption check**: Plan assumes `createCalendarEvent()` is single source of truth for all event creation. While not machine-enforced, this is a design pattern statement appropriate to plan level, backed by test validation of the gate entry points. 7. **File reference verification**: `.claude/plans/work-calendar-m2b.plan.md` exists at expected path. Companion plan cross-references are valid. 8. **Honest scope management**: Plan explicitly acknowledges it ADDS new UI surfaces (gate editor, project picker in modal) while claiming existing "grid" doesn't change. This is precise and honest - not hiding new surfaces under a false "screen unchanged" claim. New surfaces marked [사람] (human-reviewed) in acceptance, outside mechanical test scope. |
| security | pass | Attacked trust boundaries, data validation, and reference handling: (1) Reviewed data flow through import/export paths and validated that envelope format prevents self-rejection. (2) Traced reconcileProjectRefs security mechanism with required projectsLoaded parameter - verified it prevents silent data loss when projects fail to load. (3) Checked atomic write guarantees for projects+events commit via DD22 pattern. (4) Verified error notification path requires second applyStorageNotice() call after manager initialization (DD37, R12 security CRITICAL finding). (5) Examined sanitizeImportedEvents batch-rejection logic against partial import attacks. (6) Validated acceptance criteria comprehensively check all security mechanisms including missing argument detection, failed-load scenarios, and atomic write verification. (7) Searched code for path leaks, credential exposure, or privilege escalation vectors. Found none. Plan addresses all prior CRITICAL/HIGH security findings from R0-R12 review rounds with specific design decisions (DD1-DD37). No bypass paths or residual vulnerabilities identified. |
| test | pass | I attacked the plan's core testing claims: (1) DD3-FIXTURE width requirement - The plan mandates 4 fixed inputs with at least one marked DD3-FIXTURE-WIDTH to prevent all width-less tests; Validation line 1290-1291 checks this with `grep -c "DD3-FIXTURE-WIDTH"`. This is properly separated at line 1284 from the 4-count check. (2) V2V3V4-CHAIN marker for migration testing - Validation line 1273 checks this; the plan correctly addresses the DD10 trap where v3 migration could be skipped. (3) Reference implementation in Task 5 - The plan requires embedding v3 behavior code (lines 972-975) with explicit 5-line limit (lines 981-982). While the Validation bash doesn't machine-check the implementation's presence or length, the plan transparently acknowledges at DD24 (lines 1046-1061) that correctness verification happens at the manual assertion level (Acceptance line 1430), not the shell level. (4) Function existence checks - Validation 2 (lines 1137-1150) checks all 12+ required functions exist via grep regex. (5) Call site checks - Validation 2b (lines 1196-1245) verifies specific functions call deriveEventRange(), normalizeGates(), and reconcileProjectRefs() at correct parameter passes. (6) Validation split into approval-time vs implementation-time - Lines 1063-1077 properly separate checks that run now (syntax, function existence) from those needing implementation (harness tests). I found no untested load-bearing claims. The plan extensively documents its testing limitations (DD24) and honestly represents what machines can and cannot verify. Tests are falsifiable at appropriate levels: shell checks catch existence gaps, human acceptance tests catch behavioral failures. |
| invariant | fail | I attacked: (1) fail-open drift by checking whether approval-time validations can actually run and whether they enforce anything; (2) receipt anchoring by verifying when baseline files are created relative to plan approval; (3) rollback safety by examining whether migration chains are mechanically verified vs text-marker-based; (4) skip predicates by checking if validation blocks can be conditionally skipped; (5) gate mechanics by verifying whether the Acceptance section's structure actually prevents merging without human verification. I traced the execution flow from plan approval through Task 0-8, examining the timing of baseline creation, the nature of validation checks at approval vs implementation time, and the enforcement mechanisms. I searched for conditions where validations pass without corresponding implementation (empty stubs, grep patterns matching in wrong contexts, sed formatting assumptions). I examined the relationship between test assertions and actual browser behavior, and verified whether the plan acknowledges its own limits clearly enough that readers won't mistake best-effort documentation for enforceable gates. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 377673,
  "halt_stage": null,
  "backlog_appended": 6,
  "backlog_skipped_nonblocking": 2,
  "granted": 4,
  "reviewed_plan_hash": "sha256:d8b7b45ac1eb2092e949b46899a7c88f31930a650bf52dbc96708f23823a9880",
  "plan_path": ".claude/plans/work-calendar-m2.plan.md",
  "recorded_at": "2026-08-23T10:23:16.887Z"
}
```
