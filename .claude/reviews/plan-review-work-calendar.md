# Plan Review Panel — work-calendar

**Plan**: `.claude/plans/work-calendar-m2-5.plan.md` · **Plan version**: `sha256:20fd253ffc9e9c02eb582ef015f2f50b459032ae9ca6332c7b1b0d3381955027`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 5 blocking finding(s): architect/HIGH, architect/FAIL, invariant/CRITICAL, invariant/HIGH

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | Adapter function access pattern broken in existing harness code. Plan moves selectStorageBackend to storage-adapter.js and exposes via window.__newTabStorage object (DD7, Task 1:290), but doesn't update existing access at test/positioning.smoke.js:1558 which does frameWindow['selectStorageBackend']. After refactor, this returns undefined since const lexical bindings aren't window properties. | test/positioning.smoke.js:1558 accesses `const select = frameWindow['selectStorageBackend']`. Plan Task 1 Action says 'window.__newTabStorage 객체로 노출한다' - functions exposed only via object, not as direct window properties. Plan Task 2 Action requires fixing line 1588 which uses select(), but doesn't show updating line 1558 access. DD7 says cross-window access needs 'window.__newTabApp = app' pattern, not lexical scope. |
| architect | MEDIUM | Plan creates new runAdapterIdentityCases test (Task 2) that needs access to PREVIEW_STORAGE_KEY and STORAGE_BACKEND from frameWindow, but doesn't specify access pattern. These are only exposed via window.__newTabStorage after refactor, but plan doesn't show the accessor code. | Plan Task 2 Action (line 351-352): 'runAdapterIdentityCases 로 고정한다... 하네스 창과 iframe 창의 어댑터가 같은 `PREVIEW_STORAGE_KEY` 와 같은 `STORAGE_BACKEND`' - test needs these values but plan provides no code showing how. DD7 establishes `window.__newTabStorage` pattern but Task 2 doesn't apply it. |
| architect | MEDIUM | Plan doesn't explicitly specify substitution pattern for 41 chrome.storage.local replacements. Says 'Test 2 Action: 어댑터 핸들을 두고 chrome.storage.local 등장 41회를 전부 그 핸들로 치환' but doesn't show what the handle is or how it's accessed - critical for implementation clarity. | Plan Task 2 Action (line 343-345) says to substitute 41 occurrences with an adapter 'handle' but doesn't specify the pattern. Line 346 says 'Validate가 남은 등장이 하나라도 있으면 실패한다' - validates completeness of substitution but not clarity of substitution target. |
| invariant | CRITICAL | Task 3, 4, and 6 Validate commands hardcode non-existent machine-specific path to MCCP CLI, causing gate failure on any execution | Plan lines 416, 441, 510 all embed: `node C:/Users/Administrator/.claude/plugins/cache/mccp/mccp/1.32.6/scripts/receipt/cli.js hash-plan ...`. Glob search confirms file does not exist at this path. Path contains hardcoded username, OS-specific drive letter, and version number, making it non-portable. |
| invariant | HIGH | task2.pass marker gating is vulnerable to silent skip-all when file doesn't exist - subsequent tasks check content but can't run their Validate without the marker file, creating circular dependency that fails closed instead of detecting missing work | Plan lines 416, 441, 510 all require: `grep -q '^assert_failures=0$' .claude/plans/work-calendar-m2-5.task2.pass`. If task2.pass doesn't exist, these grep commands fail immediately, blocking Task 3-6 entirely. This is HALT (correct) not degrade, but the cascading failure makes it impossible to distinguish between 'Task 2 was skipped' and 'validation infrastructure is broken'. |
| invariant | MEDIUM | Validation section at approval time (section 1) is fully portable and correctly gates Task 0 premises, but post-implementation Validation sections 2-3 lack equivalent portability checks for the task2.pass marker creation itself | Plan section 1 (lines 569-624) uses portable grep and curl patterns. Section 2 (lines 627-662) checks for absence of pre-moved code. But neither section validates that task2.pass will be created correctly - Task 0-d is supposed to create evidence as a prerequisite, yet Validation section 1 does not check for the existence of `.claude/plans/work-calendar-m2-5.moved-range.txt` which Task 1 requires (DD6, line 267-269). |
| invariant | MEDIUM | Plan describes .gitattributes LF enforcement as preventing CRLF damage to existing M2 anchors (DD12, line 187-191), but M2 anchors already exist with CRLF line endings and remain broken; the fix is only prospective for M2.5 anchors | File `.claude/plans/work-calendar-m2.rebaseline.sha256` contains CR at end of line 2 (confirmed by Read). Plan states existing M2 anchor fails with `shasum -c` exit 1 due to CRLF (line 182). Task 0-a adds .gitattributes rule, but this doesn't retroactively fix M2 anchors or prevent checkout from re-introducing CRLF on the M2-5 anchors if the rule is added after Task 0-a checkout. |
| invariant | MEDIUM | Approval Validation 1-e claims to test the premise 'harness dies on http origin' using code evidence (line 598), but the actual code check (line 605) only verifies non-presence of adapter handle in test file, not that chrome.storage will actually be undefined at runtime | Validation 1-e says `grep -q '__newTabStorage' test/positioning.smoke.js && { exit 1; }` — confirms adapter handle is NOT in test file. But this doesn't prove chrome.storage is undefined on localhost; it only proves the harness hasn't been modified yet. The premise requires runtime proof which Task 0-d is supposed to create, but Task 0 actions are 'procedural rules' without machine enforcement (line 276-277). |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Verified adapter citations (newtab.js:157 selectStorageBackend comment, 1035 dataset.storageBackend, 3612 atomic set pattern, 225 STORAGE_BACKEND assignment, 1220 version 4 whitelist) - all citations accurate. Confirmed five adapter items exist: PREVIEW_STORAGE_KEY, selectStorageBackend, createStorageAdapter, STORAGE_BACKEND, storage. Verified SETTINGS_VERSION exists at line 18. Checked that backup functions (buildBackupEnvelope, parseBackupEnvelope) don't exist yet so plan creates them from scratch. Examined test/positioning.smoke.js harness structure: loadApp creates iframe, installs error hooks, and existing code at line 1558 attempts frameWindow['selectStorageBackend'] access. Verified this function is currently a const (not window property) in newtab.js. Analyzed DD7's lexical scoping claims - tested by plan authors themselves. Checked cross-window access pattern: plan cites window.__newTabApp but plans to use window.__newTabStorage object pattern. Identified that Task 2 must fix line 1588 (hardcoded 'extension' assertion) but doesn't address line 1558 which provides the select function that line 1588 uses. Plan validation checks don't include runtime access verification - only syntax (node --check) and grep pattern matching. |
| security | pass | **Trust boundaries and data handling refutation:** Attacked the backup envelope deserialization (DD11): The plan specifies whitelist parsing with rejection of __proto__, constructor, prototype at the envelope level. This prevents basic prototype pollution at the top level. While the plan doesn't explicitly address recursive checks for nested objects, the directive to mirror `sanitizeImportedEvents()` pattern implicitly covers this via the defensive field extraction pattern documented in newtab.js:361-363. JSON.parse() itself cannot set prototypes; the risk only emerges if spread/assign are used on untrusted data — the codebase already forbids this pattern. Attacked the deny-list vs whitelist choice (DD2): The plan uses deny-list for backup creation (backup all keys except excluded ones) because whitelist would silently drop future keys. This is correct — a forward-compatible approach necessary for reliable backups. Attacked whether unknown keys in restored data could cause issues: The restore simply writes data back to storage, and code reads defensively (verified against existing patterns). Unknown keys are ignored. Attacked version compatibility (DD13): The three-way gate (same/lower/higher version) is sound. Lower versions are allowed because startup migrations are idempotent and run after reload. Higher versions are rejected to prevent old code reading unknown schemas. Tested this against the existing SETTINGS_VERSION_V2/V3 pattern in the codebase — the guards are independent constants preventing the "version bump swallows migration" trap. Attacked undo slot recursion (DD2, DD5): The plan requires `BACKUP_UNDO_SLOT_KEY` to be in `BACKUP_EXCLUDED_KEYS` to prevent recursive slot nesting. Task 6 case 6 tests this. Slot is shared storage space with backup data — if storage is evicted, slot is gone too. This is explicitly documented as a non-backup, which correctly manages user expectations. Attacked atomicity of restore (DD3, DD4): The restore does one `storage.set()` call containing both restored data and undo slot. Either both write or neither writes. The chrome.storage.local adapter (pass-through to native API) and localStorage adapter (single setItem on JSON string) are both atomic. Page reload ensures memory sync with storage. Attacked the sourceLoadFailed flag: Backup envelope includes this flag to indicate if storage read failed during backup creation. Plan correctly allows restoring corrupted data (DD9 — "손상까지 그대로 복사하며, 그것이 백업이 해야 하는 일이다"). This trades off validation strictness for backup reliability in broken states. Attacked shell command injection in Task 0-7: File paths are literal strings, not user input. The `$HOME` variable is shell-builtin and safe. Validation uses grep/awk/shasum on literal paths with no interpolation risk. Attacked batch rejection (DD11): Like `sanitizeImportedEvents()`, the plan specifies batch rejection — if any item fails validation, entire envelope is rejected. This prevents silent data loss or partial restore scenarios. Pattern is consistent with existing codebase. Attacked excludedKeys transparency (DD1): The backup envelope includes `excludedKeys` array, making exclusions transparent to the reader. Cannot be used to forge inclusions because data integrity is validated — if the data contains a key not listed in excludedKeys, the key is restored anyway (deny-list philosophy applied consistently). Could not locate or construct a concrete path from an adversarial input to a security consequence in the backup/restore flow. The denial-of-service vector via oversized backups is acknowledged in Task 6 but is covered by total size limit validation mentioned in DD11 (specification gap on the numeric value, but not a plan defect — implemented as pattern-mirror of existing code). No partial-state leakage found; all writes are atomic. No capability escalation or tamper surface discovered outside the designed trust boundary (single user, local storage only). |
| test | pass | Attacked the test specification across six dimensions: (1) Falsifiability of major design decisions — verified that DD1 (envelope format), DD2 (deny-list), DD3 (atomic restore), DD4 (slot co-commit), DD6 (adapter move), DD9 (no canExport guard), and DD13 (version gating) each have corresponding test cases with either marker checks or assertions specified. (2) Test case completeness — confirmed all 13 cases are named in Task 6 (lines 461-497), though cases 1-8 lack static grep markers (acknowledged limitation at line 501-508, mitigated by Task 7-a browser execution gate requiring 0 assertion failures). (3) Test soundness — verified Case 12's version upgrade claim is logically sound: restore envelope < current version → restore as-is → reload triggers migrations in Application constructor (lines 5721-5726) → version upgrades. Implementation detail (await reload) is normal developer guidance, not a defect. (4) Validation gates — traced all 4-stage validation pipeline: approval time checks (Validation 1), post-implementation checks (Validation 2), anchor verification (Validation 3), browser execution (Validation 4, Task 7-a). Each stage targets different verification class (static code presence, functional requirements, artifact integrity). (5) Task sequencing — confirmed Task 2 marker file gate (`task2.pass`) is correctly required by Tasks 3-6 and includes plan hash check (line 829) preventing stale markers after plan changes. (6) Existing test harness compatibility — verified `loadApp()` exists (line 154), `runAll()` exists (line 2340), `waitFor()` pattern exists for async waits (line 77-97), providing all primitives needed for implementation. Found no falsifiable claims without test coverage. All acknowledged limitations (manual steps, weak static checks, no CI runners) are explicitly documented and mitigated by browser execution gate. |
| invariant | fail | I attacked the following invariant erosion vectors: (1) **Receipt anchoring and digest coverage** - verified M2.5 anchor structure, found hardcoded MCCP CLI path in Validate commands that breaks portability; (2) **Fail-closed gate machinery** - traced Task 2→Task 3 dependency through task2.pass marker, found gate fails due to missing tool file rather than validation logic; (3) **Existing defects** - confirmed M2 anchors are actually broken (CRLF line endings), traced DD12 remediation to find it's prospective-only; (4) **Accounting and baseline states** - verified storage-adapter.js doesn't exist (correct), chrome.storage.local appears 41 times (matches plan), but found no moved-range.txt file that Task 0-f should create; (5) **Skip predicates and unverified premises** - found Validation 1-e provides only code evidence for "harness dies on http" but acknowledges runtime evidence is separate, making premise unanchored. Did not find evidence that Task 0 moved-range.txt is checked, or that the hardcoded MCCP path question was addressed in any review round beyond acknowledgment of portability limitation. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 376836,
  "halt_stage": "5.2e",
  "backlog_appended": null,
  "backlog_skipped_nonblocking": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:20fd253ffc9e9c02eb582ef015f2f50b459032ae9ca6332c7b1b0d3381955027",
  "plan_path": ".claude/plans/work-calendar-m2-5.plan.md",
  "recorded_at": "2026-08-26T09:47:15.217Z"
}
```
