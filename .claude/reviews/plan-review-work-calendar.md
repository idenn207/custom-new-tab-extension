# Plan Review Panel — work-calendar

**Plan**: `.claude/plans/work-calendar-m2.plan.md` · **Plan version**: `sha256:71a2448a2eb310f3ca7c6c1a1767e27cf9c6d96f288daa3a7f8781b0e462ccfe`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 14 blocking finding(s): architect/CRITICAL, architect/CRITICAL, architect/CRITICAL, architect/CRITICAL — MCCP_REVIEW_SINGLE_PASS=deferred_to_prd_completion 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | CRITICAL | Core data integrity functions required by the plan's design decisions do not exist in the codebase. | Plan Task 2 Action (line 635-636) specifies creating `promoteEventsToV3(events)` and `promoteEventsToV4(events)` as pure functions. Plan Task 3 Action item 5 (line 733) specifies creating `reconcileProjectRefs(events, projects, { projectsLoaded })`. Plan Task 4 (line 775-778) specifies that CRUD functions `addGate`, `updateGate`, `removeGate` must call `deriveEventRange(event)` before commit. Grep search of newtab.js confirms NONE of these functions exist: promoteEventsToV3, promoteEventsToV4, deriveEventRange, reconcileProjectRefs, normalizeGates, createCalendarGate - all return zero matches. These functions are referenced as requirements in DD19 (line 163-169), DD26 (line 242-276), DD28 (line 707-710), DD36 (line 789-794) but do not exist in code. |
| architect | CRITICAL | The plan's own validation gate will fail because it explicitly checks for non-existent functions. | Validation section 2 (lines 974-981) includes shell script: `for fn in promoteEventsToV3 promoteEventsToV4 deriveEventRange createCalendarGate createCalendarProject loadProjects persistProjects reconcileProjectRefs addGate updateGate removeGate; do grep -qE "$(decl $fn)" newtab.js \|\| { echo "MISSING: $fn"; exit 1; }`. This validation check will fail for 6 of these 11 functions (promoteEventsToV3, promoteEventsToV4, deriveEventRange, reconcileProjectRefs, normalizeGates, createCalendarGate). |
| architect | CRITICAL | Missing boundary control for referential integrity. Plan claims `reconcileProjectRefs` is the single source of truth for protecting against orphaned project references. | Plan Task 3 (line 733-741) claims: '`reconcileProjectRefs(events, projects, { projectsLoaded })` → 새 배열' is required. Plan line 707-710 explicitly states: 'If this item is missing, DD28 will delete data... replaceEvents() calls reconcileProjectRefs(nextEvents, this.projects, ...) and if this.projects is empty the imported events' projectId gets demoted to null even though the project exists in storage.' Plan line 718 shows it must be called by persistProjects(). Plan line 762 shows it must be called by replaceEvents(). This function does not exist, creating a data integrity boundary leak. |
| architect | CRITICAL | Missing single point of control for maintaining derived field consistency. Plan requires `deriveEventRange()` to be called from five specific call sites after any gate modification. | Plan DD26 (lines 242-276) states: 'The invariant must be this: functions that create or modify gates call deriveEventRange() before returning.' Specifically requires calls from: (1) createCalendarEvent() last line (Task 1), (2) addGate/updateGate/removeGate before commit (Task 4), (3) promoteEventsToV4() before returning (Task 2). Plan line 646 warns: 'This sentence was marked as CRITICAL twice (test F1 · invariant F4) — DD26 warned that without the third call, startDate ≠ min(planned) events will be committed, but Task Action doesn't list that call.' Validation block 2b (lines 1028) requires: `for f in createCalendarEvent promoteEventsToV4 addGate updateGate removeGate; do site "$f" "deriveEventRange("; done`. Function does not exist. |
| architect | HIGH | Plan makes a false claim about Task 3 creating all required functions when the list includes functions that are proposed to be created elsewhere. | Plan Task 3 (line 670): '이 Task가 새로 만드는 것 — 아래가 전부다' lists 6 items, but item 5 is reconcileProjectRefs which Task 3 claims to create, yet there is no corresponding function definition in the action for normalizeGates (required by line 789) or createCalendarGate (required by DD20, line 171). Task 1 is supposed to create createCalendarGate (line 609 references newtab.js:304 as the mirror for createCalendarProject, implying createCalendarGate should follow the same pattern). None of these exist. |
| architect | MEDIUM | Plan cites specific file locations and existing code patterns as mirrors for new functions, but those mirrors may not exist or may not be analogous to what the plan claims. | Plan Task 1 (line 609) cites 'Mirror: newtab.js:304 createCalendarEvent()' for createCalendarGate. Plan Task 3 (line 672) cites 'Mirror: newtab.js:2062 loadEvents() with same discipline.' Plan DD4 (line 106) cites 'newtab.js:327-330 DD6 comment' for legacy fields pattern. While I confirmed getEventDueState exists at line 2627, I have not verified all cited locations precisely match their description, creating doubt about whether the cited patterns are truly analogous. |
| security | HIGH | Plan specifies critical parameter-passing requirement for `applyStorageNotice()` that is NOT validated by shell script | DD37 (plan lines 446-452) explicitly warns: 'If you only pass `projectsLoadFailed`, the "설정 마이그레이션 실패" message from the first call gets SILENTLY ERASED... this function rebuilds messages from scratch each time and replaces them in their entirety'. The required solution is to call `applyStorageNotice()` TWICE: first at line 4120 with `{migrationFailed}`, second after initialize with `{migrationFailed: !v2Ok \|\| !v3Ok, projectsLoadFailed: ...}`. However, the validation script (lines 970-1097) contains NO checks for: (1) that applyStorageNotice is called exactly twice, (2) that the second call includes BOTH parameters, or (3) that migrationFailed is not omitted. Line 732 warns the exact failure mode, yet the shell validation never inspects applyStorageNotice call sites or parameter contents. |
| security | MEDIUM | Validation script checks for parameter mention but not parameter validation for `reconcileProjectRefs` | Plan lines 736-741 (reconcileProjectRefs contract) state: 'The third argument is required and throws if missing... if projectsLoaded is not boolean, immediately throw'. Lines 1031-1032 validate this with: `body "$f" newtab.js \| grep -q "projectsLoaded" \|\| { echo "$f: reconcileProjectRefs called without projectsLoaded parameter (DD28)"; exit 1; }`. This grep only checks that the string 'projectsLoaded' appears somewhere in the function—it does NOT validate: (1) that projectsLoaded is a boolean, (2) that non-boolean values throw, or (3) that the parameter is actually passed to reconcileProjectRefs. A function could contain the comment '# projectsLoaded' and pass. |
| security | MEDIUM | Plan prose describes trust boundary (load-write coupling) via `projectsLoadFailed` flag but shell validation does not verify order of operations | DD37 lines 438-440 require `loadProjects()` to be called 'right after await this.loadEvents()' in CalendarManager.initialize(). Plan line 699 says 'right after this.loadEvents()'. Lines 686-687 describe the risk: if load fails, `this.projectsLoadFailed = true` must block writes via persistProjects. The validation script (lines 970-1097) checks that functions exist and are called, but does NOT verify: (1) the order of loadEvents and loadProjects calls within CalendarManager.initialize(), (2) that persistProjects checks projectsLoadFailed before writing, or (3) that the flag is set atomically before any write could occur. A developer could move the loadProjects call before loadEvents without detection. |
| test | HIGH | DD3 (due state comes only from terminal gate) can be properly tested with the fixture design | Plan lines 859, 1069-1072: Plan acknowledges 'v3 data is all single-date, so without width items DD3 could be implemented backwards and still pass.' Line 1072 Validation only checks `grep -c 'DD3-FIXTURE' >= 4`, not fixture content. The critical fixture #1 specified as `today-3` to `today` (with width, line 854) is mandated in Action, but Validation only counts markers. Someone could implement all 4 fixtures as no-width items (e.g., all `today` to `today`) and pass the marker count check while DD3 would remain untestable. The plan itself identifies this at line 1070 as 'F1 identified as a failure' |
| test | MEDIUM | Legacy fields are preserved so rollback to v3 code will work (DD4 implies backward compatibility) | Plan lines 105-107: DD4 claims legacy fields stay and 'v3 code로 롤백해도 렌더가 산다' (render still works with v3 code). But immediately states 'v3 코드가 교체되므로 이 저장소에서 돌릴 수단이 없다' (v3 code is replaced so no way to run it in this repo). Concludes 'DD4가 보증하는 것은 **v3가 의존하는 입력이 온전하다**까지' (DD4 guarantees only that inputs v3 depends on exist intact). So the claim 'rollback works' is not actually falsifiable - only tested that fields exist, not that old code can run. |
| test | MEDIUM | User sees the same screen before and after migration (core claim at line 14) | Plan line 888: 'snapshot() diff 0' is tested manually (Acceptance 1196-1197 marked as `[사람]` - human checks). Lines 1196-1197 state 'diff 0이어야 한다' (must be 0) but these are manual/human-performed assertions with no automation. Plan acknowledges at line 1234-1249 that out of 40 Acceptance items, 33 are human checks with 'no machine-enforced gate preventing accidental rendering changes'. Plan explicitly states the validation approach at line 938: only `node --check` runs at approval time, actual rendering equivalence is human-verified after implementation. |
| test | MEDIUM | The plan creates 4 specific fixture values as shown in the table (Action section line 850-857) | Plan line 850 references 'R7 test F1·F4' and states '값이 Validate 에만 있으면 Action 은 자기 완결이 아니다' (if values only in Validate not Action, then Action is not self-contained). However, Validation 2b (line 1071) has no check for specific fixture values - only `grep -c 'DD3-FIXTURE' >= 4`. The table with specific values (today-3 to today, today-5 to today-1, etc.) is in Action but not validated. No test of `grep` or pattern match ensures fixture #1 has width vs no-width. |
| invariant | CRITICAL | The baseline file hash validation (Validation 3, line 1097) can pass even when the re-export step is omitted. Lines 904-906 explicitly state: 'If re-export is skipped, the anchor binds nothing.' The hash check compares against a committed file that may not represent current test state if re-export was skipped. | Line 904-906: 재내보내기가 빠지면 앵커가 아무것도 묶지 못한다...저장소 루트의 work-calendar-m2.baseline.json은 Task 0이 한 번 내려받아 둔 파일일 뿐 그것과 연결돼 있지 않다. Also line 1097: shasum -c check can pass if test files don't change, regardless of whether re-export occurred. |
| invariant | HIGH | M2 milestone is split across two sub-plans (work-calendar-m2.plan.md and work-calendar-m2b.plan.md) with explicit requirement that both must complete for M2 to be marked complete, but no gate prevents premature completion. The PRD states 'This row becomes complete only when BOTH plans are done' (line 103), yet the only enforcement is a human-checked Acceptance item (line 1230). | Line 103: 이 행은 두 플랜이 모두 끝나야 complete가 된다. PRD row 2 shows Status: in-progress with Plan pointing to both sub-plans. Line 1230: [ ] `[사람]` PRD Open Question 1 해소 표기...Status는 아직 in-progress - this is a [사람] (human, unforced) item only. |
| invariant | HIGH | Validation 3's critical baseline hash gate (line 1097) depends on manual browser operations in Task 0 that cannot be mechanically verified. The plan explicitly admits (line 1115): '게이트가 커밋을 막지 못한다. CI도 커밋 훅도 없으므로' (gates cannot block commits; there is no CI or hooks). Task 0 baseline export requires clicking a browser button with no automated proof of execution. | Line 532: Task 0 requires manual browser procedure to click 베이스라인 내보내기 button and manually save JSON file. Line 1115: 게이트가 커밋을 막지 못한다. Line 928-935 shows only syntax check runs at approval time; Validation 3 runs after implementation with no enforcement mechanism. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Searched for existence of 11 core functions the plan claims Tasks 1-4 will create: promoteEventsToV3, promoteEventsToV4, deriveEventRange, createCalendarGate, createCalendarProject, loadProjects, persistProjects, reconcileProjectRefs, addGate, updateGate, removeGate. Six of these return zero matches in newtab.js (promoteEventsToV3, promoteEventsToV4, deriveEventRange, reconcileProjectRefs, normalizeGates, createCalendarGate). Verified the plan's Validation section 2 (lines 974-981) contains a shell script that explicitly checks for existence of these functions and will fail if they're missing. Confirmed plan DD26 requires deriveEventRange to be called from specific call sites (createCalendarEvent, promoteEventsToV4, addGate, updateGate, removeGate) - function does not exist. Confirmed plan DD28 requires reconcileProjectRefs as single point of control for referential integrity in persistProjects and replaceEvents - function does not exist. These are load-bearing architectural claims: the plan's entire approach to data integrity, derived field consistency, and migration depends on these functions as single points of control. |
| security | fail | I examined: (1) the existing newtab.js code structure to understand current trust boundaries and data handling patterns; (2) createCalendarEvent() whitelist pattern as model; (3) the plan's specific design decisions (DD20, DD27a-27c, DD28, DD37) that describe new security-critical functions and their calling contracts; (4) the existing loadEvents() and persistEvents() patterns to verify the plan mirrors them correctly; (5) the validation section line-by-line to identify what checks exist and what is missing; (6) whether the shell script validates prose-only specifications around applyStorageNotice parameter passing, reconcileProjectRefs boolean validation, and loadProjects call ordering. I attempted to trace a concrete hostile path: (a) developer implements second applyStorageNotice call with only projectsLoadFailed parameter → plan explicitly warns this erases migration failure message → validation script does not catch this; (b) developer calls reconcileProjectRefs without projectsLoaded or with wrong type → plan says this must throw but validation only checks string presence; (c) developer calls loadProjects before loadEvents → plan requires specific order for flag safety but validation doesn't check order. All three paths reach consequence (silent data loss or flag misbehavior) and plan text warns about them, but validation is incomplete. |
| test | fail | Searched plan for test coverage of DD3 (due state from terminal gate): Found plan explicitly acknowledges at lines 859 & 1070 that fixture design cannot properly test DD3 if only no-width items present, yet Validation line 1072 only counts `DD3-FIXTURE` markers (>=4) without validating content. Searched for tests of DD4 backward-compatibility claim: Found plan explicitly states (line 107) v3 code no longer exists in repo so 'rollback works' cannot be tested, only that legacy fields are present. Searched for machine-enforced tests of 'screen doesn't change': Found only manual Acceptance items 1196-1197, no automated gate. Searched Validation 2b section (lines 959-1073) for content validation of fixtures: Found no pattern match or assertion checking specific fixture values (today-3, today-5, etc.) from table. Searched for any automation of fixture value checks: Found none. Checked existing test file for runCalendarV4EquivalenceCases function: Does not exist yet (expected - plan says it will be created). Verified test file has runCalendarV3MigrationCases which uses collector.add() with domain assertions, showing pattern should work." |
| invariant | fail | Attacked three core invariant areas: (1) receipt anchoring of the baseline integrity gate (Validation 3, line 1097), (2) the split milestone gate for M2 completion across work-calendar-m2b.plan.md, (3) whether manual Task 0 procedures establish verifiable receipt. Found fail-open drift in baseline hash validation, unguarded split milestone condition, and unanchored manual procedures that feed the integrity gate. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 293418,
  "halt_stage": null,
  "backlog_appended": 14,
  "backlog_skipped_nonblocking": 6,
  "granted": 4,
  "reviewed_plan_hash": "sha256:71a2448a2eb310f3ca7c6c1a1767e27cf9c6d96f288daa3a7f8781b0e462ccfe",
  "plan_path": ".claude/plans/work-calendar-m2.plan.md",
  "recorded_at": "2026-08-23T04:10:22.172Z"
}
```
