# Plan Review Panel — work-calendar-m2b

**Plan**: `.claude/plans/work-calendar-m2b.plan.md` · **Plan version**: `sha256:c0d1a75f7633e9e28fdbce5e684fed0031c322073d955ed77c493502af1f7ec2`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 4 blocking finding(s): architect/FAIL, test/HIGH, test/HIGH, test/FAIL — MCCP_REVIEW_SINGLE_PASS=deferred_to_prd_completion 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | MEDIUM | DD13 specifies the onboarding decision condition with an unqualified `!onboardingSeen` without explicitly stating which object owns it | Plan line 103 gives condition as `this.calendarManager.projects.length === 0 && !this.calendarManager.projectsLoadFailed && !onboardingSeen`. Lines 111-112 warn about exactly this problem: 'bare `this.projects` 로 적으면 구현이 `undefined.length` 에서 터지거나, 구현자가 소유자를 임의로 추측해' — but lines 111-112 only fix this for the first two conditions. The third condition `!onboardingSeen` lacks an explicit owner. DD13 prose identifies the owner as `SettingsManager` with setting key `calendarOnboardingSeen`, but the condition statement itself does not specify the access pattern: should it be `this.settingsManager.getSetting('calendarOnboardingSeen')`? The condition and the abstraction boundary are misaligned. |
| architect | MEDIUM | DD13 requires the onboarding decision to be made 'after `await this.settingsManager.initialize()`' but this placement is ambiguous and potentially impossible in the current code structure | DD13 line 107: '이 판정은 `Application.initialize()` 안에서, **`await this.settingsManager.initialize()` 뒤에** 선다.' (This decision is made inside Application.initialize(), AFTER `await this.settingsManager.initialize()`). However, examining newtab.js lines 5500-5506, `settingsManager` is created and initialized as the LAST statement in `Application.initialize()`. There is no code that executes after `await this.settingsManager.initialize()` within the current method structure. The plan does not explicitly state whether the implementer should (a) add code after this final await, (b) restructure the initialization order, or (c) split the decision into a separate method. This leaves the placement ambiguous. |
| architect | MEDIUM | The plan acknowledges the danger of unqualified ownership (lines 111-112: 'bare `this.projects` 로 적으면... 구현자가 소유자를 임의로 추측해') but then repeats the pattern with the third condition | DD13 lines 111-112 explicitly warn against putting ownership ambiguity in the condition statement: implementers will guess wrong, code will fail with `undefined` errors, or work incorrectly in silent ways. The plan fixes this for the first two conditions by writing them as `this.calendarManager.projects.length` and `this.calendarManager.projectsLoadFailed`. But the third condition `!onboardingSeen` is left unqualified. The plan's own architectural principle is violated by this inconsistency. Task 3 line 256 says to 'fix Application.initialize() — wire up the onboarding decision' but does not specify the concrete access pattern for the flag. |
| test | HIGH | Task 1 occupancy test assertions can verify DD31 (gates-based occupancy with same-date collapsing) | Plan line 217-225 specifies 5 assertions for occupancy tests, but the plan never shows HOW to compute bucketKeys, bucketSizesByDate, or chipCountsByDate from the calendar object. These are internal fields (eventsByDate at line 3322) that the test code must extract, but the extraction logic is not defined. Different implementers could compute these values differently, invalidating the test's ability to falsify DD31. For case 5 (same-day dev/review gates), if gates aren't actually read by rebuildIndex(), the test might still pass accidentally if startDate/endDate happen to produce the same bucket size. |
| test | MEDIUM | getCellDueState() change to accept dateKey parameter is falsifiable and tested | Plan line 242 requires changing getCellDueState(dayEvents, dateKey) signature to distinguish dropped gates. However, Acceptance line 410 makes this a manual '[사람]' check only ('눈으로 확인'). There is no automated test that would fail if getCellDueState() still has the old signature. The plan acknowledges at line 245 that calendar geometry diff 0 is NOT required ('달력 경로의 기하 diff 0은 이 플랜에서 요구하지 않는다'), so snapshot() won't catch signature changes. This is a critical render function change with zero automated verification. |
| test | MEDIUM | Task 2 panel metadata change (showing gate dates not ranges) has automated test coverage | Plan Acceptance line 411 makes panel metadata testing '[사람]' (manual only). The specification at line 237-239 is precise about HOW to change it (gate dates instead of range), but there is no automated test that would fail if createTodoItem() didn't read gates[] or still showed startDate..endDate ranges. An implementer could skip this change and the test suite would not catch it. |
| test | LOW | DD31-FIXTURE marker count validation at line 330-331 ensures occupancy test covers all required scenarios | Validation 2 checks `grep -c 'DD31-FIXTURE' >= 5`. The plan acknowledges at line 208 this only catches one failure mode: 'polarity 없는 항목만으로 케이스를 써 두어 DD31이 구현되지 않아도 통과하는' 실패 모양은 확실히 잡는다'. But it cannot catch: (1) All 5 markers on one fixture type (testing same scenario 5 times), (2) Markers on fixtures that don't actually test occupancy, (3) Fixtures that don't call rebuildIndex(). The check is a marker-counting regex, not a test validity check. |
| test | MEDIUM | Acceptance criteria for project name placement at line 413 verifies DOM structure | Line 413 requires project name in '.calendar-todo-meta의 첫 항목' but explicitly states '[사람]' and '기계로 판정할 수단이 없다'. No automated test verifies the DOM nesting depth or that project name is literally the first child (not hidden by CSS or displaced by siblings). This is Task 2's most specific output rule (line 243) but has zero machine verification. |
| test | HIGH | Baseline integrity check at Acceptance line 402 prevents overwriting M2前半部 baseline file | Plan line 376-377 shows how to verify work-calendar-m2.baseline.json wasn't overwritten. However, Validation 3 at line 355-356 only checks that m2b baseline's meta.assertFailures === 0, NOT whether m2 baseline was preserved. These are two separate checks. If an implementer receives the file as work-calendar-m2.baseline.json (line 159-160 warns this will overwrite), the Validation 3 check would pass (checking m2b file), but the Acceptance line 402 check could fail. But by then, M2前半部's audit record is destroyed. The order matters: Validation 3 should verify m2 is safe BEFORE Task 0 completes. |
| invariant | MEDIUM | Anterior M2 baseline file overwrite could occur silently if Validation check is skipped | Plan lines 160-162 acknowledge the anterior work-calendar-m2.baseline.json is not version-controlled (.gitignore line 98), so `git checkout` cannot restore it if overwritten. The protection is the Validation check at lines 376-377 which verifies the hash. However, this check runs as a manual procedural step, not enforced by CI or pre-commit hooks (line 189: 절차 규칙이고 기계적 강제가 없다). If Task 0 accidentally copies the downloaded file to the repo root without running the full procedure and validation, the overwrite would not be detected until the Validation shell script is run—which is optional per the plan's governance model. The Acceptance item at line 402 does list this check, suggesting the reviewer is expected to run it. |
| invariant | MEDIUM | DD13 (onboarding) acceptance test incomplete—only validates error case, not happy path | Acceptance item at lines 413-414: 'Read failure를 첫 실행으로 오인하지 않는다 — projectsLoadFailed가 참인 상태에서...' tests only the scenario where projectsLoadFailed=true. It does not explicitly test the normal case: (1) first load with no projects → onboarding shows, (2) user creates project or skips, (3) reload → onboarding does NOT show. If the implementation checks !onboardingSeen BEFORE await this.settingsManager.initialize() (contrary to DD13 line 106-107), the flag would be undefined, making !undefined=true, causing onboarding to show every time. This initialization order bug would pass the documented acceptance test because that test only exercises the error path. Line 419 says to 'load the extension and test manually' but does not require the reload verification step. |
| invariant | LOW | Task 4 re-export procedure lacks procedural detail compared to Task 0 | Task 0 (lines 165-179) provides a detailed step-by-step procedure for handling the file name mismatch: the browser button hardcoded to download as 'work-calendar-m2.baseline.json' must be renamed to 'work-calendar-m2b.baseline.json' in the Downloads folder before moving to the repo. Task 4 (lines 264-267) only states 'press Export Baseline button and overwrite work-calendar-m2b.baseline.json' with no explicit procedure for the rename. The Validation check at line 286 would catch if the file was not re-exported (by checking for occupancy/* keys), but the procedure itself is vague enough that developers might confuse file names or forget the rename step. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Verified DD13 condition specification at line 103 against actual newtab.js implementation; checked SettingsManager initialization timing (newtab.js lines 5490-5506) against DD13 placement requirement; examined whether current code structure allows code to execute after `await this.settingsManager.initialize()` (it does not, as it is the last statement); verified that DD13 itself warns about unqualified ownership (lines 111-112) but leaves the third condition unqualified; checked Task 3 Action section for explicit access pattern specification (none found); confirmed that the condition and the abstraction boundary are not properly aligned, creating risk of misimplementation. |
| security | pass | Attacked: (1) Trust boundary on onboarding flag—who writes `calendarOnboardingSeen` and what prevents forgery. Found: only SettingsManager writes it via chrome.storage.sync, isolated per extension. (2) Path handling in baseline file procedure (Task 0)—checked for absolute paths, directory traversal, information leakage. Found: uses safe `$HOME/Downloads/` and relative paths; no committed data includes machine-specific info. (3) Baseline file overwriting risk—traced grep+shasum validation before/after mv step; verified previous baseline remains protected. (4) Grid/panel occupancy consistency (DD32)—verified both `rebuildIndex()` and `getEventsForDate()` are in same Task, blocking split-commit divergence. (5) Onboarding initialization timing—verified check occurs after `await this.settingsManager.initialize()` (line 5506), preventing undefined-as-true trap. (6) Dropped gate handling—verified cells remain occupied but rendering special-cases them to avoid false due-state leakage. Attempted but could not land: no data validation bypass, no privilege escalation, no bypass of the three-condition AND logic, no forge path for settings, no uncontrolled file operations. |
| test | fail | I examined the plan by: (1) Verifying line number citations against actual source code (rebuildIndex at 3321, getEventsForDate at 3353, getCellDueState at 3945 all confirmed accurate). (2) Checking if test functions (runCalendarOccupancyCases, runCalendarOnboardingCases) exist - they don't (expected, these are to be created by Task 1/3). (3) Searching for DD31-FIXTURE markers - none found yet (expected for a plan). (4) Examining test infrastructure: loadApp() does trigger full app initialization including rebuildIndex(). (5) Reading Task 1 occupancy specs - 5 fixed inputs defined with precise expected values, but test code for extracting bucketKeys/bucketSizesByDate/chipCountsByDate not shown. (6) Analyzing Task 2 changes: three specific rules defined (panel metadata dates, cell-level due state, project name placement), but only manual acceptance criteria for two of three. (7) Checking Acceptance section structure - 21 items total confirmed, 6 automated, 1 automated+manual, 14 manual. (8) Reviewing baseline safety checks - split between Validation 3 and Acceptance line 402, with potential race condition on file overwrite timing. |
| invariant | pass | Examined the plan's invariant-erosion landscape: fail-open drift, unknown input handling, receipt anchoring, rollback safety, and HALT-vs-degrade behavior. Verified that: (1) all Validation shell script checks exit with code 1 on failure (HALT), (2) two sha256 anchors (.claude/plans/work-calendar-m2b.baseline.sha256 and .claude/plans/work-calendar-m2b.rebaseline.sha256) verify pre- and post-implementation state; (3) anterior baseline file protection via hash verification (line 376-377) would catch overwriting IF Validation runs; (4) DD13 onboarding condition includes all three required predicates (projects.length, !projectsLoadFailed, !onboardingSeen), and plan specifies initialization order (line 106-107 'after await this.settingsManager.initialize()') but does not test that order; (5) Task 0 and Task 4 file handling both download a file with the wrong name (work-calendar-m2.baseline.json) due to hardcoded button; Task 0 has explicit rename procedure, Task 4 does not. Confirmed gates are fail-CLOSED (exit 1, not silently passing); acknowledged procedural limitations are explicit in the plan (line 189 'no mechanical enforcement', line 397 'this repository has no CI/runner'). No silent fail-open gate found." |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 785333,
  "halt_stage": null,
  "backlog_appended": 4,
  "backlog_skipped_nonblocking": 10,
  "granted": 4,
  "reviewed_plan_hash": "sha256:c0d1a75f7633e9e28fdbce5e684fed0031c322073d955ed77c493502af1f7ec2",
  "plan_path": ".claude/plans/work-calendar-m2b.plan.md",
  "recorded_at": "2026-08-23T14:47:03.016Z"
}
```
