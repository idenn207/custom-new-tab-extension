# Plan Review Panel — work-calendar-m2b

**Plan**: `.claude/plans/work-calendar-m2b.plan.md` · **Plan version**: `sha256:b5dd7979a4be6288b7ff36697b93c5ef2e774c16d13039734f7938a2e25e525d`
**Verdict**: `converged` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=true
**Layers**: L1 converged · L2 converged · L3 not fired

> Reason: L1 + L2 quorum satisfied (4/3 responses, 4 distinct roles); L3 not fired

## Findings

None — all 4 fielded reviewer(s) responded and passed.

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | Verified the structural coherence of DD31-DD35 (design decisions) and the data flow architecture. Confirmed all major citations for code locations are accurate (newtab.js:3321 rebuildIndex, 3353 getEventsForDate, 4179 panel query, 4198 createTodoItem, 4135 createChips, 4062 createDayCell). Verified m2a prerequisite data model exists (event.gates[] and event.projectId confirmed in createCalendarEvent at lines 608-609). Validated that both occupancy index AND panel query are changed together (DD32 protection against half-implementation). Confirmed window-cutting constraint is preserved and explicitly stated in Task 1 Action. Verified task ordering preserves architectural dependencies (Task 1-2 must complete before Task 3 onboarding creates projects). Checked baseline file management logic and confirmed .gitignore correctly excludes work-calendar-m2.baseline.json (line 98). Examined all six render functions to confirm which do/don't need changes aligns with the plan's requirements. Validated onboarding condition specification requires all four AND conditions (no OR) and includes settingsManager.calendarSettingsLoaded as first guard. Confirmed test harness structure integrates Task 1 and Task 2 assertions within single runCalendarOccupancyCases function as specified. No structural violations, boundary leaks, circular dependencies, or abstraction failures detected. |
| security | pass | **Fail-open onboarding mechanism (DD13):** Traced both paths where `calendarOnboardingSeen` could become `undefined` without proper initialization: (1) storage.get() throws in loadSettings() catch block (line 4587-4589) does not rethrow, leaving field unset; (2) SettingsManager.initialize() returns early (line 4341-4342) when DOM elements missing, never calling loadSettings(). Verified plan's fix initializes both `calendarSettingsLoaded = false` and `calendarOnboardingSeen = false` in constructor (line 531), sets `calendarSettingsLoaded = true` only in try block after reading calendarOnboardingSeen (line 532). Confirmed this creates fail-closed logic: when storage fails, `calendarSettingsLoaded` stays false, making the entire AND decision false, preventing onboarding. **Four-condition AND logic verification:** Examined decision formula (lines 515-520) requiring ALL four conditions true: `calendarSettingsLoaded AND projects.length === 0 AND !projectsLoadFailed AND !calendarOnboardingSeen`. Verified first condition is NOT negated (would be fail-open if inverted). Checked no OR operators exist. **Initialization ordering:** Verified Application.initialize() creates CalendarManager (line 5490-5491), then SettingsManager (line 5500-5506), ensuring both exist and are initialized before onboarding decision. Validation checks this ordering (line 880-882). **Flag write-side protection:** Confirmed plan specifies flag written both on project creation AND on user skip (line 535, 884-893), with grep validation requiring >=2 call sites (line 892-893). Verified plan explicitly excludes writing flag on storage-read failure (line 535). **Data leakage prevention:** Checked baseline files—`work-calendar-m2b.baseline.json` IS committed but contains only performance metrics; `.sha256` files contain only hashes. No absolute paths, no machine identifiers, no sensitive user data. Prior milestone's `work-calendar-m2.baseline.json` is protected by guard checks (line 944-949) and not overwritten. **XSS prevention:** Plan explicitly prohibits `innerHTML` in onboarding surface and project name rendering (line 1108), with mechanical grep validation in Phase 4. **Prerequisite field validation:** Validation checks `createCalendarEvent()` creates gates[] and projectId fields (line 672-674), preventing null-reference crashes in Task 1 gate iteration and Task 2 render rules. **No privilege escalation paths found:** Fields only writable via SettingsManager methods (saveCalendarOnboardingSeen), not exposed as public getters/setters. Projects array read-only from storage via CalendarManager. No external code can manipulate conditions. **Race conditions:** Onboarding decision is synchronously evaluated in Application.initialize() after both managers initialized; no async operations between decision and use. **All six onboarding test cases specified:** Plan includes test case 4 (storage read failure leaving calendarSettingsLoaded false) and test case 6 (DOM missing leaving both fields false but not undefined), distinguishing from test case 3 (projectsLoadFailed scenario). These specifically validate the fail-closed construction (line 544-546, 548-560). |
| test | pass | Attacked the following major test falsifiability claims: 1. **Occupancy transitions (DD31)**: Checked whether Validation 3-2c actually verifies the discontinuous occupancy output. Confirmed: the baseline values check (lines 987-997 of plan) explicitly validates that `range/01-month-boundary` produces `[2026-01-28, 2026-02-03]` not the full 7-day range. The test would fail if gates are not used or window-cutting is removed, since the occupancy structure would change. 2. **Grid-panel consistency (DD32)**: Validated whether DD32-CONSISTENCY-1 and DD32-CONSISTENCY-5 assertions would catch half-implemented changes. Confirmed: These assertions (lines 393-404 of plan code) check BOTH grid chip counts and getEventsForDate results simultaneously, so patching only one would fail. 3. **Pre-implementation prerequisites (Validation 1-c)**: Verified that `gates[]` and `projectId` fields exist in createCalendarEvent. Confirmed: Lines 608-609 of newtab.js show both fields present and operational. The prerequisite check at plan line 671-674 would correctly detect if either field was removed. 4. **Onboarding condition structure (Validation 2-b)**: Checked whether the grep patterns for the 4-part AND condition (lines 821-837 of plan) would catch incomplete implementations. Confirmed: The checks are scoped to Application.initialize() body, require all 4 conditions present, require 3+ AND operators, require NO OR operators, and require exact field names. A condition with only 2-3 parts or using OR would fail all checks. 5. **Co-located test design (Task 1 + Task 2 assertions)**: Verified whether combining occupancy fixtures with rendering rule assertions could hide incomplete work. Confirmed: Validation 2 line 724-726 explicitly checks within runCalendarOccupancyCases body for each TASK2-RULE-1/2/3 marker using assert(). Omitting these would make validation fail even if occupancy assertions exist. 6. **Window-cutting preservation**: Checked whether removal of window boundaries in rebuildIndex would be caught. The window-cutting logic is not explicitly machine-tested with a grep, but Validation 3-2c's baseline value check would fail if window-cutting were removed (occupancy arrays would differ from expected `[2026-01-28, 2026-02-03]` discontinuous pattern). 7. **Line drift in pre-impl phase (Validation 1-b)**: Verified that cited lines (3321 for rebuildIndex, 3353 for getEventsForDate, etc.) match actual code. Confirmed: Read lines 3321 and 3353 in newtab.js — both contain the expected functions. 8. **Baseline file overwrite risk (Validation 3-1)**: Checked whether the procedural gap around work-calendar-m2.baseline.json could be caught post-implementation. Confirmed: The validation correctly distinguishes between file absence (non-blocking NOTE) and file mismatch (blocking failure). The .gitignore tracking was verified (line 98). 9. **Test fixture structure (DD13-CASE count)**: Checked whether 6 onboarding cases could be dummies. The grep counts non-comment lines with DD13-CASE (line 717), and Acceptance items verify actual behavioral results at runtime. 10. **Rendering rule assertions**: Verified that the three Task 2 rendering rules (panel meta dates, dropped gate colors, project name placement) have actual code implementations in plan. Code blocks at lines 464-495 show specific DOM selectors and assertions that would verify the rendering. Found no CRITICAL or HIGH severity gaps in test falsifiability. All major claims have machine-checkable validation or behavioral acceptance tests that would catch violations. |
| invariant | pass | Attacked the following high-risk areas: 1. **Fail-open drift in onboarding logic** — Examined DD13 condition check (line 101-127). Plan adds field initialization in constructor (false defaults) and requires all 4 conditions be AND'd together. Validation 2-b (lines 806-837) now checks all 4 conditions present, joined with && (not \|\|), and first condition not negated. Previous version only checked one condition; current version validates each separately. Found this properly fail-closed. 2. **Undefined value handling** — The critical path where `calendarSettingsLoaded` and `calendarOnboardingSeen` could remain undefined if settings load fails. Plan fixes by: (a) initializing to false in SettingsManager constructor (line 116), (b) only setting to true in try block of loadSettings (line 117), (c) checking calendarSettingsLoaded first in condition (line 823-824). Validation 2-b scopes checks to constructor body specifically (lines 847-850), not file-wide, catching implementations that put defaults in initialize() instead (which early-returns on DOM absence). This is fail-closed. 3. **Assignment scope validation** — Line 868-871 validates calendarSettingsLoaded=true appears in try block, not catch/finally. Uses awk with exclusive range boundaries to avoid matching `} catch (error) { assignment;` which sed ranges would have caught. Tested this logic and found it correct. 4. **Baseline anchoring and file preservation** — Task 0 (lines 197-233) attempts to prevent overwriting work-calendar-m2.baseline.json from M2a. Uses mv with renamed file directly from Downloads (never placed in repo with old name). Guard checks distinguish file absent (safe, return 0) vs. file present-wrong (error, exit 1). Validation 3-1 (lines 943-950) re-verifies. Both checks fail-closed if anchor is missing or file corrupted. 5. **Validation scope erosion** — Checked multiple validations to see if scope narrowing allowed gates to be skipped. Validation 2 (line 688-726) scopes fixture checks to function body (occ() helper), preventing comments-only implementations. Validation 2-b scopes constructor checks to SettingsManager class (sm_ctor helper). Validation 2-c scopes getEventsForDate body to just that method. All scoping appears correctly bounded. 6. **Skip predicates and gate sequencing** — Examined if later validations could be skipped. Approval-time Validation 1-c requires createCalendarEvent() to exist and mention gates/projectId, establishing prerequisite. Task 0 must run first (marked as "첫 비가역 지점"). Validation 3 depends on artifacts from Task 0-4. No discoverable path to skip earlier gates and pass later ones. 7. **Resource accounting** — Checked if `saveCalendarOnboardingSeen()` is called in both success paths (create + skip). Validation 2-b section (v) at line 890-893 explicitly checks method declared once and called 2+ times. Found correct. 8. **Rollback paths** — Examined if claimed rollback of baseline preservation would work. File is gitignored (line 189-190), so git checkout won't recover it. Plan acknowledges and prevents the problem by never placing file in repo with old name in first place. This is fail-closed design, not rollback design. Found no defects in fail-closed logic, skip predicates, receipt anchoring (baseline SHA256 checks), resource accounting, or HALT-vs-degrade failures." |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "converged",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "converged",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": true
  },
  "wall_clock_ms": 526450,
  "halt_stage": null,
  "backlog_appended": null,
  "backlog_skipped_nonblocking": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:b5dd7979a4be6288b7ff36697b93c5ef2e774c16d13039734f7938a2e25e525d",
  "plan_path": ".claude/plans/work-calendar-m2b.plan.md",
  "recorded_at": "2026-08-26T05:53:52.136Z"
}
```
