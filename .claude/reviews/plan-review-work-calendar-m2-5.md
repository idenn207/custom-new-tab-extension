# Plan Review Panel — work-calendar-m2-5

**Plan**: `.claude/plans/work-calendar-m2-5.plan.md` · **Plan version**: `sha256:23885087c2800a399fbb572f646728e8d2f479974d5033a0e0fd8129c2895c33`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 3 blocking finding(s): invariant/CRITICAL, invariant/HIGH, invariant/FAIL — MCCP_REVIEW_SINGLE_PASS=deferred_to_prd_completion 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| invariant | CRITICAL | task2.pass skip predicate allows Task 2 bypass without execution—gate checks format but not truthfulness | Line 508-515 describes manual file creation without machine enforcement of test execution. Line 1023 and 1152 explicitly acknowledge '여전히 위조 가능' (still forgeable) and that 'Task 2 통과 뒤에야 Task 3 이 시작한다. **순서를 기계로 건다**' relies on Validate checks (line 828-834) that verify only the plan hash matches current state, not that Task 2 actually ran. Someone with knowledge of plan SHA can create the file manually. R1, R3, R4, R5, R7, R8 all flagged this as a skip-predicate flaw. |
| invariant | HIGH | Validation 1-g does not halt if .gitattributes rule is missing—approval gate passes despite prerequisite void | Line 795-798: 'grep -q 'sha256 text eol=lf' .gitattributes && echo NOTE' prints a NOTE only if the rule EXISTS. If missing, the grep fails silently and execution continues (no exit 1). The rule is supposed to be added in Task 0-a (line 314-326, implementation time), but Validation 1 (approval time, line 744-801) does not require it to exist yet, only notes it if already present. This is fail-open: if .gitattributes is deleted before Task 0 runs, Validation 1 still passes, and only Task 0 Validate (line 397) would halt. |
| invariant | MEDIUM | Baseline anchor cannot be verified post-implementation—digest coverage excludes the new created file (storage-adapter.js) | Task 0-b baseline (line 343) hashes: newtab.js newtab.html newtab.css test/positioning.smoke.js test/positioning.smoke.html (5 files). Task 7-c rebaseline (line 709-711) hashes the same 5 PLUS storage-adapter.js (6 files). storage-adapter.js is created in Task 1, so no baseline hash exists for comparison. Validation 3 line 873-880 only checks existence and line count of baseline.sha256, NOT '-c' verification (line 870-871 explains 구현 뒤 -c 는 구조적으로 반드시 실패). This is correct by design (baseline is sealed at Task 0 time), but it means the baseline cannot anchor the pre-Task-1 state of storage-adapter.js because that file did not exist. |
| invariant | MEDIUM | .gitattributes rule enforcement deferred past approval—fail-closed timing is broken for prerequisite | Line 314-326 Task 0-a adds the .gitattributes rule with inline confirmation, but this runs during IMPLEMENTATION (Task 0), not during APPROVAL (Validation 1). At approval time, Validation 1-g (line 795-798) does not fail if the rule is missing—it only prints a NOTE if already present. Then Task 0 Validate (line 397) requires it: 'grep -q 'sha256 text eol=lf' .gitattributes && shasum...' (AND-ed, so failure halts Task 0). The gap: Approval passes without the rule. Implementation halts without it. This breaks fail-closed ordering—the gate that MUST hold (Rule exists) is not enforced until after approval. |
| invariant | MEDIUM | Static validation of task2.pass cannot distinguish forged files from honest execution—no rollback anchor for fake file | Lines 1023, 1040, 1152 document repeated findings (R1-R8) that task2.pass can be forged if someone knows the plan SHA at the moment Task 2 finishes. The control is position-based (current plan SHA), not cryptographic. If the plan is modified after Task 2 (but before Task 3), the old task2.pass becomes stale and Task 3 fails, forcing re-run of Task 2. But if someone forges the file WITH the current plan SHA while creating it, there is no machine test of actual execution to catch it. R8 (line 1152), R10 (line 1207) both state the root cause is lack of headless runner. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | Attacked the plan along the architect lens (structure, boundaries, abstraction): (1) Verified all critical line citations are accurate (`newtab.js:157` adapter warning, `:5841` window exposure, `:203-209` merge pattern, `:18` SETTINGS_VERSION). (2) Examined abstraction integrity: DD6 (adapter move) claims byte-identical transfer; verified cited pattern exists at `:157-158`. DD7 (lexical vs. window) verified via existing `window.__newTabApp` precedent at `:5841`. DD14 (scoped substitution) grounded in `:203-209` showing `set()` merges; correctly delegates stale-key detection to `collectStaleInScopeKeys`. (3) Checked boundary leaks: restore abstraction ("recover whole state from file") is bounded by UI3/UI5/Task 5 disclosure that images excluded. Deny-list approach for unknown keys assumed safe but not audited in code. (4) Traced coupling and seams: storage-adapter exposure via `window.__newTabStorage` mirrors existing `window.__newTabApp` surface (acknowledged line 1286). Script loading order validated via `awk` checks in Validate blocks. (5) Examined task gating: task2.pass dependency is fail-closed (Validate fails if missing) but acknowledges forgery possible. (6) Checked M3/M4 forward-compatibility rule in DD13: guidance only, no code enforcement, but reasonable for future milestones. (7) Validated Validation gate sequencing: Task 0-f records content hash via `moved-range.txt`, Task 1 Validate checks it—fail-closed if Task 0-f skipped. (8) Reviewed panel record L2: 10 rounds R1-R10 examined 50+ findings, architect perspective passed R1-R3, R6-R7; no blocking HIGH/CRITICAL from architect since R2. Absorbed findings documented in plan text itself (lines 971-1207). No undisclosed structural holes discovered." |
| security | pass | Attacked prototype pollution via nested objects in backup JSON (JSON parsing semantics prevent exploitation); version escalation via future schema (explicitly rejected before storage access); deny-list bypass via edited backup file (hardcoded exclusions + test case 14 verification); atomic restore safety via set/remove ordering (superset-on-crash design prevents data loss); unauthorized access to backup file (Downloads folder, no chrome.storage exposure); privilege escalation through restore data (whitelist parsing, batch rejection on failure); storage adapter exposure risk (window.__newTabStorage is intentional per DD7, pre-existing pattern mirrors window.__newTabApp at newtab.js:5841). Traced: (1) malformed backup through parseBackupEnvelope rejection path to untouched storage; (2) future version backup to reject via DD13 settingsVersion gate before storage.get() call; (3) stale key removal order to set() then remove() with superset-safe crash state; (4) excluded key preservation to hardcoded list bypassing restore scope; (5) undo slot persistence through same set() call as restore data. Could not construct evidence of exploitable trust boundary, data leakage path, or privilege escalation that the plan does not address. |
| test | pass | 1. **Adapter portability - examined whether selectStorageBackend() correctly picks 'extension' vs 'local-preview' for each context.** Task 2 Case 8 checks that both harness and app have the same constants, and Task 1 runtime witness checks that DOM attribute is set. Task 7 requires human verification that value is correct for each origin. This is adequate - the value correctness is falsifiable via browser execution (Task 7), which the plan explicitly requires. 2. **DD14 range-limited restore implementation - verified whether 'set() once then remove() stale keys' is actually tested.** Case 14 validates end state (stale key is gone), Case 10 validates call counts and order via spy, Case 1 validates images and slot remain. These combined ensure the implementation must match spec. 3. **DD13 version gateway - checked whether all three version cases (match/lower/higher) are tested.** Cases 11 (higher rejected) and 12 (lower promoted) test explicit paths, Case 1 round-trip implicitly tests match case. Plan explicitly states any path must do `set()` once before `remove()` and reload after success. 4. **Deny-list safety (Case 9) - examined whether deny-list actually works vs hidden whitelist.** Plan acknowledges (R2, line 1004) that Case 9 alone is narrow but Case 14 together validates that stale keys are removed, creating defensive coverage. 5. **Task 2.pass marker file - checked whether creation is specified and gated.** Plan DD line 509-515 specifies creation after running harness. Task 3-6 Validate gates on file existence and content. Plan acknowledges forgery risk (line 519) and notes only runner prevents this - that's an accepted tradeoff. 6. **Filename format validation - confirmed format 'newtab-backup-YYYY-MM-DD.json' is untested in harness.** Plan Task 5 describes format, no unit test validates it, but human test in Task 8 confirms file structure. This is appropriate for UI detail. 7. **Undo button disabled state - checked whether conditional disable is tested.** Case 5 tests undo works after restore, but no test validates button is disabled before slot exists. Plan Task 5 line 604 states requirement, Task 5 Validate checks button exists, Task 8 human test validates behavior. Limitation is noted and acceptable. 8. **Validation phases structure - confirmed that approval-time (phase 1) and post-implementation (phase 2) checks are properly separated.** All approval-time checks are code-only (grep, syntax); post-implementation checks require actual files to exist. No gap found. 9. **Test marker validation (runBackupRestoreCases, __futureKeyProbe, setCallSpy, etc.) - verified that markers prove presence, not correctness.** Plan explicitly states this (line 683-684): "증명하는 것은 케이스가 거기 있다 까지이고 단언의 값이 옳은지는 아니다 — 그 판정은 브라우저 실행(Validation 4)이 내린다." Browser execution (Task 7) catches correctness. Acknowledged tradeoff. 10. **Review absorption - examined all 8 review rounds (R1-R8) to identify remaining unaddressed findings.** R8 lists 13 blocking findings; plan notes that "task2.pass 위조 가능", "표식 grep 은 존재만 증명", and "러너가 없어" distinguish honest from forged - all acknowledged as requiring `id=m2-headless-runner` (outside this milestone). No unaddressed critical gaps found. All major claims either have adequate test coverage (some manual) or are transparently acknowledged as limited by lack of CI/runner. Plan demonstrates falsifiability through its extensive review history and explicit limitation documentation. |
| invariant | fail | I attacked the following fail-closed invariants: 1. **Skip predicates** — traced task2.pass file from creation (manual, user-dependent) through Task 3-6 validation (format+hash checks only). Found that the skip predicate can be forged with knowledge of plan SHA; gate checks format but not execution. 2. **Prerequisite enforcement order** — examined .gitattributes rule requirement at approval time vs implementation time. Validation 1-g prints a note if rule exists but does not halt if missing. Rule is added later in Task 0-a. This breaks fail-closed ordering: approval should block on missing prerequisite, but Validation 1 is informational only. 3. **Digest coverage** — verified what baseline.sha256 (Task 0) includes vs rebaseline.sha256 (Task 7). Found baseline excludes storage-adapter.js (doesn't exist yet at approval), rebaseline includes it. No baseline hash of the new file can be anchored to pre-Task-1 state. 4. **Rollback paths** — examined undo slot creation (DD5, Task 4). Found it is not a rollback, only an undo for restore mistakes. Undo slot lives in storage and has no guarantees if storage is evicted. No cryptographic anchor. 5. **HALT vs degrade** — looked for silent failures and partial passes. Task 2 Validate correctly halts if task2.pass is missing (exit 1 line 829). But Validation 1-g degraded from HALT to NOTE for .gitattributes absence. All five findings are acknowledged in the plan's review sections (R1-R10) as known limitations due to lack of headless runner. However, they represent erosion of the fail-closed property this gate claims to enforce. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 421781,
  "halt_stage": null,
  "backlog_appended": 3,
  "backlog_skipped_nonblocking": 3,
  "granted": 4,
  "reviewed_plan_hash": "sha256:23885087c2800a399fbb572f646728e8d2f479974d5033a0e0fd8129c2895c33",
  "plan_path": ".claude/plans/work-calendar-m2-5.plan.md",
  "recorded_at": "2026-08-27T08:03:13.035Z"
}
```
