# Codex / 리뷰 패널 findings 백로그

게이트가 **지금 흡수하지 않기로 한** 지적이 쌓이는 append-only 원장이다. 두 경로가 여기에 적재한다.

- `/mccp:plan` Phase 5.4 — Codex 라운드에서 `DEFER_TO_BACKLOG` 로 분류된 항목
- `/mccp:plan` Phase 5.2g2 — `MCCP_REVIEW_SINGLE_PASS` 로 반복 라운드를 생략했을 때 L2 패널이 낸 blocking findings

**두 번째 표기 `~~반증(날짜)~~` 는 흡수가 아니라 기각이다.** 지적의 전제를 실제로 실행해 확인했더니 사실이 아니었다는 뜻이며, 근거를 같은 셀에 함께 적는다. `~~해소~~`(고쳤다)와 구분되어야 하는 이유는, 기각된 지적은 다음 라운드에 같은 형태로 다시 올라올 수 있고 그때 근거 없이 재흡수하면 없는 결함을 좇게 되기 때문이다.

**행을 지우지 않는다.** 해소된 항목은 `Finding` 셀 앞에 `~~해소(날짜)~~` 를 붙여 표시하고 행 자체는 남긴다 — 지적이 사라지면 "검토했는데 안 고치기로 했다"와 "애초에 없었다"를 구분할 수 없게 된다.

아래 헤더 줄은 형식이 아니라 **계약**이다. `scripts/lib/plan-review/backlog-append.js` 의 `HEADER_RE` 와 대시보드의 `derive/sources/backlog.js` 가 정확히 이 줄에 표를 앵커하므로, 문구를 바꾸면 적재된 행이 모든 소비자에게 보이지 않게 된다.

| Date | Severity | Source plan | Finding |
|---|---|---|---|
| 2026-08-21 | HIGH | .claude/plans/work-calendar-m1.plan.md | ~~해소(2026-08-21)~~ L2 security: Trust boundary violation: answers.md file integrity not fully verified between GATE-A and GATE-C, allowing validation criteria to be weakened · 원문 .claude/reviews/plan-review-work-calendar.md · id=3723da7b |
| 2026-08-21 | HIGH | .claude/plans/work-calendar-m1.plan.md | ~~해소(2026-08-21)~~ L2 security: Incomplete response to pre-existing security finding: answers.md file lacks git status validation despite explicit identification as trust boundary · 원문 .claude/reviews/plan-review-work-calendar.md · id=930820d5 |
| 2026-08-21 | FAIL | .claude/plans/work-calendar-m1.plan.md | ~~해소(2026-08-21)~~ L2 security: reviewer returned verdict=fail · 원문 .claude/reviews/plan-review-work-calendar.md · id=988d9372 |
| 2026-08-21 | HIGH | .claude/plans/work-calendar-m1.plan.md | ~~해소(2026-08-21)~~ L2 test: GATE-C validates that inheritance is 'reflected in output' and that impe ccable used Brand Commitments correctly (DD13, line 493: 'GATE-C가 그 상속이 산출물에 실제로 반영됐는지를 본다') · 원문 .claude/reviews/plan-review-work-calendar.md · id=bffb42e8 |
| 2026-08-21 | FAIL | .claude/plans/work-calendar-m1.plan.md | ~~해소(2026-08-21)~~ L2 test: reviewer returned verdict=fail · 원문 .claude/reviews/plan-review-work-calendar.md · id=b164d498 |
| 2026-08-21 | HIGH | .claude/plans/work-calendar-m1.plan.md | ~~해소(2026-08-21)~~ L2 invariant: Reference validation uses substring matching instead of exact section matching, creating fail-open vulnerability for future section name collisions · 원문 .claude/reviews/plan-review-work-calendar.md · id=6130e307 |
| 2026-08-21 | FAIL | .claude/plans/work-calendar-m1.plan.md | ~~해소(2026-08-21)~~ L2 invariant: reviewer returned verdict=fail · 원문 .claude/reviews/plan-review-work-calendar.md · id=a60b6308 |
| 2026-08-21 | HIGH | .claude/plans/work-calendar-m1.plan.md | L2 architect: The answer sheet template (lines 542-545) provides an incomplete list of sections to be inherited in Brand Commitments · 원문 .claude/reviews/plan-review-work-calendar.md · id=542b95a0 · **부분유효 · ~~해소(2026-08-22)~~ — 답안지 `document --seed` 절에 "Layout·Shapes 는 값 없이 규칙 수준으로만 쓴다" 를 명시하고 die 문구의 오해 소지까지 함께 적었다(5차 라운드 design-critique F1 편집과 같은 자리).** 리터럴 주장은 틀렸다(DESIGN.md 에 `Layout`·`Shapes` 절이 아예 없으므로 이름으로 상속할 대상 자체가 없다 — 리뷰어가 "생성물이 가질 7절"과 "원본에서 상속할 절"을 합쳤다). 남는 알맹이: 원본에 대응 절이 없는 Layout·Shapes 를 seed 가 구체 수치로 채우면 `gate-final` 의 토큰 전수 검사가 죽는다. 원본 토큰 108개에 `1px`·`10px`·`12px`·`14px`·`16px`·`1rem` 이 있고 `grep -F` 라 `1px` 는 `21px` 안에서도 걸리므로, px 값이 하나라도 들어가면 사실상 확정 실패다. **조치**: Task 3 의 `/impeccable document --seed` 인터뷰에서 Layout·Shapes 를 값 없이 규칙 수준으로만 쓰게 한다 — 이것은 UI6(컴포넌트 스펙 미기재)·DD7(값이 아니라 규칙)이 이미 요구하는 바이며, 답안지에 그 문장이 없을 뿐이다. 실패 시 die 문구("상속 값이 복사됐다 — 참조로 바꿔라")는 이 경우 오해를 부른다: 참조할 원본 절이 없으므로 올바른 복구는 참조 전환이 아니라 **값 삭제**다 |
| 2026-08-21 | CRITICAL | .claude/plans/work-calendar-m1.plan.md | ~~반증(2026-08-21)~~ L2 architect: The answer sheet references 'Named Rules 절' which is a subsection (###), not a top-level section (##) · 원문 .claude/reviews/plan-review-work-calendar.md · id=b9e1636a · 반증근거 refs_resolve 의 절 추출 정규식은 `^#{2,4}` 이므로 DESIGN.md:355 `### Named Rules` 가 그대로 추출된다 — 최상위 절만 본다는 전제가 사실이 아니다 |
| 2026-08-21 | CRITICAL | .claude/plans/work-calendar-m1.plan.md | ~~반증(2026-08-21)~~ L2 architect: The plan's answer sheet section names (e.g., 'Elevation 절') may not match impeccable's generated names ('Elevation &amp; Depth') · 원문 .claude/reviews/plan-review-work-calendar.md · id=a7538f42 · 반증근거 refs_resolve 는 **원본** DESIGN.md 의 절 이름과 대조하고 그 이름은 `Elevation` 이다(DESIGN.md:341 `## 4. Elevation: 유리라는 깊이`). `Elevation & Depth` 는 생성물 헤더 이름이며 gate-c 가 부분문자열로 따로 본다 |
| 2026-08-21 | FAIL | .claude/plans/work-calendar-m1.plan.md | L2 architect: reviewer returned verdict=fail · 원문 .claude/reviews/plan-review-work-calendar.md · id=4bf8898b |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m1.plan.md | design-critique F3: 대리 판정 둘째 물음("겹친 부하를 어떤 축으로 가르는가")의 가용 축 제약 집합이 한 자리에 모여 있지 않다 — 알파 불가(2단계 고정, 이미 중요도가 사용) · hue 추가 불가(accent 하나) · 색 단독 불가(Do 규칙) · 상태 색은 발생 시에만. 읽는 사람이 네 곳에서 스스로 모아야 한다. M1 위험은 Acceptance 대리 판정이 잡고 축 선택 자체는 DD7 이 M1 에서 금하므로 M3 입력으로 보존한다 · 원문 .claude/plans/work-calendar-m1.plan.md ## Design Critique |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m1.plan.md | L2 architect: The plan's gate validation strategy can distinguish between documents that claim to inherit and documents that actually inherit · 원문 .claude/reviews/plan-review-work-calendar-m1.md · id=42c6a75d |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m1.plan.md | L2 architect: The answer sheet format sufficiently constrains what can be injected during the impeccable interviews without additional runtime validation · 원문 .claude/reviews/plan-review-work-calendar-m1.md · id=0a1a65fb |
| 2026-08-22 | FAIL | .claude/plans/work-calendar-m1.plan.md | L2 architect: reviewer returned verdict=fail · 원문 .claude/reviews/plan-review-work-calendar-m1.md · id=c175d073 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m1.plan.md | L2 test: Values are inherited from DESIGN.md without redefinition (DD13, DD4) · 원문 .claude/reviews/plan-review-work-calendar-m1.md · id=c218e88f |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m1.plan.md | L2 test: Tags are correctly placed as [셸] or [시계] based on mode-specificity (DD5) · 원문 .claude/reviews/plan-review-work-calendar-m1.md · id=75093457 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m1.plan.md | L2 test: Rationale/justification is updated when values are inherited with different success metrics (DD5, line 101-103) · 원문 .claude/reviews/plan-review-work-calendar-m1.md · id=1409073e |
| 2026-08-22 | FAIL | .claude/plans/work-calendar-m1.plan.md | L2 test: reviewer returned verdict=fail · 원문 .claude/reviews/plan-review-work-calendar-m1.md · id=44a902d0 |
| 2026-08-22 | MEDIUM | newtab.css:2153 | **M1 범위 밖 — 코드 무변경(UI8) 때문에 남긴다.** `PRODUCT.md:108` 이 가리키는 죽은 참조(`DESIGN.md`의 존재하지 않는 `Glass` 절)와 **동일한 문장이 `newtab.css:2153` 주석에도 있다** — "텍스트가 아니라 표면(brightness) 쪽에서 풀어야 한다 — DESIGN.md의 Glass 절". M1 의 DD12 는 문서 쪽만 고치고 `refs_resolve` 도 문서 넷만 보므로, M1 종료 시점에도 저장소에 같은 죽은 참조가 하나 남는다. 값 자체는 확인됐다: 실제 CSS 는 `brightness(0.62)`(`newtab.css:862`·`:1493`)이고 `DESIGN.md:357` 이 옳다. 조치는 코드를 만지는 마일스톤(M3)에서 이 주석을 `DESIGN.md` 4절 The brightness Rule 참조로 바꾸는 것 · 원문 실측 2026-08-22 · id=css-glass-ref |
| 2026-08-22 | MEDIUM | 상류(idenn207/mccp#155) | **M1 범위 밖 — mccp 자신의 결함.** impeccable 이 npm CLI 에서 marketplace plugin 으로 배포 채널을 옮기면서 `impeccable-detect.js#probeSkillAvailable` 이 실제 설치를 인식하지 못한다(`impeccable@anthropics` 를 찾는데 실제 키는 `impeccable@impeccable`). 지금 `true` 가 나오는 것은 레거시 `~/.claude/skills/impeccable` 폴백 때문이고, **DD19 가 요구하는 정리를 하는 순간 mccp 의 디자인 게이트가 조용히 꺼진다**(fail-open). `/mccp:setup` Phase 3 은 폐지된 npm CLI(latest 3.6.0, plugin 은 4.1.1)를 권하며 그 레거시 디렉터리를 다시 만든다. M1 에 미치는 실효는 receipt 의 `impeccable_skipped` 가 참으로 찍힐 수 있다는 것뿐이며, 이 마일스톤의 판정자는 receipt 가 아니라 `verify.sh` 다 · 상류 보고 완료 · id=mccp-155 |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m1.plan.md | ~~반증(2026-08-22)~~ 6차 L2 test: "verify.sh 가 아직 없고 gate-a→gate-final 전체 순서를 끝까지 돌린 적이 없다" · **반증근거**: verify.sh 부재는 결함이 아니라 설계다 — Task 0 이 만들고 GATE-A 는 파일이 존재한 뒤에 돈다(플랜이 "부트스트랩 순환은 없다"로 명시). 전체 순서는 **plan 단계에서 원리적으로 완주할 수 없다**: Task 2·3 이 impeccable 대화형 인터뷰(4.1.1은 world workshop + 결정 페이지)를 요구하기 때문이다. 그 완주는 Acceptance 의 마지막 항목이 implement 단계에 요구하며, 그것이 이 항목이 있는 이유다. 대신 plan 단계에서 가능한 것은 전부 실행했다 — 블록 추출·`bash -n`·함수 단위 실행·gate-a 실제 실행(레거시 가드 발화 확인) · id=6t-critical |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m1.plan.md | ~~반증(2026-08-22)~~ 6차 L2 test: "answers.md 가 존재하지 않는다" · **반증근거**: 위와 같은 범주 오류다. 답안지는 Task 0 의 산출물이고 GATE-A 가 그 뒤에 읽는다. 리뷰어가 요구하는 것은 "플랜이 Task 0~7 을 이미 수행했을 것"이며 그것은 플랜 단계의 판정 대상이 아니다 · id=6t-answers |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m1.plan.md | ~~반증(2026-08-22)~~ 6차 L2 test: "DESIGN.md 의 4절은 176행에서 시작하는 '1. Overview' 이므로 DD12 의 `4절 The brightness Rule` 참조가 틀렸다" · **반증근거**: 리뷰어가 **행번호와 절 번호를 혼동**했다. 176행은 `## 1. Overview`, `## 4. Elevation: 유리라는 깊이` 는 **341행**이고 `**The brightness Rule.**` 은 357행으로 그 4절 안에 있다. 실측으로 확정: 새 DD21 의 `번호\|이름` 대조표가 `4\|brightness` 를 담고, `DESIGN.md 4절 The brightness Rule` 은 통과하며 `2절 The brightness Rule` 은 차단된다 · id=6t-sec4 |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m1.plan.md | ~~반증(2026-08-22)~~ 6차 L2 test: "GATE-E 는 tag-map 행 수만 보고 절이 맞는지는 안 본다" · **반증근거**: GATE-E 는 행 수 대조에 **더해** 파일별로 실제 헤더를 파싱해 각 절을 tag-map 에서 찾고(없으면 die), 선언 태그와 실제 태그가 다르면 die 한다. 리뷰어 자신이 근거에서 그 루프의 존재를 인정하면서 "per-file 이라서" 부족하다고 적었는데, tag-map 행이 파일명을 담으므로 per-file 이 정확한 동작이다. 행 수 14 = 8+6 이 맞고 **모든 실제 절이 map 에서 발견되면** map 은 실제 집합과 정확히 일치한다(`sort -u` 뒤의 수이므로 중복도 배제된다) · id=6t-gatee |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m1.plan.md | ~~반증(2026-08-22)~~ 8차 L2 test: "GATE-C 는 Layout·Shapes 절이 수치 없이 쓰였는지 검사하지 않는다 — gate-final 의 토큰 검사가 사후에 잡을 뿐이다(prevention 이 아니라 fail-closed recovery)" · **반증근거**: GATE-C 에 그 검사가 **있다**. `awk '/^## /{ inx = ($0 ~ /Layout|Shapes/) } inx'` 로 두 절만 뽑고 `BADTOK` 로 `em|rem|px|s`·hex·비율 토큰을 적발해 die 한다. 합성 산출물(`## Layout` 에 `8px`, `## Shapes` 에 `12px`)로 실행 검증했고 `적발 토큰: '12px 8px '` 로 차단됐다. 이 검사는 정확히 리뷰어가 없다고 한 prevention 이며, die 문구가 "값 삭제"라는 올바른 복구를 지시하는 것까지 설계돼 있다 · id=8t-layshape |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m1.plan.md | ~~반증(2026-08-22)~~ 8차 L2 test: "DD18/DD13 은 workshop 이 핀을 지켰는지 게이트가 확인하지 못한다 — 플랜 스스로 '게이트는 이것을 잡지 못한다'고 적었다" · **반증근거**: 리뷰어가 **플랜의 정직한 잔여 명시를 결함의 증거로 인용**했다. 상속이 실제로 일어났는지는 논증의 문제이고 셸이 판정할 수 있는 종류가 아니다. 플랜은 그것을 숨기지 않고 적었으며, 형태 축 넷(DD23 Brand Commitments 절 내 must-inherit · DD21 절 번호 실재 · DD16 규칙 이름 충돌 · gate-final 토큰 전수)으로 기계가 좁힐 수 있는 만큼 좁혔다. **잡는 척하는 것이 정확히 DD20 이 고친 결함**(게이트가 잡는다고 적힌 것을 안 잡는 것)이므로, 이 지적을 "흡수"하는 유일한 방법은 그 결함을 다시 만드는 것이다. 잔여는 Acceptance 통독이 맡는다 · id=8t-pin |
| 2026-08-22 | LOW | .claude/plans/work-calendar-m1.plan.md | 8차 L2 test: "선언하지 않은 절을 추가로 상속하거나 선언한 절을 재정의하는 것은 DD16 이름 충돌 외에는 잡히지 않는다" · **부분유효 · M3 입력으로 보존**. 리터럴로는 맞다. 다만 M1 의 방향은 **과잉 상속이 아니라 상속 누락**이 위험이고(seed 가 새 세계를 발명하려 든다 — DD18), 선언 밖 상속은 값 복사면 gate-final 토큰 전수가, 규칙 재정의면 DD16 이 각각 잡는다. 남는 것은 "값도 규칙 이름도 안 겹치면서 선언 밖 절을 참조하는 것"이며 그것은 UI5 위반이 아니라 참조 과다이므로 M1 에서 막을 이유가 약하다. M3 가 scan 모드로 갈 때 다시 본다 · id=8t-undeclared |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m1.plan.md | ~~반증(2026-08-22)~~ 9차 L2 invariant ×4건(부트스트랩 역설 / 판정자 부재 / receipt 앵커 불성립 / 추적 요구 불가능 — **넷 다 같은 주장**): "GATE-A 가 `tracked "$ANSWERS"; tracked "$VERIFY"` 를 요구하는데 그 파일들은 Task 0 이 만들 것이므로 논리적 불가능" · **반증근거(실행)**: `tracked()` 가 쓰는 `git ls-files --error-unmatch` 는 커밋이 아니라 **인덱스**를 본다. 프로브 실측 — `git add` **전 실패, 후 통과**(커밋 없음). 따라서 Task 0 의 순서 생성 → `git add` → `gate-a` 가 그대로 성립하고 커밋은 Task 7 에 온다(DD8). 리뷰어가 관측한 것은 "플랜 검토 시점에 파일이 없다"이며 그것은 Task 0 이 아직 수행되지 않았다는 사실이지 결함이 아니다. 6차 패널 test 가 낸 같은 오독의 재발이므로 Task 0 머리에 기전을 못박았다 · id=9i-bootstrap |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m1.plan.md | ~~반증(2026-08-22)~~ 9차 L2 test: "DD1·DD3 의 impeccable 동작 주장을 실행 전에 검증하는 test 가 없다 — 게이트는 문서 생성 뒤에야 잡는다" · **반증근거**: (a) 플랜이 이 잔여를 DD1 에서 **명시적으로 계산해 두었다** — "GATE-A 가 확인하는 것은 문서이지 동작이 아니다… 그 시점까지 쓰인 것은 저장소 밖 임시 디렉터리 하나이고 GATE-D 가 무변경을 증명하므로 잃는 것은 인터뷰 한 번의 시간뿐이다". (b) 리터럴 주장도 틀렸다 — **GATE-B 의 양성 프로브가 `context.mjs` 를 실제로 실행**해 `PRODUCT_INIT_REQUIRED` 를 요구하고 `incumbent visual implementation` 부재를 확인한다. 이것이 생성 **전에** 도는 실행 검증이다. (c) 남는 잔여(같은 판본에서 동작만 바뀜)를 앞당기려면 seed 를 한 번 돌려야 하는데 그것이 곧 Task 3 이므로 앞당길 자리가 없다 · id=9t-behavior |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m1.plan.md | ~~해소(2026-08-22)~~ 9차 L2 test: "Layout·Shapes 수치 정규식이 지나치게 넓어 `The 1px Rule` 이나 설명문 `1px margin` 도 걸린다 — 오탐이고 문서화도 안 됐다" · **부분유효 → 문서화로 해소**. 걸린다는 것은 사실이다. 그러나 두 절의 계약이 "값 없이 규칙 수준으로만"(UI6/DD7)이므로 수치가 규칙 이름에 있든 설명문에 있든 **계약 위반이며 정탐**이다. 좁히면 "규칙 이름에 숨긴 수치"라는 우회로가 생긴다. 방향도 안전하다 — 막히면 값을 지우면 되고(die 문구가 그렇게 지시), 놓치면 gate-final 이 더 나쁜 문구로 뒤늦게 죽는다. GATE-C 주석에 이 판단을 명시했다 · id=9t-regex |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m1.plan.md | 9차 L2 test ×2(DD7 대리 판정의 논증 충분성 / DD18 핀이 실제로 적용됐는지): **잔여로 확정하고 보존**. 둘 다 플랜이 스스로 "게이트는 이것을 잡지 못한다"고 적은 자리를 인용한 것이다. 상속과 논증 충분성은 셸이 판정할 수 있는 종류가 아니며, 잡는 척 적는 것이 정확히 DD20 이 이번에 고친 결함이다(다섯 라운드를 고치고도 `gate-final` 에 Open Question 검사가 없던 자리). 기계로 좁힐 수 있는 축은 이번 라운드에 넷 늘렸다 — DD21 절 번호 · DD23 상속 목록의 절 내 위치 · DD24 답안지↔플랜 결속 · DD25 절 순서. 잔여는 DD26 의 rubric 과 Acceptance 통독이 맡고, 판정 결과는 DD20 이 기계로 요구하는 PRD 줄에 기록된다 · id=9t-residual |
| 2026-08-22 | LOW | .claude/plans/work-calendar-m1.plan.md | 9차 L2 test ×2 + invariant ×1 (LOW): (a) "Task 0 부트스트랩이 순차 서술만 읽으면 명확하지 않다" — 리뷰어 스스로 "실측 기록을 보면 실제로 동작한다"고 인정했고, 그 사이에 Task 0 머리 주석을 추가했다(id=9i-bootstrap). (b) "DD25 는 최상위 절 순서만 보고 하위 헤더 순서는 안 본다" — 리뷰어 스스로 "게이트가 주장하는 바를 정확히 검사한다"고 인정한 **설계 범위**이며, 하위 헤더는 절 안의 구성이지 정규 순서의 대상이 아니다. 둘 다 조치 불요 · id=9-low |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m1.plan.md | Implement-Codex R1 F2: "`.gitattributes` 의 LF 규칙은 Task 0 이 지시만 하고 GATE-A 는 검사하지 않는다 — GATE-A 는 현재 `verify.sh` 바이트에 CR 이 있는지만 보므로, `.gitattributes` 를 빠뜨린 채 LF 파일을 만들면 통과하고 이후 재체크아웃에서 CRLF 로 내려와 bash 가 죽는다" · **유효하나 M1 에서 고치지 않는다**. 제안된 수리는 GATE-A 에 `.gitattributes` 추적 검사를 더하는 것인데, `verify.sh` 는 플랜 본문 bash 블록과 **바이트 비교**되므로 게이트 수정은 곧 플랜 수정이고, 플랜 수정은 plan sha256 을 바꿔 `mccp-plan-codex` 수신증과 DD20 의 PRD 게이트 앵커를 함께 무효화한다. MEDIUM 한 건의 대가로는 폭발 반경이 너무 크다. 완화: Task 0 이 `.gitattributes` 를 실제로 만들고 그 사실을 구현 보고서에 명시한다 · id=impl-r1-f2 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m1.plan.md | **실행으로만 발견된 verify.sh 결함 2건 — 아직 고치지 않았다.** (a) `sections-numbered` 를 만드는 awk 가 `^## [0-9]+\.` 분기에서 `[셸]`·`[시계]` 태그를 **벗기지 않는다**(`^#{3,4}` 분기에는 `sub(/\[[^]]*\]…/)` 가 있다). 그래서 Task 6 이 태그를 붙인 뒤에는 **콜론 없는 절**의 이름이 `Typography [셸]`·`Do's and Don'ts [셸]`·`Components [시계]` 로 잡히고, 그 절을 번호로 가리킨 참조가 전부 `참조가 단 절 번호가 원본과 다르다` 로 죽는다(콜론 있는 절은 `sub(/:.*$/)` 가 우연히 태그까지 함께 지워 살아남는다 — 즉 **우연히** 통과하고 있었다). gate-final 바로 위 주석이 "refs_resolve 가 [셸]·[시계] 를 벗겨낸다"고 적고 있어 의도는 명백하고 코드가 그것을 따르지 않는다. 이번 실행은 참조에서 번호를 빼는 것으로 우회했다(DD4 는 이름이 원본에서 반복될 때만 번호를 요구하고 두 이름은 반복되지 않으므로 규약 위반이 아니다). 수리는 `^## [0-9]+\.` 분기에 `sub(/\[[^]]*\][[:space:]]*/, "", name)` 한 줄. (b) 토큰 전수 검사의 `grep -qF "$TOK"` 에 `--` 가 없어 `-4px`·`-0.05em` 등 **음수 토큰 6개가 grep 옵션으로 해석돼 검사되지 않는다**(`grep: unknown option` 이 실행 로그에 그대로 남는다). `! grep` 이 감싸고 있어 오류 종료가 "복사 없음"으로 읽히는 **fail-open** 이다. 이번 실행에서는 `grep -F --` 로 따로 확인해 실제 복사가 없음을 검증했다. 수리는 `grep -qF -- "$TOK"`. 둘 다 플랜 본문 bash 블록을 고쳐야 하고(바이트 비교), 그것은 plan sha256 과 PRD 게이트 기록 줄을 함께 바꾸므로 M3 이 플랜을 다음에 여는 시점에 묶어서 처리한다 · id=m1-verify-defects |
| 2026-08-22 | MEDIUM | plugins/mccp | mccp `scripts/lib/impeccable-detect.js:139` 의 `probeKeys` 가 매니페스트 키를 `impeccable@anthropics` 와 `impeccable` 둘만 찾는데, marketplace 설치의 실제 키는 `impeccable@impeccable` 이다. 그래서 plugin 4.1.1 이 설치·활성화돼 있어도 프로브가 실패해 폴백(`~/.claude/skills/impeccable`)으로 내려가는데, **그 디렉터리는 work-calendar M1 플랜의 DD19 가 지우라고 요구한 폐지된 CLI 설치본이다.** 결과적으로 플랜의 선행 조건을 지킨 실행일수록 mccp 가 `skill-missing` 을 내고 `impeccable_skipped=true` 가 찍혀 `/mccp:pr` 이 차단된다. 이번 실행은 일급 override `MCCP_IMPECCABLE_SKILL=available` 로 정정했다(값이 참임을 SKILL.md 로 확인). 수리는 `probeKeys` 에 `impeccable@impeccable` 을 더하거나, `impeccable@` 접두어 매칭으로 바꾸는 것 — M1 verify.sh 가 이미 그 접두어 방식을 쓴다 · id=mccp-impeccable-probe |
| 2026-08-22 | HIGH | .claude/reviews/plan-review-work-calendar-m1.md | Plan-Codex R1 F0 (재앵커 라운드) · **DEFER_TO_BACKLOG** (arbiter: intent_conflict=none): "5.2e 정지 기록이 `## Findings` 에 `None` 을 적어 L1 의 C3_CREATE_EXISTS 5건을 findings 밖에 남긴다 — 실패한 게이트를 통과 증거로 오독할 수 있다". **부분유효**: 기록 자체는 자신을 통과로 제시하지 않는다(`Verdict: divergent` · `Layers: L1 divergent` · `Halted at 5.2e` · Reason 줄이 C3 위반을 인용). `None` 은 **패널** findings 절이고 그 자리에 "패널이 읽을 수 있는 결과를 내지 않았다(5.2e 정지)"가 함께 적혀 있다. 남는 실질은 L1 위반 5건이 구조화된 findings 목록에 없다는 것이며, 그 5건은 전부 `C3_CREATE_EXISTS` 로 **플랜이 이미 구현·커밋(b51622a)됐다는 사실의 부산물**이지 플랜 결함이 아니다. 조치 후보는 `record.js` 가 L1 위반을 findings 절에 전개하도록 고치는 것(상류 mccp) · id=reanchor-r1-f0 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m1.verify.sh | Plan-Codex R1 F1 (재앵커 라운드) · **DEFER_TO_BACKLOG** (arbiter: intent_conflict=**UI6**, 리뷰어 주장과 일치): "`gate-final` 은 `.m1-gates.log` 에 게이트 이름이 있는지만 보므로, GATE-C 전용 단언(SEED 마커·Components 부재·7절 순서·Brand Commitments·규칙 이름 충돌)이 Task 5 의 사후 편집에도 계속 성립하는지는 재확인하지 않는다. 원장은 게이트가 한 번 돌았음을 증명하지 그 계약이 최종 파일에서 유지됨을 증명하지 않는다." UI6(컴포넌트 스펙 미기재)과 직접 충돌하는 창이다. 제안 수리는 GATE-C 의 캘린더 계약을 재사용 가능한 함수로 뽑아 gate-final 이 편집 후에 다시 부르거나, 원장 항목을 인증 대상 파일 해시에 결속하는 것. **M1 에서 고치지 않는 이유는 id=impl-r1-f2 와 같다** — `verify.sh` 는 플랜 본문 bash 블록과 바이트 비교되므로 게이트 수정이 곧 플랜 수정이고 plan sha256 과 DD20 의 PRD 앵커를 함께 무효화한다. M3 이 플랜을 여는 시점에 묶는다 · id=reanchor-r1-f1 |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m1.verify.sh | Plan-Codex R1 F2 (재앵커 라운드) · **DEFER_TO_BACKLOG** (arbiter: intent_conflict=none): "`STAGE` 가 결정적 경로 `${TMPDIR:-/tmp}/work-calendar-m1-stage` 라 고유 실행 디렉터리·소유자 마커·락이 없다. 동시 실행이나 부분 실패 후 재시도가 같은 경로에서 GATE-B 를 통과한 뒤 서로의 산출물을 덮을 수 있고, 그러면 잘못된 답안지에서 나온 캘린더 문서가 추적 흔적 없이 생긴다 — staging 이 없애려던 바로 그 경합을 고정 이름이 유지한다." 제안 수리는 `mktemp -d` 또는 run-id 별 stage 를 기준표/원장에 기록하고, 소유자 마커가 없는 기존 stage 는 거부하며, 소유한 경로만 지우는 정리를 두는 것 · id=reanchor-r1-f2 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m1.verify.sh | Plan-Codex R1 F3 (재앵커 라운드) · **DEFER_TO_BACKLOG** (arbiter: intent_conflict=**UI2**, 리뷰어는 none 이라 했으나 arbiter 가 UI2 로 판정): "GATE-A 는 `installed_plugins.json` 에서 `installPath` 하나를 골라 레거시 디렉터리를 거부하지만, Task 2·3 은 여전히 대화형 `/impeccable` 명령을 부르지 `$SKILL` 경로를 부르지 않는다. 게이트는 plugin 캐시의 파일을 검사할 뿐 현재 세션의 명령 해석기가 그 스킬을 실행할 것임을 증명하지 못한다 — DD19 의 안전 속성 자체가 '검사한 구현 = 실행되는 구현' 인데 이 코드는 매니페스트 쪽만 본다." UI2(문서의 생성 주체가 impeccable 스킬)와 맞물린다. 제안 수리는 생성을 해석된 `$SKILL` 경로에 결속하거나, 실제 `/impeccable` 경로를 거치는 preflight 가 해석된 버전·경로를 산출물로 내고 gate-a/gate-c 가 그것을 검사하게 하는 것 — 전역 스킬 삭제를 라우팅 증거로 삼지 않는다. **M1 에서 고치지 않는 이유는 id=reanchor-r1-f1 과 같다**(바이트 비교 결속) · id=reanchor-r1-f3 |
| 2026-08-22 | MEDIUM | (상류 mccp) scripts/lib/plan-review/l1-check.js | M2 L1 실행에서 발견 · **상류 검사기 결함**: `CITATION_RE`(l1-check.js:66)의 첫 문자 클래스가 `[A-Za-z0-9_]` 라 **선행 점을 캡처하지 못한다.** 그래서 `.claude/prds/x.prd.md:143` 형태의 `path:line` 인용은 항상 `claude/prds/x.prd.md` 로 잘려 어떤 base 에서도 해결되지 않고 `C6_UNRESOLVED_CITATION` 오탐을 낸다. 이 저장소는 플랜·PRD·리뷰 기록을 전부 `.claude/` 아래 두므로 **저장소 문서를 줄 번호로 인용하는 경로가 구조적으로 전부 막힌다** — 반대로 `.claude/` 아래 파일의 줄 인용이 실제로 깨졌을 때도 같은 코드가 나오므로 진짜 결함과 구분되지 않는다. M2 는 인용을 절 이름 형태로 바꿔 우회했고 그것이 이 저장소의 기존 규약(`PRODUCT.md` 의 절 이름 참조, M1 DD12)과 일치하므로 손해가 없었다. 수리는 첫 문자 클래스에 `.` 을 더하는 것(`[A-Za-z0-9_.]`)이며 상류 변경이라 이 저장소에서 고치지 않는다 · id=m2-l1-citation-dotfile |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | M2 L2 패널 R1 invariant F2·F5 · **DEFER_TO_BACKLOG**(구조적 — M2 범위 밖): "Acceptance 항목 대부분이 사람이 브라우저에서 하네스를 돌려야만 확인되는데, 이 저장소에는 러너도 CI도 커밋 훅도 없어 체크박스를 치는 것을 막을 수단이 없다. 게이트가 닫힌 것처럼 보이면서 열려 있다." **부분 흡수됨** — DD23이 기계화 가능한 축(판정 함수의 **존재**, 베이스라인·하네스 **해시 앵커**)을 Validation 절의 `grep -q` + `shasum -c` 블록으로 실제로 닫았고, F1·F3·F4·F6은 그것으로 해소된다. **남는 잔여는 "사람이 하네스를 실제로 돌렸는가"이며 이 저장소에서 원리적으로 증명 불가능하다** — 하네스가 `test/positioning.smoke.html`을 브라우저에서 열어야 돌기 때문이고, 스크린샷 요구는 위조가 체크박스보다 어렵지 않아 강제가 아니라 의례가 된다. 유일한 실질 수리는 **헤드리스 러너 도입**(Playwright 등으로 스모크 하네스를 CI에서 실행)이며, 데이터 모델 4차 변경과 같은 커밋에 넣으면 두 위험이 곱해지므로 M2에서 하지 않는다. M3 착수 전에 단독으로 다룬다 · id=m2-headless-runner
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | ~~반증(2026-08-22)~~ M2 L2 R6 invariant F1: "`베이스라인 내보내기` 버튼이 `test/positioning.smoke.html`에 없다 — Validation 3번의 `shasum -c`가 의존하는 파일을 만들 수단이 없다" · **반증근거**: 리뷰어가 인용한 Task 0 본문이 곧 "Task 0이 그 버튼을 만든다"이다. 실측 — `test/positioning.smoke.html:101-104`에 버튼 셋뿐인 것은 사실이고, **Task 0이 아직 수행되지 않았기 때문**이다. 플랜 검토 시점에 Task 0의 산출물이 없는 것은 결함이 아니다. M1이 같은 오독을 두 번 받았고(`id=9i-bootstrap`) 같은 근거로 기각됐다. Validation 절이 "승인 시점"과 "구현 완료 시점"으로 갈린 이유가 정확히 이것이며, 앵커 검사는 Task 0 뒤에만 돈다. R6에서 Task 0 머리에 이 기전을 인용문으로 못박았다 · id=m2-r6i-bootstrap |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m2.plan.md | ~~반증(2026-08-22)~~ M2 L2 R6 invariant F2: "plan 해시가 승인 뒤 만들어지는 베이스라인 아티팩트를 구속하지 못하므로 receipt 앵커링이 불완전하다" · **반증근거**: 플랜이 **그 문장을 스스로 두 번 적었고 주장 범위를 명시적으로 좁혔다** — Task 0 항과 Validation 말미가 "앵커가 사는 범위는 Task 0과 Task 8 사이의 무단 변경뿐이고 '승인된 베이스라인으로 시작했는가'는 이 저장소의 수단으로 증명되지 않는다. 그 이상을 주장하지 않는다"로 끝난다. 리뷰어는 플랜의 자기 한계 명시를 결함의 증거로 인용했다. 잡는 척 적는 것이 M1 DD20이 고친 결함이므로 이 지적을 흡수하는 유일한 방법은 그 결함을 다시 만드는 것이다 · id=m2-r6i-anchor |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m2.plan.md | ~~반증(2026-08-22)~~ M2 L2 R6 invariant F3 + test F6(같은 주장): "Validation 절이 승인 시점 검사와 구현 시점 검사를 섞어 게이트처럼 보이게 한다 / Acceptance가 둘을 구분하지 않는다" · **반증근거**: Validation 절은 **이미 `### 승인 시점에 도는 것 (지금)`과 `### 구현 완료 시점에 도는 것`으로 갈려 있고**, 그 앞 머리말이 "3·4번은 플랜 승인 시점에 돌 수 없다… 닫힌 것처럼 보이는 열린 게이트를 만들지 않으려면 시점을 갈라 적어야 한다"로 이유까지 적는다(R4 invariant의 같은 지적을 흡수한 결과다). Acceptance도 `**이 목록의 강제력에 관하여**` 문단이 "기계가 판정하는 것"과 "사람이 브라우저에서 하네스를 돌려야 아는 것"을 나열로 가른다. invariant F3 자신이 "The plan text is honest about this limitation"이라고 인정한 뒤 같은 것을 결함으로 냈다 · id=m2-r6i-timing |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m2.plan.md | ~~반증(2026-08-22)~~ M2 L2 R6 invariant F4: "DD10의 상수 분리가 올바른 구현에 전적으로 의존하며 승인 시점에 이를 강제하는 수단이 없다" · **반증근거**: **모든 플랜에 참인 문장이고 따라서 이 플랜의 결함이 아니다** — 플랜은 정의상 아직 수행되지 않은 지시이므로 "구현이 틀릴 수 있다"는 반증 가능한 주장이 아니다. 리뷰어 자신이 답을 인용했다: v2→v4 연쇄 케이스가 이 함정을 잡고(Task 2 Validate), DD16의 spy가 주 증인이다. 남는 잔여("사람이 하네스를 실제로 돌렸는가")는 DD23이 이미 계산해 두었고 유일한 실질 수리는 헤드리스 러너다(`id=m2-headless-runner`) · id=m2-r6i-dd10 |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22) — 형태 변경으로 근본 처리.** M2 L2 R6 test F1·F2(CRITICAL)·F3·F4·F5 + architect F1, 여섯 건이 **같은 한 가지**다: 요구가 Task Action의 산문 안에 있어 "지시인가 서술인가"가 읽는 사람 판단이 된다. 리터럴로는 다섯 건이 틀렸다 — 리뷰어가 인용한 행이 곧 Task Action 본문이고, `createCalendarGate`의 경우 "이 함수도 이 Task가 만든다"를 인용한 뒤 "만들라고 적혀 있지 않다"고 적었다. **그러나 다섯 번 같은 자리에서 틀렸다면 리뷰어의 문제가 아니다** — R4가 CRITICAL로 잡은 실제 결함(`promoteEventsToV4`의 `deriveEventRange` 호출 누락)이 정확히 그 판단이 어긋나서 생겼다. DD29를 신설하고 Task 0·1·2·3·5의 Action을 번호 매긴 "새로 만드는 것"·"고치는 것" 목록으로 시작하게 바꿨다(내용 무변, 형태만). `grep` 대상을 그 목록과 일대일로 맞추고 `spyOn`을 더했다. architect F1만 진짜 빠짐이었고 DD28이 병목 둘(`persistProjects` 내부 · `replaceEvents` 내부)로 답했다 · id=m2-r6-prose-vs-instruction |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** M2 L2 R7 architect F2·F4·F5 + test F2: "DD29가 Task Action을 번호 목록으로 바꾸라고 해 놓고 **Task 4만 빠뜨렸다** — 하필 DD26의 둘째 호출 자리가 그 산문에 묻혀 있다. 그리고 Task 4는 결과 층위만 검사하고 호출 층위 spy가 없다(Task 2에는 있다)". **유효하고 고쳤다** — Task 4를 DD29 형태로 바꾸고 관문 CRDU 셋에 **이름을 줬다**(`addGate`·`updateGate`·`removeGate`). "편집기가 부른다"는 부르는 자리가 아니다. 셋 다 커밋 직전에 `deriveEventRange()`를 부르고, Task 4 Validate가 셋 각각에 spy 단언을 건다. 약속 함수 목록도 일곱 → 열로 늘렸다(DD29: grep 대상 = Task 목록) · id=m2-r7-task4 |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** M2 L2 R7 architect F1: "DD25의 `kind: null` 중복 금지를 **편집기만** 막고 `createCalendarGate()`는 검사하지 않는다 — 프로그램적 생성이나 가져오기가 우회한다". **유효하고 고쳤다** — 이 마일스톤에는 남의 파일이 `gates`를 들고 들어오는 경로가 실제로 있다(DD28). 막는 자리를 `createCalendarEvent()`로 옮겨, `gates` 조립 시 **`kind === null`인 항목끼리만** `planned` 기준 중복을 접는다. 이름 있는 관문은 접지 않는다(DD25: 같은 날 dev와 review가 서는 것이 담으려는 모양) · id=m2-r7-nullkind |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** M2 L2 R7 test F1·F4 + invariant F3·F5: "등가 케이스의 고정 입력 넷이 Validate에만 있고 기계 검사가 없다 / `deriveEventRange`·`reconcileProjectRefs`의 **호출 자리**는 grep이 보지 않고 spy만 본다". **둘 다 유효하고 고쳤다** — (1) 고정 입력 넷의 값을 Task 5 Action 표로 옮기고 각 줄에 `DD3-FIXTURE` 표식을 요구, Validation 2b가 표식을 센다. (2) **호출 자리 검사 2b를 신설** — 주석 줄을 걷어낸 뒤 `deriveEventRange(` 4회 이상(선언 1+호출 3), `reconcileProjectRefs(` 3회 이상(선언 1+병목 2). 하한 검사이고 자리를 특정하지 못한다는 것을 명시했다 — 특정은 spy가 하고, 이것은 spy가 사람 손을 요구하는 데 반해 지금 돈다 · id=m2-r7-callsite |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** M2 L2 R7 invariant F6·F8: "Validation은 승인/구현 시점을 갈랐는데 **Acceptance는 평평한 체크리스트**라 강제 불가 항목이 게이트처럼 보인다". **유효하고 고쳤다(DD30)** — 내용은 처음부터 맞았다(강제력 문단이 R2 invariant F6 흡수로 들어와 있었다). 틀린 것은 **그 정보가 놓인 자리**이고, 체크박스를 치는 사람은 목록을 읽지 그 뒤 문단을 읽지 않는다. 판정자를 항목 앞으로 옮겼다 — `[기계]` 5 · `[기계+사람]` 1 · `[사람]` 9. **DD29가 Task Action에서 고친 것과 같은 결함이 같은 문서의 다른 절에 남아 있었다** · id=m2-r7-acceptance |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** M2 L2 R7 invariant F9: "DD4의 롤백 안전성(v4 데이터를 v3 코드가 읽는다)을 증명하는 test가 없다". **유효하고 주장을 좁혔다** — 하네스가 실제로 보는 것은 "v3가 읽는 필드 셋이 v4 데이터에 남아 있고 파생값과 일치하는가"이지 "v3 코드를 실제로 돌려 봤는가"가 아니다. 후자는 v3 코드가 교체되므로 이 저장소에서 돌릴 수단이 없다. DD4가 보증하는 범위를 **v3가 의존하는 입력이 온전하다**까지로 적고 그 이상을 주장하지 않는다 · id=m2-r7-rollback |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | ~~반증(2026-08-22)~~ M2 L2 R7 invariant F1·F2·F4·F8 (CRITICAL×3 + HIGH, **넷 다 같은 주장**): "베이스라인이 수동이고 plan 해시가 그것을 앵커하지 못한다 / 브라우저 하네스는 자동화 불가인데 Acceptance에 체크박스로 있다 / 절차 규칙에 기계적 강제가 없다고 적어 놓고 게이트 항목으로 올렸다" · **반증근거**: 넷 다 **플랜이 스스로 적어 둔 한계 명시를 인용**한 것이고, 리뷰어가 인용한 문장이 곧 반증이다 — Task 0("절차 규칙이고 기계적 강제가 없다") · Validation 말미("게이트가 커밋을 막지 못한다" · "앵커가 사는 범위는 Task 0과 Task 8 사이의 무단 변경뿐이고… 그 이상을 주장하지 않는다") · Acceptance("이 저장소에는 그것을 강제할 수단이 없다"). R6에서 같은 주장을 이미 기각했다(`id=m2-r6i-anchor`·`id=m2-r6i-timing`). **잡는 척 적는 것이 M1 DD20이 고친 결함**이므로 이 지적을 흡수하는 유일한 방법은 그 결함을 다시 만드는 것이다. 실제로 고칠 수 있었던 잔여는 **배치 하나**였고 DD30이 그것을 처리했다(`id=m2-r7-acceptance`). 유일한 실질 수리는 헤드리스 러너이며 이 마일스톤 밖이다(`id=m2-headless-runner`) · id=m2-r7i-honesty |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | ~~반증(2026-08-22)~~ M2 L2 R7 invariant F7: "`spyOn` 은 존재만 검사되고 올바른 구현인지는 게이트가 안 본다 — 스텁이 통과한다" · **반증근거**: 리뷰어가 **DD24를 인용한 뒤 DD24가 설명하는 것을 결함으로 냈다.** 세 층위(존재 grep / 호출 spy / 행동 assert)는 각자 잡는 것이 다르고 어느 하나가 다른 것을 대신하지 못한다는 것이 DD24의 내용이며, "빈 스텁은 존재 층위를 통과하지만 행동 층위에서 반드시 죽는다"가 그 절의 문장이다. `undefined`를 돌려주는 `spyOn` 스텁은 `spy.calls` 참조에서 즉시 죽는다. 게이트가 구현 정확성을 보게 만들려면 게이트가 두 번째 판정자가 되어야 하고 그것이 DD11이 거부한 것이다 · id=m2-r7i-spystub |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m2.plan.md | ~~부분반증(2026-08-22)~~ M2 L2 R7 invariant F10 + architect F3: "빈 `gates` 배열·상한 초과·손상 입력의 동작이 명세되지 않았다 / `sanitizeImportedEvents()`의 관문 구조 검증 규칙이 없다" · **반증근거**: 둘 다 **Task 1 산문에 이미 있다** — "통과한 것이 하나도 없으면 `endDate`/`date`에서 관문 하나를 만들어 세운다(빈 관문 집합은 존재할 수 없다)" · "`MAX_GATES_PER_EVENT`를 두고 초과분은 절단한다" · `createCalendarGate`는 "필수값이 없거나 형식이 틀리면 `null` 반환". 가져오기의 관문 검증은 `newtab.js:293` 주석이 답한다 — "addEvent / sanitizeImportedEvents / migrateCalendarToV3가 전부 이 함수(`createCalendarEvent`)를 통과한다". **다만 리뷰어가 이것을 못 찾은 자리가 하필 번호 목록 아래 산문 꼬리**이며, 그것이 DD29·DD30이 다루는 형태 문제의 세 번째 발현이다. 남은 잔여는 "손상 입력의 오류 계약"이고 M2는 기존 관용구(`null` 반환 + 호출부가 거른다)를 그대로 쓴다 · id=m2-r7-edgecases |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** M2 L2 R8 test F1·F2·F3·F4 (**이 라운드의 유일한 blocking 원천**): "DD29를 **코드** 산출물에는 적용했는데 **하네스 케이스** 산출물에는 Task 3·4·6에서 적용하지 않았다 — Task 2는 `runCalendarV4MigrationCases`·`spyOn`을, Task 5는 `runCalendarV4EquivalenceCases`를 Action 목록에 세웠는데 나머지 셋은 Validate 산문에만 두었고, 그러면 Task 7의 'Task 2~6에서 더한 케이스'가 무엇을 가리키는지 다시 판단의 문제가 된다". **정확한 지적이고 고쳤다** — Task 3에 `runCalendarProjectCases`, Task 4에 `runCalendarGateCases`, Task 6에 `runCalendarOnboardingCases`를 Action 목록에 세우고, Task 7이 다섯을 이름으로 열거하며, Validation grep 목록과 Acceptance 개수(셋→여섯)를 함께 맞췄다. **DD29가 잡은 결함 유형의 세 번째 발현이다** — 첫째는 Task Action의 코드 요구(R6), 둘째는 Acceptance의 판정자 표기(R7/DD30), 셋째가 이것이다 · id=m2-r8-testcase-names |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** M2 L2 R8 test F5: "DD26의 세 호출 자리가 여러 Task의 Validate에 흩어져 있어 **어느 케이스가 어느 자리를 보는지가 암묵**이고, 셋 전부를 한 번에 판정하는 케이스도 없다". **유효하고 대응표를 넣었다** — DD26에 `호출 자리 × 만드는 Task × 판정 케이스 × 층위` 표를 두어 셋의 판정자를 각각 지목했다. 한 케이스가 셋을 다 보지 않는다는 사실을 숨기지 않고 적었고, 셋 전체를 훑는 값싼 사전 여과는 Validation 2b의 호출 자리 검사(`deriveEventRange(` 4회 하한)임을 명시했다 · id=m2-r8-dd26-map |
| 2026-08-22 | LOW | (인프라) mccp plan-review workflow | M2 L2 R8 실행에서 관측 · **리뷰 결함 아님**: `invariant` 리뷰어가 StructuredOutput을 부르지 않고 종료해 `results[3] = null`, `coverage 3/4`가 됐다(`parallel[3] failed: subagent completed without calling StructuredOutput (after in-conversation nudge)`). 정족수는 `required 3`이라 3응답으로 판정은 성립했고 게이트는 fail-closed 방향으로 동작했다. 재현되면 상류에 보고할 것 — 이 저장소에서 고칠 것은 없다 · id=m2-r8-invariant-null |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22) — 내가 R7에서 만든 결함이다.** M2 L2 R9 security F3(CRITICAL)+F1(HIGH): "`replaceEvents()`가 `reconcileProjectRefs(nextEvents, this.projects)`를 부르는데 **어느 Task도 `this.projects`를 저장소에서 읽는 경로를 만들지 않는다.** `this.projects`가 비어 있으면 가져온 이벤트의 `projectId`가 전건 `null`로 강등된다 — 저장소에 그 프로젝트가 멀쩡히 있는데도. 참조 무결성을 지키려고 넣은 것이 참조를 지우는 경로가 된다". **정확하고 심각하다.** Task 3에 `loadProjects()`를 세우고 `loadEvents()`(`newtab.js:2062`)의 규율을 그대로 따르게 했다(없음 vs 손상을 가르고, 손상은 `[]`로 바꾸지 않고 `projectsLoadFailed`를 세운다). 그리고 **`reconcileProjectRefs(events, projects, { projectsLoaded })`로 서명을 바꿔 `projectsLoaded`가 거짓이면 강등하지 않고 입력을 그대로 돌려준다** — 목록을 못 읽은 상태의 "목록에 없다"는 참조가 끊겼다는 뜻이 아니라 **모른다는 뜻**이고, 모르는 것을 지우면 재생 불가 데이터가 사라진다. `loadEvents()`가 읽기 실패를 빈 배열로 바꾸지 않고 쓰기를 잠그는 것과 같은 판단이다 · id=m2-r9-projects-load |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** M2 L2 R9 architect F1·F2 + security F4: (a) "기본 관문을 `endDate`/`date`에서 만든다고만 적어 **둘 중 무엇이 이기는지** 정하지 않았다 — `startDate`에서 만들면 DD3의 종단 관문이 `endDate`와 달라져 마감 의미가 바뀐다" → `planned := endDate ?? date`로 못박고 `startDate`는 쓰지 않음을 명시. (b) "`promoteEventsToV4`의 `gates = uniqueDates(...).map(...)`에서 **매핑 함수가 비어 있어 `id`를 누가 만드는지 미정**" → `.map(d => createCalendarGate({...}))`로 적어 순수 경로도 생성 게이트를 지나게 했다(건너뛰면 마이그레이션이 유일하게 검사받지 않는 관문 생성 통로가 된다). (c) "`persistEvents`의 조건부가 **결과로만** 적혀 `set()` 두 번으로 구현해도 문장에는 맞는다" → `payload` 조립 후 `set()` 한 번인 코드 형태를 그대로 박았다 · id=m2-r9-underspec |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | ~~반증(2026-08-22)~~ M2 L2 R9 test F5(CRITICAL) + invariant F5: "플랜이 Acceptance 열다섯 중 아홉을 강제 불가한 **약속**이라고 스스로 적었다 — 강제 없이 커밋할 수 있다" · **반증근거**: 이것은 **DD30이 이번 라운드에 추가한 정직성 자체**다. R7 invariant F6·F8이 "강제력 설명이 목록 뒤 문단에 있어 안 보인다"고 해서 판정자를 항목 앞으로 옮겼더니, 다음 라운드가 **보이게 된 그 사실을 CRITICAL로** 냈다. 고치는 유일한 방법은 표기를 도로 숨기는 것이고 그것이 M1 DD20이 고친 결함이다. **이 패널은 공개를 벌한다** — 같은 잔여를 R6·R7·R9 세 번 냈고 매번 심각도가 올라갔다. 실질 수리는 헤드리스 러너 하나뿐이며 이 마일스톤 밖이다(`id=m2-headless-runner`) · id=m2-r9-disclosure-penalty |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | ~~반증(2026-08-22)~~ M2 L2 R9 test F7 + invariant F3: "`grep`은 빈 스텁을 잡지 못한다" · **반증근거**: DD24가 정확히 그것을 설명하는 절이고 표까지 갖고 있다 — 존재(grep)는 "아예 없는" 경우만 잡고 빈 스텁은 행동 층위(`assert`)에서 죽는다. R7 invariant F7로 이미 기각했다(`id=m2-r7i-spystub`). 게이트가 구현 정확성을 보게 만들려면 게이트가 두 번째 판정자가 되어야 하고 그것이 DD11이 거부한 것이다 · id=m2-r9-stub-again |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | ~~반증(2026-08-22)~~ M2 L2 R9 invariant F1·F2·F6: "베이스라인 앵커 검사가 승인 시점에 못 도는데 기계 게이트로 제시된다 / 두 번째 Validation 블록은 skip predicate다 / Acceptance의 앵커 항목이 `[기계]`인데 파일은 Task 0 뒤에 생긴다" · **반증근거**: 부트스트랩 오독의 **네 번째 반복**이다(R6 F1 `id=m2-r6i-bootstrap`, R7 F1·F2·F4 `id=m2-r7i-honesty`). Validation 절은 `### 승인 시점에 도는 것`과 `### 구현 완료 시점에 도는 것`으로 갈려 있고 머리말이 이유를 적으며, Task 0 머리에는 R6 이후 인용문으로 기전이 박혀 있다. `[기계]` 태그는 **누가 판정하는가**를 뜻하지 **언제 도는가**를 뜻하지 않는다 — 시점은 Validation 절이 정한다 · id=m2-r9-bootstrap-4th |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22) — 내가 R9 수정에서 만든 결함이다. 패널이 20분 만에 잡았다.** M2 L2 R10 architect F1·F2·F3(CRITICAL×3) + security F1·F2(HIGH×2), **다섯 다 같은 것**: "`reconcileProjectRefs` 서명을 `(events, projects, { projectsLoaded })` 로 바꿔 놓고 **호출부 둘은 2인자 그대로**이고, `loadProjects()` 는 `projectsLoadFailed`(실패)를 세우는데 함수는 `projectsLoaded`(성공)를 받는다 — **반대 방향 불리언 둘 사이에 다리가 없다.** 그대로면 안전장치가 영영 발화하지 않고, R9가 막으려던 데이터 소실이 그대로 남는다". **정확하다.** 플래그는 `this.projectsLoadFailed` 하나로 두고(`loadEvents()` 의 `this.loadFailed` 규약 유지), **부정을 호출부에 노출**했다 — `{ projectsLoaded: !this.projectsLoadFailed }`. DD28·Task 3의 호출부 넷을 전부 인자까지 적힌 형태로 맞췄고, 셋째 인자 부재 시 `false` 로 읽어 **강등하지 않는** 쪽을 기본값으로 삼았다(빠뜨린 실수가 곧 데이터 소실이 되지 않게). architect F4(같은 날 `kind:null` 접기 알고리즘 미명세)도 순서 넷으로 못박았다 — 생성 게이트 **뒤**에 접는 이유는 `planned` 가 `makeDateKey()` 왕복 검증을 지난 정규 형태여야 키로 쓸 수 있기 때문이다 · id=m2-r10-signature-bridge |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m2.plan.md | M2 L2 R10 security F3·F4 · **잔여로 보존**: (a) "`persistEvents()` 가 `options.nextProjects` 를 계속 받으므로 `persistProjects()` 를 우회해 직접 부를 수 있고, 그러면 `reconcileProjectRefs` 없이 커밋된다" — 사실이다. 서명을 바꾸지 않는 것은 `opSeq`·`loadFailed`·롤백 규약을 쓰는 기존 호출부의 회귀 위험 때문이며(DD22), 우회를 기계로 막으려면 `persistEvents` 를 비공개로 만들어야 한다. M2의 값으로는 폭발 반경이 크다. (b) "호출 자리 검사(`occ >= 4`)는 세 자리에 골고루 있는지 구분하지 못한다 — 다른 데서 네 번 불러도 통과한다" — 플랜이 이미 "하한 검사이고 자리를 특정하지 못한다"고 적었고 특정은 spy가 한다(DD26 대응표). 둘 다 M3 이후에 다시 본다 · id=m2-r10-residual |
| 2026-08-22 | FAIL | .claude/plans/work-calendar-m2.plan.md | L2 invariant: reviewer returned verdict=fail · 원문 .claude/reviews/plan-review-work-calendar.md · id=7e731624 |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R0 B(codex/gpt-5.4, bundled) B1: PRD가 백업 범위를 두 목소리로 말한다 — Delivery Milestones에는 M2.5 행이 있는데 Risks 완화 칸은 "M2에 백업 경로를 넣고", Open Question은 "M2의 백업 경로가 그 완화책"이라고 여전히 M2를 가리킨다. **A(blind)는 같은 항목을 보고 "PRD가 갱신됐다"로 통과시켰다** — 변경 기록(line 103) 한 줄을 읽고 멈춘 것이고, 두 레인이 갈린 자리다. 조치: Risks·Open Question 둘 다 M2.5로 정정하고 "M2 동안 미완화"를 명시 · id=santa-r0-b1 |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R0 B3: 프로젝트 저장에 이벤트 쪽 봉인(`loadEvents` 실패 시 쓰기 차단, `newtab.js:2062`·`2151`)의 대응물이 없었다. `loadProjects()` 실패로 `this.projects=[]` 인 상태에서 프로젝트 하나를 만들면 `persistProjects([새것])` 이 `calendarProjects` 전체를 그 한 건으로 덮는다. `reconcileProjectRefs` 의 `projectsLoaded` 는 이벤트 `projectId` 강등만 막지 목록 자체를 못 지킨다. 조치: DD27a 신설 + Task 3 item 4 본문을 세 줄 계약으로 · id=santa-r0-b3 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R0 B4: `persistProjects()` 가 `this.projects` 를 언제 갈아 끼우는지 적히지 않았다. 먼저 대입하면 실패 시 메모리가 저장소를 앞서고, 안 하면 새로고침 전까지 옛 목록이 보인다. 조치: DD27b 신설 — `persistEvents()` 반환이 참일 때만 대입 · id=santa-r0-b4 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R0 B5: `snapshot()` 기하 diff 0 을 UI4("새 시각 언어를 만들지 않았다")의 판정으로 쓰고 있었다. Patterns 절이 이미 "의미 회귀를 보지 못한다"고 적어 둔 것과 정면 충돌한다 — 사각형을 안 움직이면서 색·문구·칩을 갈아도 통과한다. 조치: Acceptance 항목을 "레이아웃 무변화"로 좁히고 UI4 육안 대조 항목을 따로 세움 + Risks 완화 칸의 "기계가 경계를 지킨다" 철회 · id=santa-r0-b5 |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R0 A2(opus, blind): Validation 2b 의 한계("횟수만 세고 자리를 특정 못 한다")가 Acceptance 목록과 DD26 본문에만 있고 **블록을 돌리는 사람이 보는 자리에는 없었다.** 통과 메시지가 곧 오해의 자리다. L2 invariant F1·F2·F3 이 같은 축을 세 번 냈던 것이 이것으로 닫힌다. 조치: bash 블록 안에 통과 시 출력되는 한계 고지 8줄 추가 · id=santa-r0-a2 |
| 2026-08-22 | LOW | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R0 B6: `snapshot()` 설명이 부정확했다 — `test/positioning.smoke.js:204` 가 `inlineWidth` 도 직렬화하는데 Patterns 절과 DD15 설명 둘 다 rect·display·classes 만 적었다. 조치: 두 자리 모두 정정 · id=santa-r0-b6 |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R0 자체 발견(리뷰어 아님): (a) Acceptance 마무리 문단의 수 셋이 서로 달랐다 — "열일곱 중 열하나" 다음 문장이 "그 아홉"·"그 절반". (b) Task 3 item 2 에 `를 세운다. 각 항목은…` 으로 시작하는 주어 없는 문장 토막이 앞선 라운드 편집 사고로 남아 있었다. 조치: 둘 다 정정 · id=santa-r0-self |
| 2026-08-22 | REJECTED | .claude/plans/work-calendar-m2.plan.md | ~~반증(2026-08-22)~~ santa R0 A1·A3·A4·A5 넷 다 반증: A1("`projectsLoaded` 의미가 미명세") → DD28 과 Task 3 item 2 가 `{ projectsLoaded: !this.projectsLoadFailed }` 를 축자로 적고 있고 부재 시 `false`(fail-closed)까지 못박았다. A 는 백로그 R10 항목(수정 **전** 상태 기술)을 현재 상태로 읽었다. A3(Task 0 기계 강제 없음) → 루브릭 자신이 "정직하게 선언돼 있으면 PASS"라 했고 R6·R7·R9 에서 세 번 반증됐다. A4(Acceptance `[기계]` 오표기) → 하네스 의존 항목은 전부 `[사람]` 이고 `[기계]` 다섯은 grep·shasum·node --check 로 실제로 셸에서 돈다. A5(Task 4 Action 누락) → R7 에서 이미 고쳐졌고 A 의 증거 자신이 그렇게 적는다 · id=santa-r0-a-rejected |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22, 사용자 결정)** santa R0 B2: 플랜 Task 5가 `rebuildIndex()`에 대해 "파생 범위로 셀을 채우는 현재 동작을 유지한다"고 적었는데 PRD 32행(문제 정의: 월·수에 걸치고 화요일엔 다른 작업)과 96행(M2 결과: 불연속 배치가 그대로 표현된다)이 정반대를 요구한다. **M2의 헤드라인 결과물에서 두 문서가 어긋나 있었다.** 사용자가 "M2가 표현한다"를 골랐다(PRD를 좁히는 대안은 기각). 조치: UI15 신설 · DD31 신설(`gates[].planned`로 버킷, 파생 범위는 인덱스 입력에서 제외) · Summary에 "M2는 어느 셀이 차는가, M3은 찬 셀이 어떻게 보이는가" 경계 · Task 5 Action과 본문 · Files to Change 두 행 · 등가 케이스를 `meaning`(불변)/`occupancy`(의도적 변화) 두 무리로 갈라 단언 넷으로 재작성 · Acceptance 2항목 추가 · id=santa-r0-b2 |
| 2026-08-22 | MEDIUM | .claude/settings.json | **미해결(2026-08-22)** santa R0 운영 발견: `MCCP_SANTA_SEVERITY_GATE=high` 는 유효하지 않은 값이다(허용: `enforce`\|`off`). santa CLI 를 부를 때마다 `[mccp:santa-gate] ... Falling back to default enforce` 경고가 뜬다. 지금은 기본값과 같은 쪽으로 떨어져 판정이 달라지지 않지만, 값을 의도적으로 넣어 둔 것이라면 의도가 반영되지 않고 있다. 사용자 확인 필요 · id=santa-r0-env |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22) — 내가 santa R0 수정에서 만든 결함이다.** santa R1 B1: DD27b가 `const ok = this.persistEvents(...); if (ok) this.projects = next` 를 지시했는데 `persistEvents` 는 `async`(newtab.js:2146)라 `ok` 가 Promise이고 `if` 가 언제나 참이다 — 막으려던 일이 그대로 일어난다. `await` 를 붙여도 `persistEvents` 가 반환 **전에** `this.render()` 를 부르므로(newtab.js:2170) 대입이 렌더보다 늦어 화면이 옛 목록을 그린다. 조치: 대입 자리를 `persistEvents()` 성공 블록의 `this.events = nextEvents` 옆으로 옮기고 `persistProjects` 를 `async` 얇은 껍데기로 되돌림 + spy 단언 항목 신설 · id=santa-r1-b1 |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R1 B2: 봉인 조건을 "값이 배열이 아님"으로만 두어 **부분 손상**이 빠져 있었다. 배열은 맞고 항목 하나가 손상되면 `createCalendarProject()` 가 그것을 버리는데 `projectsLoadFailed` 는 거짓으로 남고, 다음 강등이 `{ projectsLoaded: true }` 로 정상 읽기를 선언하며 돈다. 버려진 프로젝트를 가리키던 이벤트가 전건 무소속이 되고 저장소에는 그 프로젝트가 멀쩡히 있다. R9 security F3("빈 목록으로 강등")의 부분 버전이고 그 답이 이것을 덮지 못했다. 조치: 버린 항목이 하나라도 있으면 봉인 + 개수 콘솔 고지 · id=santa-r1-b2 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22) — 내가 santa R0 수정에서 만든 결함이다.** santa R1 B3: DD27a가 "`false` 를 받으면 `applyStorageNotice()` 로 고지한다"고 적었는데 그 함수 서명은 `{ migrationFailed }` 하나이고 문구도 셋뿐이라(newtab.js:459) 프로젝트 실패를 담을 자리가 없다. 시키는 대로 부르면 사용자는 아무 고지도 못 받는다. 조치: DD27c 신설 — `state` 에 `projectsLoadFailed` 추가 + 문구 하나, Task 3 에 item 4a 로 "이 Task가 고친다" 명시. 일회성 쓰기 실패는 `persistEvents` 의 `showError` 가 이미 덮는다는 갈래도 적음 · id=santa-r1-b3 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R1 B4: `runAll()` 배선 검사가 `grep -q "runCalendarV4EquivalenceCases(collector)"` 한 줄이었는데 **그 문자열이 선언부 자신에 매치되므로 배선을 하나도 안 해도 통과한다.** 게다가 다섯 케이스 중 하나만 봤다. 조치: `sed` 로 runAll 본문 범위를 떠서 다섯 전부를 그 안에서 찾고, 본문 추출 실패 자체도 죽게 함(fail-closed) + 전제(column 0 선언·닫는 괄호)를 주석에 적음 · id=santa-r1-b4 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22) — DD31 변경의 파급이다.** santa R1 B5: 점유를 읽는 자리가 둘인데(그리드는 `rebuildIndex()` 버킷, 패널은 `getEventsForDate()` 범위 조회 — newtab.js:2874 → 2129) DD31이 앞의 것만 바꿨다. 그러면 그리드는 화요일을 비우는데 화요일 칸을 누르면 패널이 그 이벤트를 낸다. 조치: `getEventsForDate` 를 `event.gates.some((g) => g.planned === dateKey)` 로 바꾸는 지시를 Task 5 Action 에 추가 + 그리드/패널 일치 단언 항목 신설 · id=santa-r1-b5 |
| 2026-08-22 | LOW | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R1 자체 발견: Validation 2 주석이 "newtab.js 함수 일곱"이라 적었는데 바로 아래 루프는 열하나를 돈다(Acceptance 쪽은 처음부터 열하나로 맞아 있었다). 겸해서 Acceptance 마무리 문단의 어림수("절반"·"일곱 중 다섯")를 정확한 수로 바꾸고, 어림수를 쓰지 않는다는 규칙 자체를 적었다 — 항목이 늘 때마다 조용히 틀려지는 것이 R0 사고의 원인이었다 · id=santa-r1-self |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22) — 내가 santa R1 수정에서 남긴 구멍이다.** santa R2 B1: R1에서 봉인 조건을 "createCalendarProject()가 null을 돌려준 항목이 있으면"으로 넓혔는데, **그 함수는 id가 없으면 null이 아니라 crypto.randomUUID()로 새 id를 만들어 준다(DD20).** 그래서 id를 잃고 name만 남은 행은 봉인이 걸려야 하는 손상인데 새 id를 달고 되살아나고 projectsLoadFailed는 거짓으로 남는다. 이벤트들은 옛 id를 들고 있으므로 다음 강등에서 전건 무소속이 된다. 조치: loadProjects()가 생성 게이트에 넘기기 전에 `typeof row.id === 'string' && row.id`를 직접 본다 — id 자동 생성은 만들 때의 규칙이지 읽을 때의 규칙이 아니다. 가져오기 경로에는 적용하지 않는다 · id=santa-r2-b1 |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R2 B2: R9 architect F1은 "종단 관문을 **어느 날짜**에서 만드는가"에 답한 것인데 플랜이 "관문을 **몇 개** 만드는가"까지 답한 것처럼 startDate를 통째로 버렸다. 그러면 승격은 uniqueDates([startDate, endDate])로 관문 둘, 가져오기는 endDate 하나 — **같은 입력에 다른 답**이다. v4 이전에 내보낸 v3 JSON을 나중에 가져오면 5일짜리가 하루로 접히고 deriveEventRange가 startDate := min(planned) = endDate로 다시 파생해 **원래 시작일이 복구 불가능하게 사라진다.** 오류는 안 난다. DD31 이후엔 점유까지 하루로 준다. 조치: gates 부재 + 레거시 범위 필드 존재 시 `uniqueDates([startDate, endDate ?? date])` — 종단 관문 출처는 R9 그대로 · id=santa-r2-b2 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R2 B3: Task 0이 구현 **전** 하네스와 베이스라인을 해싱해 baseline.sha256을 만드는데, Task 2~7이 test/positioning.smoke.js를 고치고 Task 7이 재베이스라인한다. 그런데 Validation 3과 Acceptance가 **그 파일로 끝 상태를 검사**하므로 설계상 반드시 깨진다 — 자기 종료 조건을 만족할 수 없는 플랜이었다. 조치: 앵커를 둘로 가름. baseline.sha256은 Task 0이 그 자리에서 한 번 확인하는 **기록**, rebaseline.sha256은 Task 7이 재베이스라인 직후 뜨는 **끝 상태 게이트**. 둘 다 커밋해 차이가 감사 기록이 되게 함(뒤엣것만 두면 R0 invariant F6이 막으려던 상태로 되돌아간다) · id=santa-r2-b3 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R2 B4: 존재 검사의 decl() 정규식이 선언 접두어 그룹에 `?`를 달아 **선택적**으로 두어, `await runCalendarProjectCases(collector);` 라는 **호출 한 줄이 "선언이 있다"를 통과시킨다**(실측 확인). 검사가 있는 척하면서 아무것도 묻지 않았다. `?`는 클래스 메서드 축약을 잡으려던 것이므로 별도 가지로 분리 — (1) 줄머리 키워드 선언, (2) 들여쓴 메서드 축약이되 같은 줄에서 `) {`로 닫히는 것. 실제 선언 12개는 전부 잡고 호출 3형태·미존재 함수 4개는 전부 거부하는 것을 실측했다 · id=santa-r2-b4 |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R3 B1: Task 0이 "만드는 것"에 `test/positioning.smoke.html` 하나만 적고 "고치는 것: 없다"라고 했는데, **확장 오리진이라 인라인 스크립트가 CSP에 막히고 기존 버튼 리스너는 전부 `test/positioning.smoke.js:1119` 부근에 붙어 있다.** 그대로 따르면 버튼만 생기고 눌러도 파일이 안 받아져 `work-calendar-m2.baseline.json`이 만들어지지 않고, 그것을 요구하는 Task 7·Validation 3의 앵커 사슬이 시작조차 못 한다. 조치: 고치는 것에 smoke.js(리스너) 명시 + 해시를 그 변경 뒤에 뜬다는 순서 명시 + 버튼이 실제로 파일을 받아 내는지 보는 Acceptance 항목 · id=santa-r3-b1 |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22) — DD31이 만든 자기모순이다.** santa R3 B2: Task 7의 허용 diff 목록이 (a)(b)(c) 셋뿐이고 "기존 키의 기존 필드 **값** 변화"를 명시적으로 금지했는데, **DD31을 옳게 구현하면 정확히 그 금지된 변화가 난다**(`range/01-month-boundary`의 chip 날짜들). 플랜이 자기 구현을 자기 검증으로 떨어뜨리고 있었다. "값 변화 허용"으로 넓히면 R0 invariant F4의 열거 취지가 통째로 사라지므로 조치는 (d)를 **케이스 이름과 기대값 세 개까지 열거**하는 것으로 했고, 재베이스라인 **전에** 사람이 그 셋을 눈으로 맞추는 항목을 세웠다 — 안 맞춰 보고 누르면 (d)가 "모든 값 변화 허용"과 같아진다 · id=santa-r3-b2 |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22) — 내가 santa R2 수정에서 남긴 구멍이다.** santa R3 B3: rebaseline 앵커를 새로 뒀지만 **재베이스라인 뒤 `베이스라인 내보내기`를 다시 누르라는 지시가 없었다.** 재베이스라인은 `chrome.storage.local`의 `__smokeBaseline`을 갱신하는 동작이고 저장소 루트의 `work-calendar-m2.baseline.json`은 Task 0이 한 번 받아 둔 별개 파일이라, 재내보내기 없이 해시를 뜨면 "옛 파일이 안 바뀌었다"만 증명하고 커밋된 파일이 실제 비교 기준과 같은지는 증명하지 않는다. 조치: (1) 재베이스라인 → (2) 재내보내기·덮어쓰기 → (3) 해시 생성 순서를 못박음 + 순서 준수를 보는 Acceptance 항목 · id=santa-r3-b3 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22) — 내가 santa R2 수정에서 만든 모순이다.** santa R3 B4: 같은 입력에 두 규칙이 걸렸다 — 앞 문장은 "통과한 것이 하나도 없으면 관문 하나를 만들어 세운다", 뒤 문장은 "`gates`가 **없고** 레거시 범위 필드가 있으면". `gates`가 있었지만 전부 검증에서 떨어진 입력에 대해 두 구현이 갈린다. 조치: 조건을 "유효한 관문이 하나도 남지 않았다" 하나로 통일하고(부재와 전멸을 같게 다룬다) 떨어진 개수를 콘솔 고지하도록 함 · id=santa-r3-b4 |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22) — 내가 santa R3 수정에서 고른 답이 틀렸다.** santa R4 B1: R3 B4가 "두 문장이 같은 결정에 다른 조건을 건다"를 잡았고 나는 그것을 "유효한 관문이 하나도 남지 않았다" **한쪽으로 통일**했다. 그런데 그 답은 `gates`가 배열로 있고 전부 손상된 v4 이벤트까지 범위에서 재구성하게 만들어, 각 관문의 kind·actual·status가 **조용히 사라진 채** 양 끝 익명 관문 둘로 덮이고 오류 없이 다음 저장에 영구 커밋된다. 조치: 조건을 `gates` **키의 유무**로 갈라 세 갈래로 적음 — 부재+레거시 범위는 재구성(잃을 것 없음), 배열인데 전멸은 **null 반환**(적재는 봉인·가져오기는 배치 거절, 둘 다 기존 규약), 범위마저 없으면 null · id=santa-r4-b1 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R4 B2: 등가 케이스의 `occupancy` 투영이 "셀별 칩 개수 · 버킷 키 목록" **둘**을 담는다고 적어 놓고 단언은 버킷 키만 봤다. `rebuildIndex()`를 옳게 고쳐도 `createChips()`가 옛 범위로 그리거나 중복해 그리면 단언 넷이 전부 통과하고 **사람이 보는 그리드만 틀린 채 남는다.** 인덱스가 맞다는 것과 화면이 맞다는 것은 다른 주장이다. 조치: 칩 개수를 인덱스에서 유도한 값과 맞대는 단언 3b 추가 + Acceptance 항목 · id=santa-r4-b2 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R4 B3: Task 6이 온보딩을 "프로젝트가 하나도 없을 때"에만 걸었는데, Task 3은 읽기 실패를 `projects = []` + `projectsLoadFailed = true`로 표현한다. 그래서 **프로젝트가 멀쩡히 있는데 못 읽은 상태**가 첫 실행으로 오인되고, 안내를 따라 만들려 하면 DD27a 봉인에 막혀 실패하거나 건너뛰기로 **한 번뿐인 온보딩 플래그를 첫 실행도 아닌데 태운다.** 조치: 조건을 `projects.length === 0 && !projectsLoadFailed`로 하고 실패 상태에서는 DD27c 상시 고지가 화면을 맡게 함 + Acceptance 항목 · id=santa-r4-b3 |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R4 B4: Files to Change 가 sha256 파일 둘은 적고 정작 그 둘이 해싱하는 `work-calendar-m2.baseline.json` 을 빠뜨렸다. 목록만 보고 구현하면 그 파일이 커밋되지 않아 `shasum -c` 가 없는 파일에서 죽는다. 조치: 표에 행 추가(Task 0 생성 · Task 7 덮어쓰기 명시) · id=santa-r4-b4 |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22) — 내가 santa R4 수정에서 코드를 잘못 읽었다. 이 루프 최대 발견.** santa R5 B1: R4에서 "손상된 v4 gates는 null 반환, 적재는 봉인하고 가져오기는 배치 거절 — 호출부가 이미 갈라 두었다"고 적었는데 **둘 다 반대로 한다.** `sanitizeImportedEvents`(newtab.js:509)는 `if (!event) return;`으로 그 항목만 건너뛰고(전체 거절은 항목 수·글자 수 상한에만 걸려 있다), `loadEvents`(newtab.js:2077-2078)는 `.filter()`로 버린 뒤 곧바로 `loadFailed = false`로 봉인을 푼다. 그래서 R4 수정이 **재구성을 막으려다 삭제를 만들었다** — 손상 이벤트가 조용히 사라지고 봉인 풀린 채 열리며 다음 편집이 짧아진 배열을 영구 커밋한다. 조치: 두 호출부를 Task 1의 "고치는 것"에 넣고(적재는 버린 개수 세어 봉인 유지 + 고지, 가져오기는 배치 거절) 기존 동작 변경임과 그 대가·해제 경로(isFullReplacement)를 명시 + Acceptance 2항목 · id=santa-r5-b1 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R5 B2: Task 2의 시동 spy와 Task 5의 "마이그레이션 전 투영 A"가 **하네스가 초기화 이전을 볼 수 있다는 전제** 위에 있었다. `loadApp()`은 `__newTabApp`·`settingsManager`·`calendarManager` 초기화를 다 기다린 뒤 반환하고 시동 마이그레이션은 `Application.initialize()`(newtab.js:4115-4116) 안에서 그 전에 끝난다. spy는 0을 세고 "전"은 이미 "후"다. 조치: 증명 대상을 바꿈 — 변환은 순수 함수 직접 호출로 단언하고, 투영 A/B는 **앱을 두 번 올려서** 뜬다. "v3가 시동에서 돌았다"는 이 하네스로 증명 불가임을 숨기지 않고 적고 천장을 백로그 m2-headless-runner에 연결 · id=santa-r5-b2 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22) — 내가 santa R4 수정에서 만든 성립 불가 단언이다.** santa R5 B4: R4에서 넣은 단언 3b가 `bucketKeysToCounts(bucketKeys)`로 칩 개수를 유도하려 했는데, `eventsByDate`는 `Map`이고 **날짜당 키가 하나뿐**이라(newtab.js:2098·2114) 그 유도값은 언제나 1이다. 고정 입력이 `today`에 겹치도록 설계돼 있으므로 **옳은 구현이 죽는다.** 조치: `occupancy`를 셋으로 갈라 이름으로 못박고(bucketKeys=날짜 키 목록 · bucketSizesByDate=버킷 배열 길이 · chipCountsByDate=실제 칩 수), 3b를 `chipCounts === min(bucketSizes, MAX_CHIPS_PER_CELL)`로 다시 씀(createChips의 상한 절단·+N 반영) · id=santa-r5-b4 |
| 2026-08-22 | MEDIUM | .claude/plans/work-calendar-m2.plan.md | **부분 흡수(2026-08-22)** santa R5 B3: Validation 블록이 POSIX sh/bash 문법인데 플랜이 셸을 적지 않았다. **"이 환경에서 쓸 수 없다"는 반증된다** — Git Bash 가 깔려 있고 이 세션이 그것으로 전 구간을 돌렸다. 그러나 셸을 안 적으면 "기계 게이트가 있다"고 적어 놓고 실행자가 돌릴 방법을 모르는 상태가 되어 없는 게이트와 구별되지 않는다는 지적은 옳다. 조치: Validation 머리에 0b 블록 신설 — Git Bash 사용 명시 + 가용성 확인 한 줄 + shasum/sha256sum 대체 · id=santa-r5-b3 |
| 2026-08-22 | LOW | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22)** santa R5 리뷰어 A 제안: `reconcileProjectRefs()` 진입부에 `typeof projectsLoaded !== 'boolean'` 이면 던지는 런타임 단언 추가. fail-closed 기본값은 빠뜨려도 안전하지만 **빠뜨린 사실을 영영 숨기고**, R1 B2·R2 B1 이 두 번 다친 자리가 정확히 그 모양이었다(서명은 바뀌고 호출부가 안 따라온 것을 아무도 알려 주지 않았다). 다른 방어선이 전부 문서 규약이므로 여기 하나는 실행 시점에 세운다 + Acceptance 항목 · id=santa-r5-a-assert |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22) — 내가 santa R5 에서 고친 답도 틀렸다.** santa R6 B1: R5에서 "앱을 두 번 올려 투영 A/B를 뜬다"로 고쳤는데, **Task 2가 migrateCalendarToV4()를 Application.initialize()에 잇는 순간 첫 번째 올림도 이미 v4다.** 두 투영이 똑같이 틀린 채 맞아떨어져 v4 마감 판정 버그가 통과한다. "마이그레이션 이전 렌더"는 Task 2 이후 도달 불가능한 상태라 렌더로 뜨려는 어떤 설계도 같은 벽에 부딪힌다. 조치: 투영 A를 **v3 규칙(getEventDueState가 endDate만 읽는다, newtab.js:2629-2631)을 하네스에 다섯 줄 참조 구현으로 적어 계산한 기준값**으로 바꿈. 단언 1이 "A 렌더 == B 렌더"에서 "B == v3 규칙의 기대값"이 된다 — 타이밍 무관, 두 쪽이 함께 틀릴 수 없다. 참조 구현이 다섯 줄을 넘으면 두 번째 구현이고 DD11 위반이라는 상한도 적음 · id=santa-r6-b1 |
| 2026-08-22 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22) — 내가 santa R5 수정에서 만든 모순이다.** santa R6 B2: DD28에 "셋째 인자는 부재 시 false로 읽는다"와 R5에서 더한 "typeof projectsLoaded !== 'boolean' 이면 던진다"가 **같은 상황에 두 계약**을 걸고 있었다. Task 3 item 5는 전자, Acceptance는 후자를 요구해 호출부 하나가 갱신에서 빠졌을 때 구현자마다 다른 결과가 난다 — 한쪽은 조용히 통과. 조치: **던지는 쪽으로 통일**하고 "부재 시 false" 조항 철회 · id=santa-r6-b2 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22) — 내가 santa R5 수정에서 한쪽만 고쳤다.** santa R6 B3: Task 5의 단언 3b는 bucketSizesByDate 기준으로 고쳤는데 **Acceptance 항목은 여전히 "bucketKeys에서 유도한 값"이라고 적혀 있었다.** 날짜당 키가 하나뿐이라 같은 날 이벤트가 둘 이상이거나 상한을 넘는 날에서 옳은 구현이 죽고, 같은 날 칩을 하나로 뭉개는 틀린 구현이 통과한다. 조치: Acceptance 를 `chipCounts === min(bucketSizes, MAX_CHIPS_PER_CELL)` 로 맞춤 · id=santa-r6-b3 |
| 2026-08-22 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수(2026-08-22) — 내가 santa R5 수정에서 한쪽만 고쳤다.** santa R6 B4: Task 2에 "이 하네스로는 v3가 시동에서 돌았다를 증명할 수 없다"고 적어 놓고 **Acceptance는 "340px 캐시 정리로 v3의 실행이 단언됐다"고 말하고 있었다.** v4가 같은 정리를 직접 구현하면 v3를 건너뛰고도 통과한다(R2 invariant F5가 이미 지적한 구멍). 조치: Acceptance 문구를 "정황 증인이지 시동 실행의 증명이 아니다"로 고치고 증명되는 범위를 명시 · id=santa-r6-b4 |
| 2026-08-22 | REJECTED | .claude/plans/work-calendar-m2.plan.md | ~~반증(2026-08-22)~~ santa R6 **리뷰어 A(blind)의 7건 전부 반증**. 근거로 든 자리를 하나씩 열어 대조한 결과 A1(Task 5 Action에 getEventsForDate 없음)→652행에 있음, A3(uniqueDates 통일이 본문에 없음)→446-448행에 있음, A4(loadEvents 봉인이 본문에 없음)→422·441행에 있음, A7(Task 0이 "고치는 것 없다")→396행이 smoke.js를 고친다고 적음, A5(Validation 3이 baseline.sha256 검사)→rebaseline.sha256을 검사함, A6(블록이 "passed" 출력)→[주의] 8줄 한계 고지를 출력함, A2(UI4가 PRD 24행)→PRD 24행은 메타조사 인용줄이고 UI4는 플랜 24행이며 UI15가 사용자 결정으로 정리함. **원인은 리뷰어가 아니라 범위 설계다** — 내가 60KB 이력 원장(backlog)을 리뷰 범위에 넣었고, 그것은 결함의 **수정 전 상태** 기술이라 리뷰어가 현재 상태로 읽었다. 금지 문구를 넣었는데도 발생했고 라운드마다 원장이 길어져 유혹이 커졌다. 조치: 다음 라운드부터 backlog 를 범위에서 제외 — 이력 원장은 리뷰 대상이 아니라 출처 기록이다 · id=santa-r6-a-rejected |
| 2026-08-23 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수** santa R7 B1: `gates` 키만 잃은 v4 행과 정상 v3 행은 **모양이 같아서** `createCalendarEvent()` 혼자 구별하지 못하는데, 앞선 판은 둘 다 레거시 범위에서 재구성하게 두어 `kind`·`actual`·`status`를 통째로 버렸다. 조치: `createCalendarEvent(input, { allowLegacyGateSynthesis })` 기본값 `false` — `promoteEventsToV4`만 `true`, `loadEvents`는 `false`(v4 저장소에서 gates 없는 행은 손상). 가져오기는 판단 근거가 없어서(내보내기가 `JSON.stringify(getEvents())` 맨 배열, newtab.js:4050) **내보내기에 `{version:4, events:[]}` 봉투를 씌운다** — 맨 배열은 v3 레거시로 `true`, 봉투는 `false`, version 없는 객체는 거절. 옛 파일 하위 호환 유지. 봉투는 M2.5 복구가 "어느 스키마인가"를 물을 때도 답이 된다 · id=santa-r7-b1 |
| 2026-08-23 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수** santa R7 B4 — **이 루프에서 나온 가장 좋은 도메인 지적이다.** `dropped` 관문이 파생 범위·점유에 남으면서 `getEventDueState()`가 종단 관문을 읽으면, 사용자가 "안 하기로" 표시한 관문의 날짜가 지날 때 그 작업이 **지연으로 보고된다.** 범위를 줄인 행위가 늦은 것으로 뒤집힌다 — PRD가 M2에 요구한 "조기·지연·범위축소가 구분되어 남는다"와 정면으로 어긋나고, 구분이 저장 필드에만 있고 판정에 없으면 구분한 것이 아니다. 조치: DD5a 신설 — 파생 범위와 점유는 `dropped`를 포함하되(계획은 남는다) **마감 판정은 제외**한다. 종단 관문은 `status !== 'dropped'` 중 가장 늦은 것이고 살아 있는 관문이 없으면 `''`. `done` 파생도 같은 규칙(남은 관문이 전부 done이거나 dropped면 완료) · id=santa-r7-b4 |
| 2026-08-23 | HIGH | .claude/plans/work-calendar-m2.plan.md | **흡수 — 내가 santa R6 수정에서 한쪽만 고쳤다.** santa R7 B2: R6에서 DD28의 "부재 시 false" 조항은 철회했는데 **Task 3 item 5 에 같은 문장이 그대로 남아 있었다.** DD28·Acceptance는 던지라 하고 Task 3은 false로 읽으라 해서 구현자마다 다른 결과가 난다. 조치: Task 3 자리도 던지는 쪽으로 통일 · id=santa-r7-b2 |
| 2026-08-23 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | **흡수 — 내가 santa R5 수정에서 한쪽만 고쳤다.** santa R7 B3: R5에서 "하네스는 초기화 이전을 볼 수 없다"는 설명을 아래 문단에 적었으면서 **바로 위 Validate 의 spy 지시("연쇄 전에 promoteEventsToV3를 래퍼로 감싸고 v3Calls >= 1을 단언한다, 이쪽이 주 증인")를 지우지 않아** 같은 Task 안에서 서로를 부정하고 있었다. `loadApp()`이 초기화 뒤 반환하므로 그 spy 는 언제나 0을 센다. 조치: 주 증인을 **순수 함수 직접 호출**로 바꾸고 DD19 단일 출처는 "직접 부른 결과 == migrateCalendarToV3 태운 결과"로 증명 · id=santa-r7-b3 |
| 2026-08-23 | STRUCTURAL | .claude/plans/work-calendar-m2.plan.md | **M2 플랜 분할(사용자 지시).** santa 8라운드가 캡에서 divergent 종료한 뒤 원인을 결합으로 진단하고 PRD 기준으로 갈랐다 — M2 결과문의 동사 둘(**담는다**/**표현된다**)이 이음매다. 전반부 `work-calendar-m2.plan.md`(스키마·마이그레이션·프로젝트·관문·계획대비실제·마감 의미), 후반부 `work-calendar-m2b.plan.md`(점유 `rebuildIndex`·`getEventsForDate`·렌더 표면·온보딩). **이 분할이 구조적으로 없앤 결함 넷** — (1) 등가 케이스의 meaning/occupancy 투영 분리(전반부는 점유가 안 바뀌므로 예외 없는 단언 하나), (2) Task 7 허용 diff (d)의 자기모순(전반부는 값 변화가 전부 회귀, 후반부만 (d)를 갖는다), (3) 칩 개수 단언의 bucketKeys 유도 문제(후반부 한 곳에만 존재), (4) 투영 A 도달 불가(전반부는 v3 규칙 참조 구현, 후반부는 점유 열거). DD31·DD32·DD13은 후반부로, Task 6은 이사 간 자리로 표기. PRD M2 행은 두 플랜이 모두 끝나야 complete — UI8·UI15가 "M2에 포함/표현된다"라 말하므로 그 M2는 둘의 합이다 · id=m2-plan-split |
| 2026-08-23 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | L2 architect: Core data integrity functions required by the plan's design decisions do not exist in the codebase. · 원문 .claude/reviews/plan-review-work-calendar.md · id=70197742 |
| 2026-08-23 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | L2 architect: The plan's own validation gate will fail because it explicitly checks for non-existent functions. · 원문 .claude/reviews/plan-review-work-calendar.md · id=725ffd74 |
| 2026-08-23 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | L2 architect: Missing boundary control for referential integrity. Plan claims `reconcileProjectRefs` is the single source of truth for protecting against orphaned project references. · 원문 .claude/reviews/plan-review-work-calendar.md · id=8359d960 |
| 2026-08-23 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | L2 architect: Missing single point of control for maintaining derived field consistency. Plan requires `deriveEventRange()` to be called from five specific call sites after any gate modification. · 원문 .claude/reviews/plan-review-work-calendar.md · id=0653bb24 |
| 2026-08-23 | HIGH | .claude/plans/work-calendar-m2.plan.md | L2 architect: Plan makes a false claim about Task 3 creating all required functions when the list includes functions that are proposed to be created elsewhere. · 원문 .claude/reviews/plan-review-work-calendar.md · id=296919b5 |
| 2026-08-23 | FAIL | .claude/plans/work-calendar-m2.plan.md | L2 architect: reviewer returned verdict=fail · 원문 .claude/reviews/plan-review-work-calendar.md · id=84d301f3 |
| 2026-08-23 | HIGH | .claude/plans/work-calendar-m2.plan.md | L2 security: Plan specifies critical parameter-passing requirement for `applyStorageNotice()` that is NOT validated by shell script · 원문 .claude/reviews/plan-review-work-calendar.md · id=22662cf0 |
| 2026-08-23 | FAIL | .claude/plans/work-calendar-m2.plan.md | L2 security: reviewer returned verdict=fail · 원문 .claude/reviews/plan-review-work-calendar.md · id=211e7342 |
| 2026-08-23 | HIGH | .claude/plans/work-calendar-m2.plan.md | L2 test: DD3 (due state comes only from terminal gate) can be properly tested with the fixture design · 원문 .claude/reviews/plan-review-work-calendar.md · id=9db21b56 |
| 2026-08-23 | FAIL | .claude/plans/work-calendar-m2.plan.md | L2 test: reviewer returned verdict=fail · 원문 .claude/reviews/plan-review-work-calendar.md · id=016be038 |
| 2026-08-23 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | L2 invariant: The baseline file hash validation (Validation 3, line 1097) can pass even when the re-export step is omitted. Lines 904-906 explicitly state: 'If re-export is skipped, the anchor binds … · 원문 .claude/reviews/plan-review-work-calendar.md · id=f1855daf |
| 2026-08-23 | HIGH | .claude/plans/work-calendar-m2.plan.md | L2 invariant: M2 milestone is split across two sub-plans (work-calendar-m2.plan.md and work-calendar-m2b.plan.md) with explicit requirement that both must complete for M2 to be marked complete, but n… · 원문 .claude/reviews/plan-review-work-calendar.md · id=d59529b8 |
| 2026-08-23 | HIGH | .claude/plans/work-calendar-m2.plan.md | L2 invariant: Validation 3's critical baseline hash gate (line 1097) depends on manual browser operations in Task 0 that cannot be mechanically verified. The plan explicitly admits (line 1115): '게이트가… · 원문 .claude/reviews/plan-review-work-calendar.md · id=6ce16f12 |
| 2026-08-23 | FAIL | .claude/plans/work-calendar-m2.plan.md | L2 invariant: reviewer returned verdict=fail · 원문 .claude/reviews/plan-review-work-calendar.md · id=296f2353 |
| 2026-08-23 | HIGH | .claude/plans/work-calendar-m2.plan.md | L2 test: Plan claims 'migration doesn't change screen' (line 14: 파생 startDate·endDate를 그대로 읽으므로, 마이그레이션 전후로 사용자가 보는 것이 **같아야 한다**) · 원문 .claude/reviews/plan-review-work-calendar-m2.md · id=bec4624a |
| 2026-08-23 | FAIL | .claude/plans/work-calendar-m2.plan.md | L2 test: reviewer returned verdict=fail · 원문 .claude/reviews/plan-review-work-calendar-m2.md · id=b21ead71 |
| 2026-08-23 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | L2 invariant: Acceptance gate split between machine-enforced and human-only checks erodes fail-closed principle. 33 of 41 acceptance items are [사람] (human manual) with admitted inability to enforce. · 원문 .claude/reviews/plan-review-work-calendar-m2.md · id=18d20823 |
| 2026-08-23 | HIGH | .claude/plans/work-calendar-m2.plan.md | L2 invariant: Screen equivalence claim cannot be verified by machine. Plan claims screen diff must be 0, but verification is human-only [사람] with snapshot() admittedly unable to capture meaning/state… · 원문 .claude/reviews/plan-review-work-calendar-m2.md · id=a2806ebf |
| 2026-08-23 | HIGH | .claude/plans/work-calendar-m2.plan.md | L2 invariant: Receipt anchoring fails for baseline. Plan hash cannot constrain baseline created post-approval, yet treats rebaseline check as sufficient verification of correctness. · 원문 .claude/reviews/plan-review-work-calendar-m2.md · id=8a8c5da1 |
| 2026-08-23 | HIGH | .claude/plans/work-calendar-m2.plan.md | L2 invariant: DD26 (deriveEventRange call site verification) cannot detect wrong position. Machine validation checks existence of call, not correctness of location (must be before return). · 원문 .claude/reviews/plan-review-work-calendar-m2.md · id=6e58e5e0 |
| 2026-08-23 | HIGH | .claude/plans/work-calendar-m2.plan.md | L2 invariant: v3 migration execution cannot be verified. Plan's 340px cache witness is circumstantial; v4 could implement same cleanup directly and bypass v3 entirely, yet gate treats test passage as… · 원문 .claude/reviews/plan-review-work-calendar-m2.md · id=c33a62ac |
| 2026-08-23 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | L2 invariant: Machine gate proves only structural existence, not correctness. Plan acknowledges that passing all [기계] items does not prove implementation is correct. · 원문 .claude/reviews/plan-review-work-calendar-m2.md · id=0e27ba38 |
| 2026-08-23 | FAIL | .claude/plans/work-calendar-m2.plan.md | L2 invariant: reviewer returned verdict=fail · 원문 .claude/reviews/plan-review-work-calendar-m2.md · id=4003378d |

| 2026-08-23 | MEDIUM | .claude/plans/work-calendar-m2.plan.md | santa R0 자체 발견(리뷰어 아님): Task 5 는 등가 단언을 **셋**으로 적는데(`meaning` 불변 · `occupancy` 불변 · DD25 동점 면제) Acceptance 는 여전히 "단언 **둘**이 통과한다"로 적는다. 세는 수가 두 자리에서 다르다. santa R0 B1 수정이 `Files to Change` 를 "단언 셋"으로 맞췄으므로 남은 어긋남은 Acceptance 한 자리다 — blocking 으로 지목되지 않아 이번 라운드에서 고치지 않았다(드라이브바이 금지) |
| 2026-08-23 | MEDIUM | .claude/plans/work-calendar-m2.plan.md | santa R0 리뷰어 B 제안: `spyOn(frameWindow, name)` 의 서명이 Acceptance 에서는 `storage.set`·`render` 까지 감싸는 데 쓰이므로, `spyOn(target, name)` 으로 일반화하거나 적용 범위를 명시하는 편이 안전하다 |
| 2026-08-23 | LOW | .claude/plans/work-calendar-m2b.plan.md | santa R0 리뷰어 B 제안: 앵커 파일 표기가 두 형태로 섞여 있다 — 본문은 `work-calendar-m2b.baseline.sha256`/`…rebaseline.sha256`, Acceptance 는 `m2b-baseline.sha256` 식 축약명 |
| 2026-08-23 | LOW | .claude/plans/work-calendar-m2.plan.md | santa R0 리뷰어 B 제안: Summary 의 "그래서 이 플랜에서 화면은 달라지지 않는다"가 같은 플랜이 지시하는 모달 관문 편집기·프로젝트 선택기 추가와 충돌해 읽힌다. "메인 그리드의 점유·렌더는 달라지지 않는다"처럼 범위를 좁히는 편이 낫다 |
| 2026-08-23 | MEDIUM | .claude/plans/work-calendar-m2.plan.md | santa R0 커버리지 한계(판정 아님): 리뷰어 A(blind/opus)가 m2 플랜 Task 4~8 을 토큰 한계로 완독하지 못했다고 스스로 고지했다(207KB). 그 구간은 리뷰어 B(bundled/gpt-5.4)만 보았으므로 **그 구간에 한해 이 라운드는 이중 리뷰가 아니다.** 플랜 크기를 줄이거나 구간을 갈라 심사하지 않는 한 다음 라운드도 같은 한계를 갖는다 |
| 2026-08-23 | LOW | .claude/plans/work-calendar-m2b.plan.md | santa R1 리뷰어 B 제안(재발): Acceptance 의 `m2b-baseline.sha256`/`m2b-rebaseline.sha256` 축약 표기를 실제 파일명 `work-calendar-m2b.baseline.sha256`/`…rebaseline.sha256` 과 맞추면 체크리스트 혼선이 준다. santa R0 에서 같은 지적이 올라와 백로그에 적재했고 이번 라운드에도 다시 왔다 — 두 번 온 LOW 이므로 다음 편집 기회에 함께 고치는 편이 낫다 |
| 2026-08-23 | LOW | .claude/plans/work-calendar-m2.plan.md | santa R1 리뷰어 A 제안: Acceptance 의 `DD3-FIXTURE` 표식 하한이 "4개 이상"인데 Task 5 는 고정 입력 넷 + DD25 동점 시험용 다섯째를 만든다. 하한 검사라 통과에는 지장이 없으나, 다섯째가 등가 단언 입력 집합에 들어가지 않는 **별개**라는 사실이 Acceptance 쪽에서는 읽히지 않는다 |
| 2026-08-23 | MEDIUM | .claude/plans/work-calendar-m2.plan.md | santa R2 B3 · **잔여로 보존**: 두 플랜 모두 PRD 상태·Open Question 검사를 Acceptance 에서 `[기계]` 로 표기하는데 `## Validation` 에는 그것을 검사하는 명령이 없다(명령은 `node --check`·함수 존재/배선·앵커·브라우저 하네스뿐). 구현자가 코드 쪽을 다 통과시키고도 PRD 를 갱신하지 않은 채 끝낼 수 있고, 문서는 "기계 판정 통과" 로 읽힌다. 처방은 둘 중 하나 — Validation 에 대응 grep 을 넣거나, Acceptance 표기를 `[사람]`/`[기계+사람]` 으로 낮춘다. blocking 이 아니라 이번 라운드에서 고치지 않았다 |
| 2026-08-23 | LOW | .claude/plans/work-calendar-m2.plan.md | santa R2 B 제안: `spyOn(frameWindow, name)` 서명이 Acceptance 에서 `storage.set`·`render` 까지 감싸는 데 쓰이므로 `spyOn(target, name)` 으로 일반화하거나 범위를 명시하는 편이 안전하다. santa R0 에 이어 두 번째 제기 |
| 2026-08-23 | MEDIUM | .claude/plans/work-calendar-m2b.plan.md | santa R3 리뷰어 B 제안 · **잔여로 보존**: 후반부 Task 3(첫 실행 온보딩)이 온보딩 dismissal 저장 키와 `newtab.css` 수정 지점을 Action 본문에 적지 않아, 그 구현 결정이 Files to Change 칸에만 남아 있다. DD29(산문은 지시가 아니다)가 코드 산출물에 요구한 것과 같은 형태의 결함이나 blocking 으로 지목되지 않아 이번 라운드에서 고치지 않았다 |
| 2026-08-23 | 해소(2026-08-23) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ 앵커 파일명 두 체계(`m2b-baseline.sha256` 축약 대 `work-calendar-m2b.baseline.sha256`) — santa R0·R1 에 LOW 로, R3 에 MEDIUM(실패 시나리오 포함)으로 세 번 올라왔다. 세 번째에 고쳤다: DD35 와 Acceptance 두 자리를 전체 경로명으로 통일하고 축약명을 쓰지 않는다는 규칙을 DD35 에 못박았다(커밋은 라운드 3 픽스) |
| 2026-08-23 | MEDIUM | .claude/plans/work-calendar-m2b.plan.md | santa R4 B1 · **잔여로 보존**: 후반부 Summary 가 "바꾸는 것은 '어느 셀이 차는가' 하나다" 라고 적는데 같은 플랜의 Task 2 는 렌더 표면이 `gates`·`projectId` 를 읽게 하고 Task 3 은 첫 실행 온보딩 표면을 더한다. 한 구현자는 인덱스·패널 점유만 바꾸고 끝냈다 하고 다른 구현자는 온보딩과 프로젝트/관문 표시까지 넣어도 둘 다 "플랜을 지켰다" 가 된다. **이것은 santa R2 B2 로 전반부에서 이미 고친 것(Summary 의 무변 주장이 같은 플랜이 더하는 새 표면을 덮음)의 후반부 쌍둥이다** — MEDIUM 이라 이번 라운드에서 고치지 않았으나 같은 이유로 다음에 HIGH 로 올라올 공산이 크다 |
| 2026-08-23 | MEDIUM | .claude/plans/work-calendar-m2b.plan.md | santa R4 B2 · **잔여로 보존**: 후반부 Task 2 의 "프로젝트는 기존 뱃지 자리에 이름만" 이 실행 불가능하다 — `createTodoItem()` 은 제목·메타 텍스트·메모·편집·삭제만 그리고 **프로젝트 뱃지 자리가 없다**. 구현자마다 다른 위치에 넣거나 새 뱃지를 만들거나 메타 줄에 접거나 생략할 수 있고 Acceptance 가 어느 쪽이 옳은지 가리지 못한다. 처방은 정확한 DOM 대상과 출력 계약을 적거나, M2b 에서는 프로젝트를 눈에 보이게 그리지 않는다고 못박는 것 |
| 2026-08-23 | MEDIUM | .claude/plans/work-calendar-m2b.plan.md | santa R8 B2 · **잔여로 보존 · PRD 문구 결정이 필요하다**: PRD M2 결과문(96행)은 "겹침이 **그대로** 표현된다" 인데, 후반부 플랜은 `MAX_CHIPS_PER_CELL = 2`(`newtab.js:71`) 상한을 유지하기로 하고 그 대가를 스스로 적었다(m2b 22행: "하루 셋 이상 겹치는 사용자에게는 M2 만으로 겹침이 완전히 보이지 않으며, 그 구간이 M3 까지 남는다"). santa R7 에서 그 판단의 근거 셋(PRD 77행이 부하 표시를 M3 으로 미룸 · `+N` 이 개수를 보존 · 상한 변경은 새 시각 언어라 UI4 위반)을 플랜에 적어 흡수했으나, **PRD 결과문 자체는 여전히 "그대로" 라고 선언한다.** 처방은 셋 중 하나 — (a) PRD M2 결과문을 "겹침이 개수 요약까지 표현된다" 로 좁힌다, (b) m2b Acceptance 에 마일스톤 완료 정의를 그렇게 못박는다, (c) 상한 변경을 M3 이 아니라 M2b 로 당긴다. **PRD 를 고치는 것은 santa-loop 범위 밖이라 사용자 결정이 필요하다** |
| 2026-08-23 | INFO | .claude/plans/work-calendar-m2.plan.md | santa R8 계약 관측(판정 아님): 리뷰어 B 가 세 findings 의 `evidence` 필드에 명령과 출력을 통째로 담아 500자 상한을 넘겼고, 그 결과 `contract=partial` 로 기록되며 **HIGH 둘이 blocking 무게를 잃었다.** 내용은 유효해 그중 둘을 흡수했다(`event.done` 파생 모순 · 온보딩 one-shot 플래그). 다음에 santa-loop 를 돌릴 때 리뷰어 프롬프트에 evidence 길이 상한을 명시하면 같은 손실을 막는다 |
| 2026-08-23 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | L2 invariant: Plan approval cannot verify that promised functions exist, yet Validation treats function existence as an approval-time gate · 원문 .claude/reviews/plan-review-work-calendar-m2.md · id=c42039dc |
| 2026-08-23 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | L2 invariant: 35 of 42 Acceptance items are human-verified with no enforcement mechanism, yet the plan structure implies they are gates · 원문 .claude/reviews/plan-review-work-calendar-m2.md · id=b6b6a92a |
| 2026-08-23 | CRITICAL | .claude/plans/work-calendar-m2.plan.md | L2 invariant: Baseline anchoring fails: baseline.json created after plan approval cannot be constrained by plan sha256, and the baseline integrity check relies on an editable JSON file · 원문 .claude/reviews/plan-review-work-calendar-m2.md · id=cda6c47b |
| 2026-08-23 | HIGH | .claude/plans/work-calendar-m2.plan.md | L2 invariant: Rollback safety for migration chain is not mechanically verified: the v2→v3→v4 chain test relies on a comment marker, not code structure · 원문 .claude/reviews/plan-review-work-calendar-m2.md · id=16243558 |
| 2026-08-23 | HIGH | .claude/plans/work-calendar-m2.plan.md | L2 invariant: Plan's own statement that 'harness passing ≠ actual path operation working' undermines the assertion model · 원문 .claude/reviews/plan-review-work-calendar-m2.md · id=b16ff3e2 |
| 2026-08-23 | FAIL | .claude/plans/work-calendar-m2.plan.md | L2 invariant: reviewer returned verdict=fail · 원문 .claude/reviews/plan-review-work-calendar-m2.md · id=bb8bb6b3 |
| 2026-08-23 | FAIL | .claude/plans/work-calendar-m2b.plan.md | L2 architect: reviewer returned verdict=fail · 원문 .claude/reviews/plan-review-work-calendar-m2b.md · id=9706045b |
| 2026-08-23 | HIGH | .claude/plans/work-calendar-m2b.plan.md | L2 test: Task 1 occupancy test assertions can verify DD31 (gates-based occupancy with same-date collapsing) · 원문 .claude/reviews/plan-review-work-calendar-m2b.md · id=206eb0a4 |
| 2026-08-23 | HIGH | .claude/plans/work-calendar-m2b.plan.md | L2 test: Baseline integrity check at Acceptance line 402 prevents overwriting M2前半部 baseline file · 원문 .claude/reviews/plan-review-work-calendar-m2b.md · id=b467ab7c |
| 2026-08-23 | FAIL | .claude/plans/work-calendar-m2b.plan.md | L2 test: reviewer returned verdict=fail · 원문 .claude/reviews/plan-review-work-calendar-m2b.md · id=03f95d27 |
| 2026-08-23 | MEDIUM | .claude/plans/work-calendar-m2b.plan.md | L2 architect: DD13 판정식의 셋째 항 `!onboardingSeen` 만 소유자가 안 붙어 있다 — 앞 둘은 `this.calendarManager.*` 로 적어 놓고 이것만 bare 다. 접근 형태(`this.settingsManager.get...`)를 못박아야 한다 · 원문 .claude/reviews/plan-review-work-calendar-m2b.md · id=arch-m1 |
| 2026-08-23 | MEDIUM | .claude/plans/work-calendar-m2b.plan.md | L2 architect: DD13 이 "`await this.settingsManager.initialize()` 뒤" 를 요구하는데 그 호출이 `Application.initialize()` 의 **마지막 문장**이다(newtab.js:5506). 뒤에 붙이라는 것인지 순서를 바꾸라는 것인지 별도 메서드로 빼라는 것인지 정하지 않았다 · 원문 .claude/reviews/plan-review-work-calendar-m2b.md · id=arch-m2 |
| 2026-08-23 | MEDIUM | .claude/plans/work-calendar-m2b.plan.md | L2 test: Task 1 단언이 쓰는 `bucketKeys`·`bucketSizesByDate`·`chipCountsByDate` 를 `calendar.eventsByDate`(newtab.js:3322)에서 **어떻게 뽑는지**가 없다. 추출 방식이 구현자마다 다르면 단언의 반증력이 사라진다 · 원문 .claude/reviews/plan-review-work-calendar-m2b.md · id=test-m1 |
| 2026-08-23 | MEDIUM | .claude/plans/work-calendar-m2b.plan.md | 실측(패널 아님): 전반부 앵커 `.claude/plans/work-calendar-m2.rebaseline.sha256` 의 `test/positioning.smoke.js` 줄이 HEAD(7a24204)에서 이미 맞지 않는다 — 전반부가 재앵커를 다음 세션으로 미룬 결과다. M2b 는 그 앵커를 통째로 쓰지 않고 `work-calendar-m2.baseline.json` 줄만 검사하므로 영향받지 않는다. M2 전체가 끝난 뒤 정리 대상 · id=m2-anchor-stale |
| 2026-08-24 | 해소(2026-08-24) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 test HIGH: `DD31-FIXTURE` 표식의 **형태**가 안 정해져 `grep -c` 검사가 헛돌 수 있었다(표식 다섯을 머리말에 몰아 적어도 다섯이 세어진다). `// DD31-FIXTURE <번호>` 를 그 입력을 만드는 문장 **같은 줄 끝**에 붙이도록 Task 1 에 못박았다 · id=test-fixture-form |
| 2026-08-24 | 해소(2026-08-24) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 test MEDIUM: 플랜이 `getCellDueState(dayEvents, dateKey)` 서명 변경의 "유일한 기계 증거" 라고 주장한 (b)·(c) 는 사실 **출력** 증거라 호출부에서 미리 거르는 구현도 통과했다. 주장을 정정하고 Validation 2 에 서명 자체를 보는 grep 한 줄을 더했다(현재 1인자 코드에서 죽고 2인자에서 통과함을 실측) · id=test-m3 |
| 2026-08-24 | 해소(2026-08-24) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 invariant MEDIUM: Files to Change 의 m2b 봉투 커밋 근거가 틀렸다 — "커밋 안 하면 `shasum -c` 가 죽는다" 는 거짓이다(구현 중에는 작업 트리에 있다). 진짜 이유인 "앵커가 이 트리 밖에서도 검증 가능해야 한다" 로 바꾸고, 전반부와 달리 `.gitignore` 에 넣지 않는 것이 의도임을 명시했다 · id=inv-commit-rationale |
| 2026-08-24 | 오독(2026-08-24) | .claude/plans/work-calendar-m2b.plan.md | **패널 오독 · 수리 없음** L2 test CRITICAL: "Task 2 의 Validate 에 렌더 규칙 단언이 없다" 는 지적. **실제로는 있다** — Task 2 Validate 에 규칙 1/2/3 단언이 번호까지 붙어 명시돼 있고 Acceptance 가 그것을 가리킨다. 리뷰어가 인용한 범위(291-295)는 다섯 줄인데 그 블록은 열넷 줄이다. 첫 줄과 끝 줄만 읽고 가운데를 없다고 판정한 것으로 보인다. 이 CRITICAL 하나가 quorum 을 떨어뜨렸다 · id=false-crit-task2-validate |
| 2026-08-24 | 오독(2026-08-24) | .claude/plans/work-calendar-m2b.plan.md | **패널 오독 · 수리 없음** L2 test HIGH: 같은 근거의 재판정("Task 2 Validate 가 추가를 요구하지 않는다"). 더해 "Task 4 가 rendering/* 키를 검사하지 않는다" 는 부분도 성립하지 않는다 — 세 단언은 `collector.add('occupancy/…')` 로 남기므로 Task 4 의 `^(occupancy|onboarding)/` 검사가 이미 덮고, 플랜이 그렇게 적고 있다 · id=false-high-task2-wiring |
| 2026-08-24 | 오독(2026-08-24) | .claude/plans/work-calendar-m2b.plan.md | **패널 오독 · 수리 없음** L2 invariant HIGH: "`.gitignore` 가 m2b 봉투를 제외하지 않는데 플랜은 커밋하지 말라고 한다" 는 지적. **플랜은 반대로 커밋하라고 한다**(리뷰어가 "커밋되지 않으면 … 죽는다" 를 뒤집어 읽었다). `git check-ignore` 로 m2b 가 추적 대상임을 확인했고 그것이 플랜이 원하는 상태다 · id=false-high-gitignore |
| 2026-08-24 | 잔여(2026-08-24) | .claude/plans/work-calendar-m2b.plan.md | **공개된 한계 · 이 마일스톤 밖** L2 invariant HIGH ×2: Task 0 을 건너뛰는 것을 막을 기계적 강제가 없고, 내보내기 버튼의 하드코딩된 파일명이 수동 조율을 요구한다. **플랜이 두 가지를 모두 명시적으로 공개하고 있다**(Task 0 의 "절차 규칙이고 기계적 강제가 없다"). 이 저장소에 러너·CI·커밋 훅이 없다는 사실의 결과이며 유일한 실질 수리는 헤드리스 러너다 · id=m2-headless-runner |

| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 security CRITICAL · invariant CRITICAL · security HIGH · invariant HIGH ×2: 온보딩 판정식의 `!this.settingsManager.calendarOnboardingSeen` 이 `undefined` 로 무너지는 경로가 **둘** 있었다 — `loadSettings()` 의 삼킨 `catch`(newtab.js:4587~4589)와 `SettingsManager.initialize()` 의 조기 `return`(newtab.js:4341~4342, loadSettings 를 아예 안 부른다). `!undefined` 가 참이라 온보딩이 매 로드마다 다시 떴다. 판정식에 첫째 항 `calendarSettingsLoaded` 를 세우고 **생성자 기본값으로 `undefined` 자체를 없앴으며**(항만 더하면 같은 결함이 하나 더 생긴다), Validation 2-b 가 네 항+배선 셋을 기계로 보고 Task 3 Validate `DD13-CASE 4` 가 실패 경로를 하네스에서 재현한다. 경로 (2) 는 패널이 못 본 것으로 이번 실측에서 추가로 찾았다 · id=onboarding-undefined-failopen |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 test HIGH: Task 2 의 렌더 규칙 단언 셋을 `runCalendarOccupancyCases()` 한 함수에 합치기로 한 선택 때문에, 점유 단언 다섯만 쓰고 함수를 닫은 구현도 Validation 2 의 존재·배선 검사를 통과했다. `DD31-FIXTURE` 와 같은 형태의 `TASK2-RULE-1·2·3` 표식을 달고, Validation 2 가 그 함수 **본문 안에서 토큰별로**(합계가 아니라) 셋을 각각 확인한다 · id=task2-rule-markers |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~부분 해소~~ L2 invariant HIGH: Task 4 의 `(1) 재베이스라인 → (2) 재내보내기 → (3) 해시` 순서를 강제하는 검사가 **Task 본문 안에만** 있어 그 Task 를 건너뛴 실행이 Validation 블록을 통째로 통과했다. 봉투의 `occupancy/*`·`onboarding/*` 키 검사를 Validation 3-2b 로 **게이트 블록에 내렸다.** 잡는 것은 "(2) 를 빼먹었다" 하나이고 순서 뒤바꿈·손으로 고친 봉투는 여전히 규율이다(플랜이 그 한계를 적고 있다) · id=inv-task4-order |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 test LOW: Patterns to Mirror 의 인용 줄 드리프트 확인 명령이 산문에만 있어 아무도 안 돌렸다. Validation 1-b 로 내리되 **구현 전 전용**으로 못박았다 — Task 1~3 이 newtab.js 를 고치면 줄이 밀리는 것이 정상이라 끝 상태 게이트로 쓰면 옳은 구현이 반드시 죽는다(3-2 가 자기 baseline.sha256 을 게이트로 안 쓰는 것과 같은 이유). 구현 여부는 새 함수 존재로 가른다 · id=test-citation-drift |
| 2026-08-26 | 오독(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **패널 오독 · 수리 없음** L2 invariant MEDIUM: "파일 부재에서 Task 0 의 `m2base_guard()` 는 FAIL 인데 Validation 3-1 은 PASS 라 불일치" 라는 지적. **실제로는 둘 다 `return 0` + `NOTE:` 로 같다** — 플랜이 부재·불일치·일치 셋을 명시적으로 가르고 그 판정을 한 함수로 못박아 두었다. 리뷰어가 그 이전 판을 읽은 것으로 보인다 · id=false-med-guard-inconsistency |
| 2026-08-26 | 오독(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **패널 오독 · 수리 없음** L2 test MEDIUM: "Validation 2 의 `getCellDueState` 서명 grep 이 승인 시점에 도는데 그때는 구현이 없어 반드시 실패한다" 는 지적. **Acceptance 가 이미 그 항목을 `[기계] (Task 1·3 뒤)` 로 적고 있고**, Validation 머리말도 "승인 시점에 돌 수 있는 것은 1번뿐" 이라고 적는다 · id=false-med-getcell-timing |
| 2026-08-26 | 유예(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **DEFER_TO_BACKLOG** L2 test MEDIUM: 렌더 규칙 단언이 남기는 `collector.add('occupancy/…')` **키 이름**을 Validation 이 개별로 검사하지 않는다. 지금은 `TASK2-RULE-1·2·3` 표식이 단언 존재를 잡고 Validation 3-2b 가 봉투에 `occupancy/*` 키가 있는지를 잡는다. 키 단위 계약은 M3 이 시각 언어를 정하면서 키가 확정된 뒤에 세운다 · id=test-render-collector-keys |
| 2026-08-26 | 유예(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **DEFER_TO_BACKLOG · 의도된 설계** L2 test MEDIUM: 점유 단언 다섯과 렌더 규칙 단언 셋이 `runCalendarOccupancyCases()` 한 함수에서 고정 입력을 공유해 격리가 없다. 플랜이 이유를 적고 있다 — 함수를 늘리면 Validation 2 의 존재·배선 검사와 Acceptance 의 수가 함께 흔들린다. 격리는 헤드리스 러너가 들어와 케이스 단위 실행이 가능해진 뒤에 한다 · id=test-single-function |
| 2026-08-26 | 유예(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **DEFER_TO_BACKLOG** L2 test MEDIUM: `DD31-FIXTURE` 개수 검사가 고정 입력의 **값**이 실제로 다섯 시나리오를 만드는지는 증명하지 못한다. 형태·자리는 이미 못박았고(같은 줄 끝, 입력 하나당 한 줄) 값의 정확성은 하네스 단언 다섯이 본다. 표식이 값을 증명하지 않는다는 한계는 플랜 본문이 명시하고 있다 · id=test-fixture-count-limit |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 invariant CRITICAL: Validation 2-b (iii) 의 범위 추출 `sed -n '/^  async loadSettings() {/,/} catch (/p'` 가 **끝 줄을 포함**해서, `} catch (error) { this.calendarSettingsLoaded = true;` 처럼 catch 여는 줄에 같이 적은 구현이 `T=1` 과 범위 검사를 **둘 다 통과**했다 — try 와 catch 를 가르는 것이 이 검사의 유일한 목적인데 그 경계 한 줄에서 샜다. awk 로 **첫 `try {` 다음부터 `} catch (` 직전까지**만 뜨도록 바꿨다. 반증 실측(2026-08-26): 옛 검사는 catch-같은-줄을 PASS, 새 검사는 reject. **지적에 없던 `try` 앞 배치도 같은 fail-open 이라 함께 막았다**(그 자리는 storage.get 이 터져도 실행된다) · id=onboarding-trycatch-range |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 test MEDIUM + invariant MEDIUM(같은 줄에 대한 두 방향 지적): `getCellDueState` 서명 검사가 한 줄 정규식이라 (a) 서명을 여러 줄로 쓴 **옳은 구현이 죽고**, (b) `(dayEvents, d)` 처럼 **이름만 다른 구현은 통과**했다. 규칙 2 단언과 호출부가 `dateKey` 라는 이름에 기대므로 이름이 계약이다. 선언부터 `) {` 까지 접어 한 줄로 만든 뒤 이름까지 본다. 실측 4케이스(현행 1인자 거부 · 한 줄 2인자 통과 · 여러 줄 2인자 통과 · 이름 다르면 거부) · id=getcell-sig-fold |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 invariant MEDIUM: `F=$(grep -c "DD31-FIXTURE" test/positioning.smoke.js)` 가 파일 전체를 세서 **머리말 주석에 표식 다섯을 몰아 적은 실행이 통과**했다. 바로 아래 TASK2-RULE 검사가 이미 `occ()` 로 본문을 좁히고 있었으므로 그 헬퍼를 블록 앞으로 올려 셋이 함께 쓰게 하고 고정 입력 표식도 본문 안에서 센다. 표식이 **값**을 증명하지 않는다는 한계는 그대로다(id=test-fixture-count-limit) · id=fixture-scope-occ |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 test LOW: 그리드-패널 일치 단언 둘(1번·5번 고정 입력, DD32 의 불변식을 지는 유일한 기계 단언)이 Task 1 산문에만 있어 **요구 개수가 다섯인지 일곱인지 흐렸고** 판정자는 Acceptance 의 `[사람]` 뿐이었다. `DD32-CONSISTENCY-1`·`-5` 표식을 `DD31-FIXTURE`·`TASK2-RULE-n` 과 같은 형태로 달고 Validation 2 가 토큰별로 본다. 이 함수가 담는 단언은 **열**임을 Task 1 에 못박았다(점유 다섯 · 일치 둘 · 렌더 규칙 셋) · id=dd32-consistency-markers |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~부분 해소~~ L2 invariant HIGH: 전반부 앵커 덮어쓰기에 **되돌릴 수단이 없다**는 지적. 이 파일은 `.gitignore` 98행에 걸려 git 이 복구하지 못하고, Task 0 의 `m2base_guard()` 는 덮였는지를 *탐지*할 뿐 *막지* 못한다. Task 0 에 0-b) 를 더해 **저장소 밖(`$HOME`)에 복구본을 뜬다** — mv 목적지를 잘못 적어도 되돌릴 수 있다. 남는 한계는 "사람이 Task 0 자체를 건너뛰는 것" 이고 그것은 헤드리스 러너 몫이다(id=m2-headless-runner) · id=m2base-backup |
| 2026-08-26 | 유예(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **DEFER_TO_BACKLOG · 재지적** L2 test MEDIUM: `TASK2-RULE-n` 표식이 단언의 **질**을 증명하지 못한다 — `assert(true); // TASK2-RULE-1` 도 통과한다. 표식 검사 전반의 구조적 한계이며 id=test-fixture-count-limit 과 같은 종류다. 표식은 "빠뜨림" 을 잡고 값·질은 하네스 단언이 잡는다. 케이스 단위 실행이 가능해지는 헤드리스 러너 뒤에 세운다 · id=marker-quality-limit |
| 2026-08-26 | 오독(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **패널 오독 · 수리 없음** L2 test MEDIUM: "5번 고정 입력의 관문 접기가 **잘못된 자리**(호출부 vs 이벤트 수준)에 구현되면 Validation 2 가 못 잡는다" 는 지적. 지적 자신이 "접기를 아예 빼면 그 단언이 실패한다" 고 인정하고 있고, 남는 것은 **같은 출력을 내는 구현 위치 차이**다. 플랜은 출력(버킷 길이 1)을 계약으로 삼았지 구현 위치를 계약으로 삼지 않았으므로 결함이 아니다 · id=false-med-fold-placement |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 test CRITICAL(R2): Task 4 Validate (d) 가 `range/01-month-boundary` 의 구현 후 기대값을 **절대값으로 적어 두고도** 그것을 확인하는 줄이 어디에도 없었다 — 판정자는 Acceptance 의 `[사람]` 하나뿐이라, **DD31 을 구현하지 않고 새 케이스만 더한 실행**이 Validation 2 와 3-2b 를 전부 통과하고 옛 연속 범위 값이 정상 기준으로 굳었다. Validation 3-2c 를 더해 봉투의 그 키를 읽고 세 값을 대조한다. **브라우저가 필요 없고**(Task 4 가 내보낸 JSON 만 읽는다) 이 케이스만 절대 날짜라 돌리는 날에 흔들리지 않는다. 반증 실측(2026-08-26): 옛 7일 값 거부 · 관문 둘 통과 · 키 유실 검출. **이로써 이 마일스톤의 헤드라인 결과(UI15)가 처음으로 기계 판정을 갖는다** · id=test-headline-envelope |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 architect MEDIUM(R2): Task 2 가 렌더 함수 **여섯**을 한 줄로 묶어 "고치는 것" 이라 적어 놓고 아래 규칙 1~3 은 그중 **셋**만 정했다. 나머지 셋에서 무엇을 고쳐야 하는지 찾던 구현자가 없는 변경을 지어내거나 플랜을 미완으로 읽는다. 실측으로 갈라 표로 적었다 — 직접 고치는 것은 `createTodoItem`·`createChips`·`createDayCell` 셋이고, `renderSummary` 는 `getEventDueState()` 를 셀 뿐이며(전반부 DD5a 가 이미 종단 관문을 읽게 했다) `renderGrid`·`renderPanel` 은 오케스트레이터다. **아니오 셋에 손대지 않는 것이 계약**임을 명시했다 · id=arch-six-functions |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 architect MEDIUM(R2): `getEventDueState()` 가 이미 종단 관문을 읽는다는 **전반부 전제**(전반부 DD5a)가 이 플랜에 한 번 스쳐 지나갈 뿐 전제로 적혀 있지 않아, 이 플랜만 읽은 구현자는 마감 판정도 자기 몫이라고 읽을 수 있었다. Task 2 에 전제를 명시하고 "이 플랜은 마감 판정을 건드리지 않는다 — 바꾸는 것은 *어느 셀이 차는가*와 *그 셀에 어떤 상태를 붙이는가*" 를 못박았다 · id=arch-dd5a-prereq |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~부분 해소~~ L2 test CRITICAL(R2): "접기 단언(고정 입력 5)이 빠지면 접기를 안 한 구현이 최종 게이트를 통과한다" — 근거로 인용한 것은 **플랜 자신의 Acceptance 경고문**이다. R1 흡수로 `DD32-CONSISTENCY-5` 표식 검사가 이미 들어갔고(그 단언이 버킷 길이 1 을 본다), 3-2c 가 점유 축소를 봉투에서 본다. 남는 것은 "표식은 있는데 단언이 비었다" 이며 그것은 표식 검사 전반의 한계다(id=marker-quality-limit) · id=fold-assert-partial |
| 2026-08-26 | 오독(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **패널 오독 · 수리 없음** L2 test HIGH(R2): "Validation 이 Task 0 을 Tasks 1~4 보다 먼저 돌도록 강제하지 않는다" 는 지적. **Validation 3-2 가 정확히 그것을 잡는다** — `[ -f .claude/plans/work-calendar-m2b.baseline.sha256 ]` 로 "Task 0 을 통째로 건너뛴 실행" 을 막고 있고 플랜이 그 문장을 그대로 적고 있다. *순서 뒤바꿈*(0 은 돌았는데 4 보다 뒤)은 잡지 못하며 그것은 이미 유예돼 있다(id=inv-task4-order) · id=false-high-task0-order |
| 2026-08-26 | 오독(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **패널 오독 · 수리 없음** L2 architect LOW(R2): "Task 1 만 읽은 구현자가 Task 2 단언을 빠뜨리는 것을 문서로만 막는다" 는 지적. 지적 **자신이** "Validation 2 checks for these markers, so the gate itself catches omission" 이라고 적고 있다 — R1 흡수로 들어간 `TASK2-RULE-1·2·3` 토큰별 검사가 기계 판정이다 · id=false-low-task-coupling |
| 2026-08-26 | 잔여(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **공개된 한계 · 이 마일스톤 밖 · 재지적** L2 test CRITICAL ×2 + HIGH ×2(R2): "Acceptance 스물다섯 중 열둘이 게이트가 아니라 약속" · "표식이 단언의 **값**을 증명하지 못한다". 근거로 인용된 것이 **전부 플랜 자신의 공개 문장**이다(플랜 739행 등). 이 저장소에 러너·CI·커밋 훅이 없다는 사실의 직접적 결과이며, 플랜 개정으로 줄어들지 않는다 — 정직하게 적을수록 인용거리가 늘어난다. 유일한 실질 수리는 헤드리스 러너다. **다만 이번 판에서 3-2c 가 그 열둘 중 가장 중요한 하나(헤드라인 결과)를 기계로 내렸다** · id=m2-headless-runner |
| 2026-08-26 | 잔여(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **절차 · 게이트 밖** L2 invariant(R2): invariant 리뷰어가 StructuredOutput 을 호출하지 않고 종료해 **빈 결과**를 냈다(패널 3/4 응답). finding 이 아니라 하네스 실패이며, 그 관점의 지적이 이 라운드에 없다는 뜻이지 통과했다는 뜻이 아니다. R1 의 invariant 지적 넷은 전부 이 판에 흡수돼 있다 · id=r2-invariant-empty |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 invariant CRITICAL(R3): Validation 2-b (ii) 가 "SettingsManager **생성자**의 기본값 둘" 을 요구한다고 적어 놓고 실제로는 `newtab.js` **파일 전체**를 grep 했다. 기본값을 `SettingsManager.initialize()` 안에 둔 구현은 **DOM 요소 부재로 조기 return 하는 경로(newtab.js:4341~4342)에서 실행되지 않아** 필드가 여전히 `undefined` 인데 옛 검사는 통과시킨다 — DD13 이 막으려는 경로 (2) 가 정확히 그 경로다. `sm_ctor()` 헬퍼로 `class SettingsManager` → `constructor(` → `^  }` 범위를 뜬 뒤 그 안에서만 본다. 반증 실측(2026-08-26): 기본값을 initialize() 로 옮긴 픽스처에서 옛 검사 통과 · 새 검사 거부 · 옳은 구현 통과 · id=onboarding-ctor-scope |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 invariant HIGH(R3): Validation 2-b (i) 이 판정식 **네 항** 중 첫째 항 하나만 파일 전체에서 grep 했다. 둘째~넷째 항이 통째로 빠진 구현이 기계 게이트를 그대로 통과한다 — DD13 이 "항을 넣어 놓고 항상 참이 되는 것은 항을 안 넣은 것과 같다" 고 적은 결함이 이번에는 *항이 아예 없는* 쪽으로 열려 있었다. `app_init()` 로 `Application.initialize()` 본문을 뜨고 `getcell_sig` 와 같은 접기 관용구로 `if (`~`) {` 를 한 줄로 만든 뒤 네 항을 **각각** 본다(합계를 세지 않는다). 더해 `&&` ≥ 3(‖ 로 이으면 항 하나만 참이어도 열린다)과 첫째 항의 **부호**(부정하면 정반대 게이트가 된다)도 본다. 반증 실측(2026-08-26): 항 둘만 · `||` · 첫째 항 부정 셋 다 옛 검사 통과 · 새 검사 거부, 옳은 구현은 한 줄 형태와 여러 줄 형태 둘 다 통과 · id=onboarding-cond-terms |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 invariant MEDIUM(R3): Validation 2-b (iv) 의 순서 검사가 **파일 전역 줄 번호**를 비교해서, "판정이 `Application.initialize()` 안에 있다" 를 전혀 보지 않았다 — 두 이름이 어느 클래스에 있든 앞뒤 순서만 맞으면 통과한다. (i) 과 같은 `app_init()` 범위로 좁혀 본문 안 상대 줄 번호를 비교하게 했고, 그러면 "그 메서드 안에 있다" 와 "그 안에서 await 뒤에 선다" 가 한 검사로 합쳐진다 · id=onboarding-order-scope |
| 2026-08-26 | 유예(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **DEFER_TO_BACKLOG · 공개된 한계** L2 invariant MEDIUM(R3): DD34 가 정한 Task 순서(Task 3 온보딩이 Task 1·2 뒤)를 기계로 강제하는 검사가 없다. Validation 3-2 가 "Task 0 을 통째로 건너뛴 실행" 은 잡지만 *순서 뒤바꿈*은 못 잡는다는 것을 플랜이 이미 적고 있고(id=inv-task4-order), 이것은 그와 같은 종류다 — 이 저장소에 러너·CI·커밋 훅이 없다는 사실의 결과다. 다만 DD34 의 근거는 "온보딩이 만든 프로젝트가 적응 안 된 화면에서 안 보인다" 는 **작업 편의**이지 불변식이 아니므로, 순서가 뒤바뀌어도 최종 산출물은 같다. 헤드리스 러너 뒤에 세운다 · id=m2-headless-runner |
| 2026-08-26 | 오독(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **패널 오독 · 수리 없음** L2 invariant LOW(R3): "Validation 3-1 의 `grep -c` 가 아무것도 못 찾으면 조용히 fail-open 한다" 는 지적. **플랜이 정확히 그것을 막고 있다** — `M2N=$(grep -c …)` 뒤에 `[ "$M2N" = "1" ]` 가드가 있고, 지적 자신이 그 가드와 플랜의 설명 주석을 근거로 인용하고 있다. "grep 출력이 malformed 일 수 있다" 는 뒷부분은 구체적 실패 경로를 제시하지 않는다 · id=false-low-m2n-guard |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 test HIGH(R4): DD13 이 온보딩 fail-open 경로를 **둘** 이름으로 적었는데(`loadSettings()` 의 삼킨 catch · `SettingsManager.initialize()` 의 조기 return) Task 3 Validate 의 하네스 케이스는 `DD13-CASE 4`(읽기 실패) **하나뿐**이었다. Acceptance 항목은 두 경로를 다 적고 있었으므로 **주장이 배선보다 넓었다** — 이 플랜이 계속 고쳐 온 바로 그 모양이다. `DD13-CASE 6` 을 더했다: `loadApp()` 은 부팅 끝에 "SettingsManager 가 조기 return 하지 *않았음*" 을 기다리므로(test/positioning.smoke.js:185~188) 그것으로는 재현할 수 없고, 정상 부팅한 프레임에서 요소 하나를 지운 뒤 **두 번째 인스턴스**를 세워 조기 return 을 재현한다. `=== false` 로 보는 것이 요점이다(`!field` 로 보면 `undefined` 도 참이라 이 케이스가 잡아야 할 상태를 통과시킨다). 표식 개수 다섯 → **여섯**(Validation 2 · Acceptance · Task 3 세 자리를 함께 고쳤다) · id=dd13-case6-early-return |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 architect LOW(R4): DD32 가 패널의 조회 자리를 `newtab.js:4161 → 3353` 으로 적어 리뷰어가 **호출부 인용**으로 읽었다. 4161 은 `renderPanel()` 의 머리 줄이고 실제 호출은 **4179** 다(실측). 함수 머리와 호출 줄을 갈라 적었다 · id=dd32-call-site-citation |
| 2026-08-26 | 유예(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **DEFER_TO_BACKLOG · 재지적** L2 test MEDIUM(R4): `DD31-FIXTURE` 표식 검사가 고정 입력의 **내용**을 증명하지 못한다(주석 다섯 줄로도 통과). id=test-fixture-count-limit · id=marker-quality-limit 과 같은 지적이며 R1·R2 에서 이미 유예됐다. 형태·자리·범위는 못박혀 있고 값은 하네스 단언 다섯이 본다 · id=test-fixture-count-limit |
| 2026-08-26 | 유예(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **DEFER_TO_BACKLOG · 공개된 한계** L2 test MEDIUM(R4): Validation 2 의 검사들이 **구문적**이라, `getCellDueState` 의 서명이 맞아도 구현이 `dateKey` 를 실제로 **쓰지 않으면** 통과한다. 플랜이 그 한계를 이미 적고 있고(“2번이 통과해도 DD31·DD32 가 충족된 것은 아니다”), 그 경우는 Task 2 규칙 2 의 단언 (b)·(c) 가 브라우저 실행에서 잡는다 — 즉 판정자가 `[사람]` 이라는 뜻이지 판정자가 없다는 뜻이 아니다. 기계로 내리려면 헤드리스 러너가 필요하다 · id=m2-headless-runner |
| 2026-08-26 | 유예(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **DEFER_TO_BACKLOG** L2 test LOW(R4): 고정 입력 다섯 중 어느 것도 하루에 **셋 이상**을 세우지 않아 `MAX_CHIPS_PER_CELL`(=2) 절단이 실제로 행사되지 않는다. 단언 3 의 `Math.min(...)` 자체는 옳고(자르지 않은 수와 비교하면 옳은 구현이 죽는다) 잡아야 할 회귀는 "인덱스와 화면이 어긋난다" 이지 "상한이 동작한다" 가 아니다 — 상한은 이 플랜이 바꾸지 않는 기존 동작이다. 여섯째 고정 입력은 Task 1·Validation·Acceptance 세 자리의 수를 함께 흔들므로 M3(시각 언어)에서 밀도 표현과 함께 세운다 · id=test-chip-cap-unexercised |
| 2026-08-26 | 잔여(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **절차 · 게이트 밖 · 재발** L2 security(R4): security 리뷰어가 StructuredOutput 을 호출하지 않고 종료해 **빈 결과**를 냈다(패널 3/4 응답, quorum 은 3 이라 성립). finding 이 아니라 하네스 실패이며, 그 관점의 지적이 이 라운드에 없다는 뜻이지 통과했다는 뜻이 아니다. R2 의 `id=r2-invariant-empty` 와 같은 사고가 다른 관점에서 재발했다 — 관점당 1/4 확률로 라운드가 반쪽이 된다는 뜻이므로 패널 자체의 신뢰도 항목으로 남긴다. R3 에서 security 는 pass 였다 · id=panel-empty-result |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 test MEDIUM(R5): DD32 의 두 자리 중 **패널 쪽(`getEventsForDate()`)에 기계 판정이 하나도 없었다.** `rebuildIndex()` 만 고치고 패널 조회를 옛 범위 비교로 둔 반쪽 구현이 Validation 2·3 을 전부 통과했고, 판정자는 브라우저 실행(`[사람]`) 하나뿐이었다 — DD32 가 막으려는 바로 그 구현이다. Validation **2-c** 를 더해 그 함수 본문을 **두 방향으로** 본다: `gates` 를 읽는가(있어야) · `startDate`/`endDate` 비교가 남았는가(없어야). 한쪽만 보면 둘을 함께 쓰는 구현이 통과해 옛 규칙이 조용히 살아 있는다. 반증 실측(2026-08-26): 현행 거부 · 범위를 남긴 픽스처 거부 · gates 픽스처 통과 · id=dd32-panel-machine-check |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 test LOW(R5): `getCellDueState` 서명 검사가 **선언만** 봐서, 서명을 `(dayEvents, dateKey)` 로 넓혀 놓고 본문에서 `dateKey` 를 한 번도 안 쓰는 구현이 통과했다. 그 구현은 규칙 2 가 요구하는 "그 날짜의 관문이 전부 dropped 인 이벤트를 건너뛴다" 를 할 수가 없다. `) {` 다음 줄부터의 본문에서 그 이름을 찾는 줄을 더했다(여러 줄 서명에도 맞는다). 반증 실측: 서명만 넓힌 픽스처 거부 · 본문에서 쓰는 픽스처 통과 · id=getcell-body-uses-datekey |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 invariant MEDIUM(R5): 온보딩 판정식의 `&&` 개수 검사(`>= 3`)만으로는 `A && B && C && (D || 아무거나)` 를 막지 못한다 — 항은 넷 다 있고 `&&` 도 셋인데 넷째 항이 OR 로 약해져 **그 항이 없는 것과 같아진다.** DD13 의 판정식에는 OR 이 없으므로 접힌 판정식에 `||` 가 **하나라도 있으면 거부**하는 줄을 더했다. 반증 실측(2026-08-26): 중첩 OR 픽스처 거부 · id=onboarding-cond-no-or |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~부분 해소~~ L2 invariant HIGH(R5): Validation 3-2b 가 `k.length===0` 만 봐서 **한쪽 케이스 함수가 통째로 비었거나 던진 실행**이 통과했다(`occupancy/*` 만 있어도 통과). `occupancy/*` 와 `onboarding/*` 를 **양쪽 다** 요구하도록 바꿨고, Task 4 Validate 의 쌍둥이 검사도 같은 줄로 함께 고쳤다. 여전히 못 잡는 것은 순서 뒤바꿈과 손으로 고친 봉투이며 플랜이 그 한계를 적고 있다 · id=inv-3-2b-both-prefixes |
| 2026-08-26 | 오독(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **패널 오독 · 수리 없음** L2 invariant CRITICAL(R5): "`work-calendar-m2.baseline.json` 덮어쓰기에 롤백 수단이 없다 — 플랜이 인정만 하고 해결하지 않는다". **해결돼 있다** — Task 0 의 **0-b)** 가 저장소 밖(`$HOME`)에 복구본을 뜬다(R2 에서 id=m2base-backup 으로 더했다). 리뷰어가 인용한 189~190행은 *왜 예전에는 되돌릴 수 없었는가* 를 설명하는 문단이고 그 바로 아래가 0-b) 다. 게다가 이 트리에서 그 파일은 현재 **부재**라(실측 2026-08-25) 덮을 대상 자체가 없다 · id=false-crit-m2base-rollback |
| 2026-08-26 | 오독(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **패널 오독 · 수리 없음** L2 invariant HIGH(R5): "`work-calendar-m2b.rebaseline.sha256` 이 다른 실행 환경의 baseline.json 을 가리켜 receipt anchoring 이 깨진다". 성립하지 않는다 — 봉투와 뒤쪽 앵커는 Task 4 에서 **함께** 만들어져 **함께 커밋**되므로 클론은 둘을 같이 받는다. 앞쪽 `baseline.sha256` 은 Task 4 가 봉투를 덮으므로 끝 상태에서 `-c` 가 안 맞는 것이 **설계**이고, 그래서 Validation 3-2 는 그것을 `-c` 하지 않고 **존재만** 본다("기록이지 끝 상태 게이트가 아니다" — Files to Change·Task 0·Validation 세 자리가 같은 말을 적고 있다) · id=false-high-anchor-env |
| 2026-08-26 | 잔여(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **공개된 한계 · 재지적** L2 invariant HIGH + MEDIUM(R5): "Task 0 을 건너뛰는 것을 막을 CI·훅이 없다" · "2·3번 게이트가 승인 시점에 못 돈다". 둘 다 근거로 인용된 것이 **플랜 자신의 공개 문장**이다(251행 · 514행). 부분적으로는 이미 답이 있다 — Validation 3-2 의 `[ -f …baseline.sha256 ]` 가 "Task 0 을 통째로 건너뛴 실행" 을 잡고, Acceptance 는 `[기계]` 마다 **언제** 도는지를 괄호로 적어 승인 시점 강제를 주장하지 않는다. 남는 것(순서 뒤바꿈·첫 커밋 전 강제)은 러너 몫이다 · id=m2-headless-runner |
| 2026-08-26 | 유예(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **DEFER_TO_BACKLOG · 재지적 ×3** L2 test MEDIUM ×2 + LOW(R5): 표식 검사가 고정 입력·단언의 **내용**을 증명하지 못하고(주석으로도 통과), `DD13-CASE 4·6` 이 실제로 서로 다른 실패 경로를 세우는지도 표식으로는 알 수 없다. 봉투 대조(3-2c)가 다섯 고정 입력 중 하나만 보는 것도 같은 종류다 — 나머지 넷은 `todayKey` 상대라 돌리는 날마다 값이 달라 절대값 대조를 세울 수 없고, 플랜이 그 이유를 적고 있다. 케이스 단위 실행이 가능해지는 헤드리스 러너 뒤에 세운다 · id=marker-quality-limit |
| 2026-08-26 | 절차(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **반환 계약 위반 · finding 아님** L2 test(R5): test 리뷰어가 최고 심각도 MEDIUM 인데 `verdict="fail"` 을 반환했다. 프롬프트의 반환 계약은 "HIGH/CRITICAL 이 하나라도 있으면 fail" 이므로 이 라운드의 fail 은 계약과 어긋난다. `decide` 는 `verdict=fail` 을 그대로 blocking 으로 세므로 게이트는 fail-closed 로 동작했다(옳은 방향이다). 관점별 반환 품질 편차로 남긴다 — id=panel-empty-result 와 같은 축이다 · id=panel-verdict-contract |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 architect HIGH(R6): 이 플랜은 `event.gates[].planned` 와 `event.projectId` 가 이미 있다고 **전제**하는데 그 전제를 보는 검사가 하나도 없었다. Task 0 의 `meta.assertFailures === 0` 은 전반부의 **단언이 통과했다**만 증명하지 필드가 생겼다는 것을 증명하지 않는다 — 전반부가 렌더 출력만 단언했다면 필드 없이도 0건이 나온다. 그러면 Task 1 이 `event.gates` 를 처음 건드리는 자리에서 `undefined` 를 만나고 그 시점은 이미 첫 비가역 지점을 지난 뒤다. Validation **1-c** 를 더했고 **승인 시점에 돈다**(전제는 구현 전에 성립해야 한다). 반증 실측(2026-08-26): 현행 트리 통과 · `createCalendarGate` 를 지운 픽스처 거부 · id=m2a-prereq-check |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 test HIGH(R6): Validation 3-2c 의 기대값 셋이 근거로 커밋된 `work-calendar-m2.baseline.json` 을 들었는데 **그 파일이 이 트리에 부재해**(실측 2026-08-25) 기대값의 근거가 확인 불가였다 — 이 플랜이 인용 드리프트를 경계하는 것과 같은 문제다. 파일 없이 다시 셀 수 있게 **요일 산술로 유도**를 적었다: 창 계산은 `newtab.js:4032` 의 `new Date(viewYear, viewMonth, 1 - firstOfMonth.getDay())` 한 줄이고, 2026-01-01 은 목(`getDay()===4`)이라 1월 뷰가 2025-12-28~2026-02-07(2/1~2/3 포함), 2026-02-01 은 일(`getDay()===0`)이라 2월 뷰가 2026-02-01~2026-03-14(1월 날짜 없음)다. 절대 날짜만 쓰므로 돌리는 날에 안 흔들린다 · id=headline-values-derivation |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~부분 해소~~ L2 test HIGH·MEDIUM(R6): 표식 검사가 **주석만 있는 줄**로도 통과했다(`// TASK2-RULE-1` 한 줄이면 그만). 자리 규칙을 산문에만 적어 둔 것은 검사가 아니다. 줄 모양을 좁혔다 — `TASK2-RULE-n`·`DD32-CONSISTENCY-n` 은 그 표식이 붙은 줄이 **`assert(` 를 담아야** 하고, `DD31-FIXTURE`·`DD13-CASE` 는 **주석 전용 줄을 세지 않는다**. 반증 실측(2026-08-26): 주석 전용 표식을 섞은 픽스처에서 F 가 6→5 로 떨어지고 `TASK2-RULE-3` 이 거부됐다. **단언의 *내용*은 여전히 증명하지 못한다**(id=marker-quality-limit) — 이번에 옮긴 것은 "표식만 있고 코드가 없는" 경우 하나다 · id=marker-line-shape |
| 2026-08-26 | 오독(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **패널 오독 · 수리 없음** L2 test MEDIUM(R6): "2-c 가 키워드 검사라 `event.gates.some(...) && (event.startDate \|\| event.endDate) ? [event] : []` 같은 구현이 통과한다". **그 예시는 2-c 가 거부한다** — `startDate` 를 담고 있고 2-c 의 둘째 방향이 정확히 그것을 막는다. 지적이 든 반례가 지적을 스스로 반증한다 · id=false-med-2c-keyword |
| 2026-08-26 | 오독(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **패널 오독 · 수리 없음** L2 test MEDIUM(R6): "`DD13-CASE 6` 이 새 인스턴스를 보므로, 판정을 `settingsManager.initialize()` **전에** 두는 구현은 통과한다". 그 구현은 **Validation 2-b (iv)** 가 잡는다 — `Application.initialize()` 본문 안 상대 줄 번호로 판정이 await 뒤에 서는지를 본다(R5 에서 범위까지 좁혔다). 6번이 보는 것은 "조기 return 경로에서 필드가 `undefined` 가 아닌가" 이고 자리는 (iv) 가 본다 — 다른 것을 보는 두 검사다 · id=false-med-case6-indirect |
| 2026-08-26 | 오독(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **패널 오독 · 수리 없음** L2 test MEDIUM(R6): "Task 2 규칙 1 의 단언 코드가 없어서 (a) 만 검사하고 (b) 를 빼먹으면 옛 형식이 통과한다". **플랜이 (a)·(b) 를 둘 다 요구하고 왜 (b) 가 가르는 자리인지까지 적고 있다** — 옛 경로가 `` `${formatShortDate(startDate)} – ${formatShortDate(endDate)}` `` 라 양 끝 날짜가 새 규칙과 똑같이 나오므로 **구분자**가 유일한 판별자라는 문단이 그 자리다 · id=false-med-rule1-assertion |
| 2026-08-26 | 유예(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **DEFER_TO_BACKLOG · 공개된 한계** L2 test LOW(R6): Validation 1-b 의 인용 줄 드리프트 검사가 구현 뒤에는 영구히 건너뛰어져 지속적 가치가 없다. **의도된 설계다** — Task 1~3 이 `newtab.js` 를 고치면 줄이 밀리는 것이 정상이고, 끝 상태 게이트로 쓰면 옳은 구현이 반드시 거기서 죽는다(3-2 가 자기 `baseline.sha256` 을 게이트로 안 쓰는 것과 같은 이유). 구현 뒤의 인용 갱신은 커밋 시점의 규율이다 · id=test-citation-drift |
| 2026-08-26 | 유예(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **DEFER_TO_BACKLOG · 설계 논쟁** L2 architect MEDIUM ×2 + LOW(R6): "`rebuildIndex()`·`getEventsForDate()` 는 질의 계층이므로 전반부(M2a) 몫 아닌가" · "14행은 '하나' 18행은 '다섯' 이라 범위가 모호하다" · "M3 이 깨끗한 이음매를 못 가진다". 셋 다 플랜이 이미 다루고 있다 — 분할 근거는 「이 플랜이 전반부와 갈린 이유」 절이고, DD31 은 그 결정이 **사용자가 내린 것**임을 날짜와 함께 적으며(2026-08-22), 14→18행의 정정은 santa R4·R5 에서 *일부러* 넣은 자기 정정이다. M3 이음매는 Design Routing Guide 가 "관문의 시각 언어는 M3" 로 경계를 긋는다. 설계 취향의 문제이지 결함 근거가 아니다 · id=arch-milestone-boundary-taste |
| 2026-08-26 | 절차(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **하네스 실패 · 재발 3회** L2 invariant(R6): invariant 리뷰어가 StructuredOutput 을 호출하지 않고 종료해 빈 결과를 냈다(패널 3/4). R2 invariant · R4 security 에 이어 세 번째이며, 라운드마다 관점 하나가 통째로 빠질 수 있다는 뜻이다 — 그 관점이 통과했다는 뜻이 **아니다** · id=panel-empty-result |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 test HIGH ×3 + MEDIUM ×2 · invariant HIGH · architect MEDIUM(R7): **단언 열을 산문으로만 적고 코드로 적지 않았다.** 그래서 표식 검사가 `assert(` 를 요구해도 `assert(true) // TASK2-RULE-1` 이 **어길 계약이 없었다.** R4·R5·R6·R7 네 라운드에 걸쳐 같은 뿌리로 반복된 지적이다. Task 1·Task 2 Validate 에 단언 열 전부를 **코드로** 적었다 — 비교의 양쪽을 서로 다른 출처에서 뽑는 `allGatePlannedDates`(이벤트의 관문) vs `bucketKeys`(인덱스), DD32-CONSISTENCY 둘의 실제 비교식, TASK2-RULE 셋의 실제 비교식. 실측으로 드러난 부수 결함 셋도 함께 고쳤다 — (1) `isSubset` 이 저장소에 **없고** `setEq` 는 다른 케이스 함수 안의 지역 const 라(test/positioning.smoke.js:844) 둘 다 새로 둬야 한다, (2) `MAX_CHIPS_PER_CELL`·`DUE_STATE_LABELS` 는 `const` 라 **프레임 전역에 안 붙어**(function 선언과 달리) 하네스가 값을 복제할 수밖에 없다, (3) 1번 고정 입력 단언이 `length === 2` 만 봐서 today-1·today 를 점유한 구현도 통과했다 — 날짜 집합 비교로 바꿨다 · id=assertions-as-code |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 architect HIGH(R7): Task 1 Action 이 `rebuildIndex()` 의 **창 절단**을 말하지 않았다. 현행 코드는 렌더 창 밖 이벤트를 건너뛰고(`newtab.js:3327`) 창 안으로 잘라 넣는데(`3329`), 그 이유가 상한이다(5,000건 × 366일 — `newtab.js:2199`~`2202`). 제약이 Validation 의 전제 단언 쪽에만 있어서 **Action 만 읽은 구현자는 창 절단을 지우고**, 그러면 이 플랜이 성능 회귀를 만든다. Action 에 "고유 planned 중 `windowStartKey <= planned && planned <= windowEndKey` 인 것만" 을 명시했다. DD29 가 금지한 "규칙은 산문에, 지시에는 없음" 이 다시 이 자리였다 · id=task1-window-clip |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 invariant CRITICAL(R7): Validation 2-b 가 판정식과 **읽기** 배선만 보고, DD13 이 요구하는 "플래그는 만들었을 때와 건너뛰었을 때 **둘 다** true 로 쓴다" 를 보는 줄이 없었다. 건너뛰기에서 안 쓰는 구현은 넷째 항이 영원히 false 라 **건너뛴 사용자에게 매 초기화마다 다시 뜨는데**(santa R8 B1 이 막으려던 그 상태) 기계 게이트를 하나도 건드리지 않았다. (v) 를 더해 선언 하나 · 호출 둘 이상을 본다. 실측(2026-08-26): 구현 전 트리에서 "선언이 0곳" 으로 거부 · id=onboarding-save-callsites |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 invariant HIGH(R7): Task 0·4 의 이름 바꾸기 절차가 내보내기 버튼의 하드코딩된 파일명을 전제하는데 그 이름을 보는 검사가 없었다. 이름이 리팩터링·오타로 바뀌면 `mv` 가 없는 파일을 가리켜 조용히 실패하고, **되돌릴 수 없는 자리의 절차가 전제부터 무너진다.** Validation 1-c 에 `link.download` 고정 검사를 더했고 **승인 시점에 돈다**. 실측(2026-08-26): 현행 통과(test/positioning.smoke.js:2000) · 이름을 바꾼 픽스처 거부 · id=export-filename-pinned |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 architect MEDIUM(R7): `runCalendarOccupancyCases()` 가 Task 2 단언 셋도 담는다는 사실이 Validate 와 산문에만 있고 **Task 1 Action(지시)에는 없었다** — 경고였지 계약이 아니었다. Action 에 "이 함수는 단언 **열**을 담는다(점유 다섯 · 일치 둘 · 렌더 규칙 셋)" 를 계약으로 적었다 · id=task1-colocation-contract |
| 2026-08-26 | 오독(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **패널 오독 · 수리 없음** L2 invariant CRITICAL(R7): "`event.gates` 가 `null`/`undefined` 인 이벤트가 있으면 Task 1 이 렌더 중 크래시한다". **코드가 반증한다** — `createCalendarEvent()` 는 gates 가 비면 어느 경로에서든 `null` 을 반환한다(`newtab.js:572` v4 손상 · `591` 레거시 합성 실패 · `594` v4 인데 gates 없음). 즉 저장소에 들어간 이벤트는 **항상** 비어 있지 않은 `gates` 배열을 갖는다. 그 불변식은 전반부(M2a)가 세운 것이고 그 Validation 의 몫이며, 1-c 의 몫은 모델이 존재하는지까지다 · id=false-crit-gates-nullable |
| 2026-08-26 | 오독(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **패널 오독 · 앞 라운드 인용** L2 invariant HIGH ×2(R7): "3-2c 기대값에 유도가 없다" · "1-c 가 assertFailures 로 전제를 확인한다". **둘 다 R6 에서 해소된 내용을 R6 *리뷰 문장*에서 인용했다** — 유도는 id=headline-values-derivation 으로 요일 산술까지 적혔고, 1-c 는 id=m2a-prereq-check 로 신설돼 `assertFailures` 와 무관하게 `createCalendarGate()`·`createCalendarEvent()` 본문을 직접 본다. 리뷰어가 이전 판의 리뷰 기록을 근거로 든 것으로 보인다 · id=false-high-stale-citation |
| 2026-08-26 | 유예(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **DEFER_TO_BACKLOG · 재지적** L2 test MEDIUM · LOW(R7): "Task 3 이 케이스 4·6 을 어떻게 세우는지 안 적었다" 는 지적은 성립하지 않는다(둘 다 세우는 법이 코드까지 적혀 있다 — 4번은 `runLoadFailureCases()` 관용구, 6번은 요소 제거 + 두 번째 인스턴스). "1-c 가 M2a 완료 커밋 해시를 안 적는다" 는 지적은 실체가 있으나 **커밋 해시를 앵커로 삼으면 리베이스·스쿼시에서 즉시 낡는다** — 이 플랜은 대신 *모델이 있는가* 를 직접 본다. 러너 도입 뒤 M2a 앵커 자체를 receipt 로 묶는 것이 실질 수리다 · id=m2-headless-runner |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 architect HIGH(R8): 창 절단 제약을 R7 에서 더하면서 `rebuildIndex()` **지시 줄이 아니라 뒤따르는 설명 문단**에 넣었다 — 리뷰어가 "플랜이 스스로 금지한 DD29 위반(규칙은 산문에, 지시에는 없음)이 바로 그 수리 안에 있다" 고 지적했고 성립한다. 제약 절을 지시 문장 안으로 옮기고, 문단은 *왜 거기 있는지*의 근거로 바꿨다 · id=task1-window-clip |
| 2026-08-26 | 해소(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | ~~해소~~ L2 architect LOW(R8): Files to Change 의 `README.md` 행이 "불연속 배치 동작 갱신" 뿐이라 무엇을 적을지가 구현자 해석에 열려 있었다. 바꿀 문장("범위가 셀을 채운다" → "관문 날짜만 채우고 사이는 빈다")과 더할 한 줄(`dropped` 도 점유)을 명시했고, **시각 언어를 설명하지 않는다**(UI4)를 함께 적었다. 기계 검사는 두지 않는다 — 문서 문구를 grep 으로 고정하면 표현을 바꿀 때마다 게이트가 죽는다 · id=readme-scope |
| 2026-08-26 | 오독(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **범주 오류 · 수리 불가능 · CRITICAL ×5** L2 test(R8): "`runCalendarOccupancyCases()` 가 저장소에 없다" · "`DD31-FIXTURE` 표식이 코드에 하나도 없다" · "`rebuildIndex()` 가 아직 `startDate..endDate` 를 돈다" · "`getEventsForDate()` 가 아직 범위 비교다" 등 다섯. **전부 구현이 아직 없다는 관찰이고, 이것은 구현 전 플랜 심사다.** 플랜은 그 함수들을 "만드는 것" 으로 적고 있고 리뷰어 프롬프트도 "Plan under review" 라고 말한다. **플랜을 어떻게 고쳐도 만족시킬 수 없는 지적이다** — 만족하려면 구현이 이미 끝나 있어야 한다. 같은 리뷰어가 R7 에서는 같은 사실을 "expected for a plan" 이라고 적었으므로 라운드 간 판정 기준이 흔들린 것이다 · id=panel-plan-vs-impl-confusion |
| 2026-08-26 | 유예(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **DEFER_TO_BACKLOG · invariant pass 라운드의 비차단 지적** L2 invariant MEDIUM ×3 + LOW(R8): (a) 1-c 가 `gates`·`projectId` 를 **문자열 존재**로만 보고 구조(배열인지, `planned` 를 갖는지)는 안 본다 — 플랜이 그 좁음을 명시하고 있고 구조 불변식은 전반부(M2a)의 몫이다, (b) `link.download` 검사가 승인 시점이라 **승인 뒤 Task 0 전에** 바뀌면 낡은 증거가 된다 — 실체가 있으나 그 창을 닫으려면 Task 0 안에서 다시 봐야 하고 그것은 절차이지 게이트다, (c) 3-2c 기대값에 "독립 게이트가 없다" — R6 에서 요일 산술 유도를 더했고 리뷰어가 그 문단 앞의 옛 문장을 인용했다, (d) 판정식 접기 awk 가 병적 포맷에서 어긋날 수 있다. 넷 다 비차단이며 러너 도입 뒤 재평가한다 · id=m2-headless-runner |
| 2026-08-26 | 유예(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **구현 실행 중 유예 · DEFER_TO_BACKLOG** Task 0(구현 전 베이스라인 내보내기)·Task 4(재베이스라인·재내보내기·앵커 둘)·Validation 3-2/3-2b/3-2c/3-3·Validation 4(스모크 단언 0건)·Task 5(하루 재현 테스트)는 **확장을 로드한 Chromium 이 있어야 돈다.** 하네스(`test/positioning.smoke.html`)는 `chrome.storage.local` 이 진짜여야 하므로 `chrome-extension://` 오리진에서만 실행되고, 이 저장소에는 러너도 CI 도 없으며 이 세션의 브라우저 자동화 경로에는 확장을 로드할 수단이 없다. **Task 0 의 구현 전 스냅샷은 아직 복구 가능하다** — 전반부 종료 커밋 `a82debd` 가 `test/positioning.smoke.js` 를 추적하고 있으므로 그 시점을 체크아웃해 내보내면 같은 봉투를 얻는다(전반부 `work-calendar-m2.baseline.json` 이 `.gitignore` 98행에 걸려 영구 소실됐던 것과 다른 상황이다). 코드·하네스 쪽 기계 검사(Validation 1·1-b·1-c·2·2-b·2-c·2-d)는 전부 exit 0 이다 · id=m2-headless-runner |
| 2026-08-26 | 유예(2026-08-26) | .claude/plans/work-calendar-m2b.plan.md | **DEFER_TO_BACKLOG · 로컬 코드리뷰 LOW** `getEventsForDate()` 가 DD32 를 위해 문자열 비교 하나(`startDate <= dateKey && dateKey <= endDate`)에서 `events × gates` 중첩 스캔(`(event.gates \|\| []).some(...)`)으로 바뀌었다. 상한은 `MAX_IMPORT_EVENTS`(5,000) × `MAX_GATES_PER_EVENT`(12) = 6만 회이고 `renderPanel()` 한 번에 한 번 도므로 실측 영향은 무시할 수준이라 이번에 고치지 않는다. 고친다면 `dateKey` 가 렌더 창 안일 때 `eventsByDate` 를 재사용하고 창 밖일 때만 전체 스캔으로 떨어지는 형태이며, 그 분기를 지금 넣으면 "패널은 창 인덱스를 우회한다" 는 DD32 의 단순한 계약이 조건부가 된다 — 측정 없이 그 대가를 치를 이유가 없다. 헤드리스 러너로 렌더 시간을 재게 된 뒤 판단한다 · id=m2-headless-runner |
